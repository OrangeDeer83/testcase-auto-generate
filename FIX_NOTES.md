# 修復紀錄

> 記錄每次修 bug／改善 UX 時「為什麼壞掉、怎麼修好的、背後用到什麼可以遷移到其他情境的觀念」，主要是寫給使用者看的學習筆記，也順便讓之後回頭查、或下一個接手的 Claude 不用重新翻一次 diff 才搞懂修法。跟 [PROGRESS.md](PROGRESS.md) 的差異：PROGRESS.md 是變更歷史總覽（做了什麼），這份專注在單一問題的根因、修法、與原理本身。維護方式見 `.claude/skills/fix-notes/SKILL.md`。

## 2026-09-02 回覆澄清問題後，如果沒有任何用例被異動，畫面上完全沒有回應

**問題**：使用者回報回覆完一個澄清問題後，畫面上沒有得到任何回應，只看到問題數量從 3 個變成 2 個，看起來像是訊息送出去卻石沉大海，不確定自己的回覆到底有沒有被處理。

**修法**：根因出在剛上線的「AI 建議、人工確認套用」機制（見同一份文件更早的條目）——`handleSendMessage` 送出聊天訊息後，只有在 `res.pending_changes.length > 0`（AI 這次真的提出了用例調整建議）時才會在對話紀錄裡加一則摘要訊息；如果使用者這次的回覆沒有讓任何用例內容變動（例如單純回答問題、或明確表示「這個先不管」），`pending_changes` 就是空陣列，摘要訊息完全不會出現。同一時間，畫面上唯一會更新的是「尚未解決的澄清問題」區塊——如果這次回答讓問題數減少，這個區塊會重新渲染成比較短的清單，但**這個區塊本身的訊息內容是空字串**（`describeResult` 回傳 `content: ''`，只帶 `questions` 陣列，文字內容完全由問題清單本身撐場面），所以使用者感受到的就是「畫面上什麼文字都沒出現，只有問題數量默默變少」。修法很直接：`changeSummary` 改成無條件產生一則訊息，有建議就照舊講「AI 提出了 N 項用例調整建議」，沒有建議就明講「這次沒有提出用例調整建議，測試用例內容維持原樣」；順便把 `describeResult` 裡「已更新測試用例」這句話拿掉開頭的「已更新」（這句話在聊天情境下已經不準確——用例內容現在要人工套用才會真的變動，不是聊天回覆自動就更新了），改成中性的「目前共 N 筆用例」。

**背後的通用觀念**：**改變一個功能的資料流時，要連著把「使用者怎麼知道發生了什麼事」一起改，不能只改資料本身怎麼流動**。「AI 建議、人工確認套用」這個功能上線時，重點都放在「資料不再被直接覆蓋」這件事本身（見前一則條目），卻沒有意識到舊版「有變動就有摘要訊息、沒變動就沒有摘要訊息」這個邏輯，在新設計裡的前提已經整個變了——舊版沒有摘要訊息時，恰好都是「這次真的什麼都沒變」的情況，使用者感受不到差別；新版沒有摘要訊息，卻可能是「使用者剛回答了一個問題、系統也確實處理過，只是恰好沒有任何用例欄位需要跟著改」，這是完全不同的兩種「沒有東西可以顯示」，卻共用了同一段「不顯示任何東西」的程式碼路徑。這提醒了一件事：**任何「有事發生才顯示訊息」的省略邏輯，都要重新檢查前提是否還成立**，尤其是重構資料流之後——沉默有兩種：「什麼都沒發生」的沉默，跟「發生了、但沒有你在意的那種變化」的沉默，這兩種對使用者來說觀感完全不同，前者可以沉默，後者一定要講清楚，不然使用者沒辦法區分是系統沒反應、還是系統確實處理了只是結果剛好是「沒有異動」。

**檔案**：`frontend/src/pages/WorkspacePage.tsx`（`handleSendMessage` 的 `changeSummary` 改成無條件產生訊息；`describeResult` 拿掉「已更新」這個在聊天情境下不準確的措辭）。

**驗證方式**：`npx tsc --noEmit`、`npx vitest run`（45 個測試全過，純文案調整沒有新增邏輯需要測試）。瀏覽器實測（dev 環境 18002/5175，新建一個可刪除的測試專案，測完已刪除）：產生 6 筆用例、模型提出 4 個澄清問題，針對其中一個回覆「這個先不管，之後再說」（明確表示不處理，不會讓任何用例內容變動），確認送出後畫面上依序出現「這次沒有提出用例調整建議，測試用例內容維持原樣。」這則訊息、再接著顯示剩下 3 個問題的清單，不再是「什麼都沒說、只有問題數默默變少」。

## 2026-09-02 開新對話、還沒產生用例時，標題列沒有貼齊卡片邊緣

**問題**：使用者回報開新對話、還沒選素材產生用例的「選擇要使用的素材」畫面，上方標題列（對話名稱那一條）跟下面卡片的邊緣沒有完全貼齊，四個角落看得出一小段沒對齊、露出底色的縫隙，看起來像兩塊拼接歪掉的區塊。

**修法**：根因是 `.workspace-title-row` 這個標題列的樣式，是用「負 margin 把自己拉到跟 `.app-main` 的內距（24px／32px）齊平」這個技巧做出貼齊視窗邊緣的橫幅效果（見同一個檔案裡更早的註解說明）——但這個計算是假設 `.workspace-title-row` 直接是 `.app-main` 的子元素。「選擇要使用的素材」這個畫面（`WorkspacePage.tsx` 的 `!hasResult` 分支）卻把標題列包在 `.panel` 卡片裡面，`.panel` 自己有一組不同的內距（22px／26px）跟邊框、圓角、陰影，兩邊的內距數字對不上，負 margin 的算法就跟著跑掉，貼齊的效果失準，四個角落露出沒被背景蓋到的縫隙。已經套用結果、有測試用例的主畫面（同一個元件的另一個分支）沒有這個問題，因為那邊的標題列是直接放在沒有額外內距的 `.workspace-view` 裡，負 margin 的假設成立。修法不是去調整負 margin 的數字硬湊出正確位置（湊出來的數字換一個內距设定又會歪掉），而是讓兩個分支用同一種父層結構：把標題列從 `.panel` 卡片裡搬出來，改成跟 `.panel` 平行放在 `.workspace-view` 底下——這樣兩個分支的 `.workspace-title-row` 都符合它原本的假設（父層沒有額外內距），不用改標題列本身的任何樣式數字。

**背後的通用觀念**：**用負 margin／絕對定位把一個元素「拉出」父層的內距、貼齊更外層容器邊緣，這種技巧本質上是在元素與某個特定祖先層級之間建立一份隱性契約**——契約的內容是「父層的內距是固定的某個數字」，程式碼本身完全看不出這份契約，只有寫這段 CSS 的人（透過註解，或記在腦子裡）知道。這份契約只要被違反——像這次把同一個元件套進一個內距不同的容器——效果就會不動聲色地跑掉，而且不會出現任何錯誤或警告，只會在畫面上留下一段難以第一眼看出原因的縫隙。這類「用數字硬算出來貼齊」的樣式，比較安全的做法是要嘛把假設寫成清楚的註解並且每個使用的地方都驗證過，要嘛（像這次的修法）乾脆讓每個使用情境都符合同一份結構假設，不要讓同一段樣式規則被套用在假設不成立的父層底下。

**檔案**：`frontend/src/pages/WorkspacePage.tsx`（「選擇要使用的素材」畫面的 JSX 結構調整，`.workspace-title-row` 改成 `.workspace-view` 的直接子元素，`.panel` 卡片只包住素材選取跟產生按鈕的部分）。

**驗證方式**：`npx tsc --noEmit`、`npx vitest run`（38 個測試全過，這個修法本身是純版面調整，沒有新增邏輯需要測試）。瀏覽器實測（dev 環境 18002/5175，新建一個可刪除的測試專案，測完已刪除）：開一個新對話、進入「選擇要使用的素材」畫面，確認標題列背景完整延伸到卡片四個邊緣，跟下面的白色卡片之間沒有露出縫隙；也重新檢查了已經有用例的主畫面（另一個分支），確認沒有被這次的結構調整波及，標題列一樣正常貼齊。

## 2026-09-01 聊天式編輯改成「AI 建議、人工確認套用」，不再自動覆蓋用例內容

**問題**：使用者反思整個對話助手的可控性，討論到「與其想辦法讓 AI 更聽話，不如讓真正的資料異動先經過人工確認再生效」——原本的設計是聊天訊息一送出，AI 回傳的內容就直接覆蓋整份 `test_cases`（靠 `enforce_lock_on_llm_result` 事後保護鎖定用例、靠前端的 `diffTestCases` 事後標黃提示哪裡變了），錯誤或不如預期的修改要等使用者看到結果才能發現，且已經是既成事實，得再花一次來回請 AI 改回去。

**修法**：把「AI 提出的內容」跟「畫面上正式生效的內容」拆成兩份狀態：

1. 後端新增 `PendingChange`（`backend/app/models/test_case.py`）：`{id, action: "add"|"update"|"delete", data}`。`/chat` 收到 AI 回應後，不再直接把 `test_cases` 換掉，而是用新函式 `compute_pending_changes`（`test_case_lock.py`）拿「AI 這次的完整回應」跟「目前正式生效的內容」用 **id** 比對（不是名稱——原因跟這個專案已經踩過的鎖定用例改名 bug 一樣：AI 可能連名稱一起改，名稱比對會誤判成刪除+新增），算出真正有差異的部分，包成一筆一筆待確認的建議。正式的 `test_cases` 維持原樣不動，`result_version` 也不跟著變——這代表這次聊天呼叫**沒有任何資料真的被改動**，純粹是多了幾筆建議。
2. 使用者要到畫面上的用例卡片，針對每一筆建議個別點「套用」或「忽略」（新增/刪除/修改動作分別對應「新增此用例」「確認刪除」「套用」，忽略統一是「忽略」/「保留」），才會透過新的 `POST .../pending-changes/{id}/apply`／`.../dismiss` 兩個 endpoint 真正寫進正式清單。套用當下才做的鎖定檢查（`LockedCaseConflictError`）是必要的第二道防線：建議提出的當下這筆用例可能還沒被鎖，但使用者按套用之前，另一個分頁把它鎖定了，不能只信任建議產生當時的狀態。
3. 使用者可以同時累積好幾筆不同用例的待處理建議（先不理上一輪的建議、繼續往下聊），用 `merge_pending_changes` 處理；但同一筆用例只保留「最新一輪」的建議，不會疊加成兩筆互相衝突的項目。
4. 只有初次「產生測試用例」（`/generate`）維持原本直接寫入的行為——這是刻意的範圍限制，不是遺漏：初次產生沒有「之前的內容」可以比較，全部套用建議清單化只會變成「按 N 次套用才看得到自己剛產生的用例」，體驗更差。
5. 前端 `TestCaseTable.tsx` 針對每筆有待確認建議的用例，在卡片頂端顯示提示 banner（新增/修改用藍色，刪除用紅色）與套用/忽略按鈕；修改類的建議還會在對應欄位下方多顯示一行「AI 建議：...」（跟既有「原本：...」的灰字比對是同一種視覺語言，方便直接對照）；新增類的建議因為對應的用例還不存在於清單裡，另外用一張虛線邊框的卡片呈現在列表最下方。

**背後的通用觀念**：**「讓 AI 更準確」跟「讓 AI 的輸出不能未經確認就影響到正式資料」是兩種互補、但本質不同的可靠性策略**——前者持續改善 prompt、加驗證規則，永遠有改不完的邊界情況；後者直接改變資料流的形狀，把「AI 的輸出」跟「系統的正式狀態」中間插入一個人工關卡，不管 AI 這次表現得多好或多差，錯誤的影響範圍都被鎖在「使用者還沒點套用」這個安全的中介狀態裡，沒有辦法繞過去。這是很多需要「自動化 + 高風險資料變更」場景（不只是 AI，任何自動化批次處理、資料匯入都適用）的共通設計模式：**區分「產生建議」跟「套用建議」兩個階段，並且讓兩者之間必須經過一個明確、使用者可以拒絕的關卡**，比起單純追求「產生建議的那一步不要出錯」更根本地解決了「出錯的代價」這件事。

**檔案**：`backend/app/models/test_case.py`（新增 `PendingChange`，`GenerationResult` 新增 `pending_changes`）、`backend/app/services/test_case_lock.py`（新增 `compute_pending_changes`／`merge_pending_changes`／`apply_pending_change`／`dismiss_pending_change`）、`backend/app/routers/conversations.py`（`/chat` 改成建立待確認建議而非直接覆蓋，新增 `pending-changes/{id}/apply`／`dismiss` 兩個 endpoint）；前端 `frontend/src/types.ts`（新增 `PendingChange`，`GenerationResult` 新增 `pending_changes`）、`frontend/src/api.ts`（新增 `applyPendingChange`／`dismissPendingChange`）、`frontend/src/diffTestCases.ts`（新增 `describeCaseFieldChanges`／`getProposedFieldValues`，用 id 比對單一已知配對的用例，不靠名稱猜）、`frontend/src/components/TestCaseTable.tsx`（新增建議 banner／套用忽略按鈕／新增建議卡片）、`frontend/src/pages/WorkspacePage.tsx`（移除聊天後「直接比對 test_cases 前後差異」的邏輯，改成依 `pending_changes` 產生摘要訊息與捲動定位）、`frontend/src/index.css`（新增 `.case-proposal-*`／`.proposed-value` 樣式）。

**驗證方式**：`.venv/Scripts/python.exe -m pytest`（後端 82 個測試全過，新增 `compute_pending_changes`／`merge_pending_changes`／`apply_pending_change`／`dismiss_pending_change` 的完整測試，含「同一筆用例新建議蓋掉舊建議」「套用當下才發現已鎖定」等情境）、`npx tsc --noEmit`、`npx vitest run`（前端 45 個測試全過，新增 `describeCaseFieldChanges`／`getProposedFieldValues` 測試）。瀏覽器實測（dev 環境 18002/5175，真實 LLM，測完已刪除測試用專案）：產生 6 筆用例後，一次訊息請 AI 同時做三件事（改一筆優先級、新增一筆、刪除一筆），確認畫面上分別出現藍色「修改」banner（帶「AI 建議：P1」的欄位提示）、紅色「刪除」banner、虛線邊框的「新增」建議卡片，且用例總數在套用前維持原本的 6 筆不變；分別點擊三個「套用／確認刪除／新增此用例」按鈕後，確認網路請求都正確打到 `pending-changes/.../apply`、畫面即時反映（優先級變成 P1、被刪除的用例消失、新用例加入清單、總數回到 6 筆），且過程中原本 5 個未回答的澄清問題全程正確保留、沒有被誤判成已解決。

## 2026-09-01 回覆澄清問題時，自動只把相關用例送給模型，不用手動勾選

**問題**：使用者原本的訴求是「想減少呼叫模型時的輸入量」，第一版做法（PR #37）是加一個「選取用例提問」的手動勾選 UI：使用者自己勾幾筆用例、按「針對已選提問」，才會縮小這次送給模型的內容範圍。做完之後使用者才澄清方向錯了：他要的不是「自己去針對某些用例主動提問」，而是「當**助手**主動提出澄清問題、我在聊天欄裡正常打字回覆時**，不要因為這樣就自動把全部用例的完整內容都送進去**」——這是完全不同的兩個情境：前者是使用者主動縮小範圍，後者是系統該自己判斷「這次回覆其實只跟其中幾筆用例有關」，使用者根本不應該要手動做任何事。

**修法**：先把 PR #37 的手動勾選功能整個從 `master` revert 掉（程式碼保留在原本的 `feat/scoped-test-case-question` branch，沒有真的刪掉，日後如果真的需要「手動指定範圍」這種互動方式還能拿出來用），再重新設計自動判斷的版本：

1. `ClarificationQuestion` 新增 `related_test_case_names` 欄位，比照既有的 `based_on_images`（模型自己標記某筆用例是根據哪幾張截圖寫的）同一種模式——請模型在提出每個澄清問題時，順便標註「這個問題跟哪幾筆用例的名稱有關」。用「名稱」而不是內部的 `id`，是因為同一輪回應裡新建立的用例，在模型當下的視角根本還沒有 `id`（`id` 是後端解析完 JSON 之後才指派的），模型只認得住名稱。
2. 使用者在聊天欄回覆時，後端（`resolve_related_test_case_ids`）把「目前尚未解決的所有澄清問題」各自標記的用例名稱取聯集、解析成一組 id，當作這次自動縮小的範圍；`build_chat_messages` 沿用 PR #37 已經寫好的「範圍內送完整 JSON、範圍外只送名稱」這套機制，把它從「使用者手動勾選觸發」改成「有待處理問題時自動觸發」。整個過程使用者不用做任何額外操作，跟平常打字回覆完全一樣。
3. **安全網**：模型自己標記關聯用例，一定會有標錯或漏標的風險，而「漏標」的代價遠比「多標」嚴重——會導致使用者這次回覆真正需要更新的某筆用例，因為不在自動判斷出的範圍內，內容送不到模型手上。應對方式分兩層：(a) prompt 明確要求模型「不確定就多標、不要漏標」；(b) 只要有任何一個待處理問題完全沒標記關聯用例，`resolve_related_test_case_ids` 就直接放棄整個自動縮小、回傳 `None`，退回原本「送完整清單」的行為——不做「猜一半」這種更危險的中間狀態。另外新增 `needs_full_context` 欄位，讓模型在縮小範圍後如果發現「這次使用者的訊息其實牽涉到範圍外的內容、看得到的資訊不夠處理」時可以誠實回報，後端收到這個信號會自動改用完整清單重新問一次、對使用者完全透明（浮動視窗會顯示「🔄 這則回覆需要完整的測試用例清單，正在重新確認…」），不會讓模型在資訊不足的情況下硬猜出一個結果。

**背後的通用觀念**：（1）**「使用者說要什麼」跟「使用者要的效果由誰觸發」是兩件要分開確認的事**——這次的教訓是，聽到「想減少輸入量」就急著做一個「讓使用者自己指定範圍」的 UI，滿足了字面上的需求（範圍確實縮小了），卻猜錯了「誰來決定範圍」這件事：使用者要的是系統自動判斷、不要求他多做動作。需求裡「做什麼」跟「誰來做」經常被含糊帶過，尤其原始需求本身帶有一點模糊性時，更需要在動手做之前把這兩件事都問清楚，而不是看到一個可行的實作方向就直接做下去。（2）**縮減送給 LLM 的內容時，「漏送」跟「多送」不是對稱的風險**：多送只是浪費一點 token、跟沒做這個優化前的狀況一樣安全；漏送則可能讓模型在看不到關鍵內容的情況下，卻還是「盡力回答」，產生一個看似合理、實際上是憑空猜測或維持舊內容不變的結果，而使用者完全不會意識到這件事發生過。任何「自動判斷範圍以縮減內容」的機制，安全的預設值都應該是「不確定就退回完整範圍」，而不是「不確定就縮小到看起來合理的範圍」——這跟這個專案一貫的「LLM 不可以亂猜」原則是同一件事的兩種不同層面（一個是模型輸出內容不能亂猜，一個是系統決定要給模型看什麼內容時也不能亂猜）。

**檔案**：`backend/app/models/test_case.py`（`ClarificationQuestion` 新增 `related_test_case_names`，`GenerationResult` 新增 `needs_full_context`）、`backend/app/services/prompt_builder.py`（`SYSTEM_PROMPT`／`SYSTEM_PROMPT_CHAT` 新增標記關聯用例與 `needs_full_context` 的規則，`build_chat_messages` 重新加入 `scoped_ids` 參數）、`backend/app/services/test_case_lock.py`（新增 `resolve_related_test_case_ids`、`merge_scoped_llm_result`）、`backend/app/routers/conversations.py`（`/chat` 自動計算 `scoped_ids`，收到 `needs_full_context` 時改用完整清單重試一次，新增 `notice` SSE 事件）；前端 `frontend/src/api.ts`（`streamGenerationResult`／`sendChatMessage` 新增 `onNotice`）、`frontend/src/pages/WorkspacePage.tsx`（新增 `retryNotice` state 與 `handleStreamNotice`）、`frontend/src/components/FloatingChat.tsx`／`ChatPanel.tsx`（往下傳遞並顯示 `retryNotice`）、`frontend/src/index.css`（新增 `.chat-stream-notice` 樣式）。PR #37 的手動勾選功能（`TestCaseTable.tsx` 的勾選框、`WorkspacePage.tsx` 的「選取用例提問」按鈕與範圍列等）已經整批 revert，程式碼保留在 `feat/scoped-test-case-question` branch。

**驗證方式**：`.venv/Scripts/python.exe -m pytest`（後端 70 個測試全過，新增 `resolve_related_test_case_ids`／`merge_scoped_llm_result`／`build_chat_messages` 範圍模式／`needs_full_context` 解析等測試）、`npx tsc --noEmit`、`npx vitest run`（前端 38 個測試全過）。瀏覽器實測（dev 環境 18002/5175，新建一個可刪除的測試專案，測完已刪除乾淨）：貼上一份刻意留白（登入功能明確、搜尋功能規格不完整）的需求文字，產生出 7 筆用例，模型正確提出 5 個澄清問題並各自標記關聯用例（例如「搜尋結果排序規則」的問題標記了 3 筆搜尋相關用例，「登入失敗訊息文案」的問題標記了 1 筆登入用例）；一次回覆全部 5 個問題後，查後端 log 確認這次請求 `scoped=4`（7 筆裡只有 4 筆真的送了完整內容給模型），且回傳結果正確只更新了那 4 筆、另外 3 筆（登入成功、登入失敗的另外兩個欄位驗證用例）維持原封不動，5 個問題也全部正確標記為已解決，沒有觸發 `needs_full_context` 重試（代表這次的標記是準確的）。
## 2026-09-01 呼叫模型時改成串流輸出，即時顯示正在產生的用例／問題

**問題**：使用者問「模型操作的過程中會有回覆嗎？我可以看到這些回覆讓我知道他正在運作嗎？」——原本的做法是後端整批等模型把完整回應都產生完才一次拿到結果，前端在這幾分鐘的等待期間完全看不到任何內容，只有一個單純的「思考中…（已等待 N 秒）」計時器，感覺不到模型「正在運作」，尤其在前一則 FIX_NOTES 記錄的逾時預算調高到 280 秒之後，這段空等的時間更長了。使用者要求改成像 Claude 聊天介面那樣，即時把模型正在做的事照實描述出來。

**修法**：分後端、前端兩層：

1. **後端串流**：`llm_client.py` 把原本一次性呼叫的 `chat_completion` 換成 `stream_chat_completion`（`stream=True`），逐段 `yield` 模型吐出來的文字片段，不再整批等完。`/generate`、`/chat` 這兩個 endpoint 改成回傳 `StreamingResponse`，用 Server-Sent Events（SSE）格式把每個片段包成 `delta` 事件即時轉送給前端；模型完整回應收完之後，還是照原本的邏輯做 JSON 解析、鎖定保護、存檔，最後包成一個 `result` 事件送出（前端看到這個事件才算真正拿到最終結果，跟串流之前的行為完全一致）；呼叫失敗（逾時、連線錯誤）時包成 `error` 事件——這裡有個限制：串流一旦開始輸出，HTTP 狀態碼跟標頭就已經送出去了，沒辦法再像以前一樣用 `HTTPException` 中途改變回應狀態碼，只能靠這個 `error` 事件讓前端自己判斷並顯示對應訊息。
2. **前端即時顯示**：新增 `streamProgress.ts` 的 `extractStreamProgress`，用正規表示式從「目前已經收到、還沒收完的原始 JSON 文字」裡抓出**已經確定寫完的完整欄位值**（`"name": "..."` 的測試用例名稱、`"question": "..."` 的澄清問題），轉成「📝 用例名稱」「❓ 問題內容」這種一句話的進度描述，依序顯示在「思考中」下面；只抓「已經有結尾引號、確定寫完」的值，寫到一半的欄位不會被誤判抓到半截內容。這個抓取邏輯刻意保持單純：不去嘗試解析或修復還沒收完的 JSON 結構本身（那是收完整份回應後 `parse_generation_result` 才做的事），純粹是「照實轉述模型目前已經產生的內容」，抓不到就不顯示，不編造、不猜測，符合這個專案「LLM 不可以亂猜」延伸到前端顯示邏輯的一貫原則。
3. 進度清單的狀態（`streamingLines`）比照同一天稍早「草稿」「已等待秒數」兩個 bug 學到的教訓，放在不會被浮動視窗收合影響的 `WorkspacePage` 那一層，不是 `ChatPanel` 自己的 state。

**背後的通用觀念**：（1）**「串流」不是只有聊天機器人那種自然語言逐字輸出才能用**——這個專案的模型輸出其實是結構化 JSON，不是一段自然語言，但一樣可以用串流傳輸原始位元組／文字，差別只在於「怎麼把還沒收完的原始內容，轉成使用者看得懂的東西」這一步要自己想辦法，不能直接把原始 JSON 片段秀給使用者看（會看到一堆破碎的大括號、引號）。用「只抓已經確定寫完的完整欄位值」這種保守策略，換取「不用真的解析半成品 JSON」的簡單實作，是很多「即時預覽還沒傳完的結構化資料」情境都能用的技巧。（2）**串流回應（`StreamingResponse`／SSE）跟一般的請求-回應模式，在「怎麼回報錯誤」這件事上有根本性的差異**——一般回應可以在處理失敗時直接改變 HTTP 狀態碼（例如回 502），但串流一旦開始輸出，狀態碼與標頭已經送出去、無法再改，只能透過應用層自訂的「錯誤事件」讓前端自己判斷。這是任何要把現有的「整批回應」端點改成串流時都會踩到的固定陷阱，錯誤處理的邏輯必須整個從「拋例外讓框架處理」改成「用資料格式本身表達錯誤」。

**檔案**：`backend/app/services/llm_client.py`（`chat_completion` 換成串流版 `stream_chat_completion`）、`backend/app/routers/conversations.py`（`/generate`、`/chat` 改成 `StreamingResponse` + SSE，新增 `_sse_event`／`_stream_llm_events` 共用邏輯）、`backend/tests/test_conversations_router.py`（改測 `_stream_llm_events` 的串流輸出與錯誤轉換行為）；新增 `frontend/src/streamProgress.ts`（`extractStreamProgress` 純函式）與對應測試 `streamProgress.test.ts`；`frontend/src/api.ts`（新增 SSE 讀取邏輯 `readSseEvents`／`streamGenerationResult`，`generate`／`sendChatMessage` 新增 `onDelta` callback）、`frontend/src/pages/WorkspacePage.tsx`（新增 `streamingLines` state 與 `handleStreamDelta`）、`frontend/src/components/FloatingChat.tsx`／`ChatPanel.tsx`（往下傳遞並顯示 `streamingLines`）、`frontend/src/index.css`（新增 `.chat-stream-progress` 樣式）。

**驗證方式**：`.venv/Scripts/python.exe -m pytest`（後端 57 個測試全過，含重寫的串流／錯誤事件測試）、`npx tsc --noEmit`、`npx vitest run`（前端 38 個測試全過，含新增的 `extractStreamProgress` 6 個測試，涵蓋「欄位還沒寫完不會被誤判抓到」這個關鍵情境）。瀏覽器實測（dev 環境 18002/5175）：先直接用 Python 對 dev 後端的 `/chat` endpoint 發真實請求，讀取前幾個原始 SSE 位元組，確認 `Content-Type: text/event-stream` 且逐字收到 `event: delta` 事件（真的是一個字一個字送過來，不是整包才送）；再到瀏覽器裡實際送出一則訊息，20 秒內親眼看到「思考中」下面依序冒出 5 個「📝 用例名稱」的進度項目，跟這個對話當下實際的 5 筆用例完全對應，最後也正確收到 `result` 事件、跟串流之前的行為一樣完成整個對話流程（本次變動摘要、測試用例更新都正常）。測試過程中在 dev 環境留下的測試訊息已用 API 移除，不影響使用者原本的對話紀錄。

## 2026-09-01 頻繁遇到「模型服務暫時無回應」，調高單次逾時預算、拿掉自動重試

**問題**：使用者反映最近多次呼叫模型都遇到「⚠️ 模型服務暫時無回應，請稍後再試一次」。查正式環境的 `backend/logs/app.log`，2026-09-01 當天的呼叫記錄呈現一個清楚的模式：

| 時間 | 結果 | 耗時 |
|---|---|---|
| 10:10 | 失敗 | ~308 秒 |
| 10:31 | **成功** | 297.5 秒 |
| 10:45 | 成功 | 93 秒 |
| 11:04 | 失敗 | ~301 秒 |
| 11:21 | 成功 | 130.6 秒 |
| 11:33 | 失敗 | ~300 秒 |

失敗的請求全部落在 300 秒左右（正好是舊設定 `timeout=150 秒 × max_retries=1 次` 的總預算上限），而且失敗跟成功的請求內容大小差不多（都是 2 張圖片、十幾萬字元的普通請求，不是先前處理過的「28 張截圖」那種異常過量情境）——代表根因不是請求太大，是**內部模型服務本身這陣子回應變慢了**，常態需要 200～300 秒才有回應，而不是像 8/28 那次事故時的正常水準（幾十秒內）。

**修法**：原本 `timeout=150.0, max_retries=1` 是為了解決更早之前「官方預設 600 秒逾時＋重試 2 次，卡滿要等快 30 分鐘」的問題而調小的（見更早的 FIX_NOTES 條目）。但 SDK 的自動重試是**完全重新發送一次請求，不會接續前一次已經跑到一半的進度**——如果模型服務本身這陣子的正常回應時間就需要 200～300 秒，「兩次各自 150 秒」的做法等於兩次都不夠用、穩定失敗，跟 `109fc647` 那筆成功記錄（用滿了兩次嘗試的預算、耗時 297.5 秒才成功）互相印證。改成 `timeout=280.0, max_retries=0`：單次嘗試給更長的預算（涵蓋目前觀察到的大部分正常回應時間），不自動重試——最差情況下使用者等待的總時間（280 秒）反而比舊設定的最差情況（150×2=300 秒）更短，但單次嘗試成功的機率大幅提高。使用者原本就會在畫面上看到清楚的失敗提示（見同一天稍早的「浮動視窗裡完全看不到任何通知」修法），可以自行決定要不要手動重新送出，拿掉自動重試不影響這個手動重試的路徑。

**背後的通用觀念**：**「重試」在網路請求的情境下有兩種完全不同的意義：一種是「重新建立連線、換一個健康的伺服器實例」（對付偶發的連線層問題很有效），另一種是「同一個請求整個重新跑一次」（對付「這個請求本身就是需要很久」完全沒有幫助，甚至更糟——因為等於把使用者已經等過的時間全部浪費掉，又要重新等一次全新的處理時間）。這個專案用的 OpenAI SDK 的 `max_retries` 屬於後者：逾時了就整個請求重新發送，不會延續、也不會縮短下一次需要的處理時間。當觀察到「失敗的請求耗時剛好卡在 `timeout × (max_retries+1)` 這個總預算上限附近」時，這是一個很明確的訊號：不是逾時設定不夠寬容，就是重試策略選錯了方向——這種情況下，把「切成兩次各自較短的嘗試」改成「一次給足夠長的預算」，往往比增加重試次數更有效，而且總等待時間可能反而更短。

**檔案**：`backend/app/services/llm_client.py`（`OpenAI` client 的 `timeout` 從 150.0 調整為 280.0，`max_retries` 從 1 調整為 0）。

**驗證方式**：`.venv/Scripts/python.exe -m pytest`（後端 57 個測試全過，沒有測試直接斷言這兩個數值，是純設定調整）。這次沒辦法在瀏覽器裡實際等一次 280 秒驗證行為（模型服務本身的回應時間不是這個專案能控制或模擬的變因），依據的是 OpenAI SDK 官方文件對 `timeout`／`max_retries` 這兩個參數的既有行為（已經是這個專案先前就在用、驗證過的機制，只是調整數值），加上從正式環境 log 裡實際觀察到的耗時分布來決定新的數值；**這個改動只調整了 dev 環境／`master` 分支，還沒同步到正式環境**——使用者在查這個問題的當下，正式環境還有一個請求正在處理中，為了不中斷那個請求，特別避免在診斷過程中重啟或動到正式環境的後端 process，同步時機留給使用者之後自行決定。之後如果同一種「耗時卡在總預算上限附近」的模式再次出現，代表 280 秒也不夠用，需要再往上調或改用其他因應方式（例如背景非同步處理、輪詢結果），而不是繼續無止盡加大 timeout 數字。

## 2026-08-31 收合浮動聊天視窗後重新展開，「已等待 N 秒」的計時器會歸零

**問題**：使用者送出訊息、模型還在處理時按叉叉收合浮動聊天視窗，重新展開後「思考中…（已等待 N 秒）」的秒數從頭算起，看起來像是等待時間被重置了，實際上模型還在處理同一次請求，並沒有真的重新開始等待。

**修法**：根因跟同一天稍早「收合視窗草稿消失」那個 bug 完全一樣的機制：`elapsedSeconds` 原本是 `ChatPanel` 自己的 `useState(0)`，靠 `useEffect` 監看 `busy` 這個布林值，`busy` 變 `true` 就從 0 開始每秒 +1 累加。`ChatPanel` 收合時整個被 unmount，內部累加的秒數當然被銷毀；重新展開時元件重新掛載，`busy` 仍然是 `true`，於是又從 0 重新累加一次——秒數看起來「歸零」，其實是元件重新開始算而已，不是真的每次都對。修法是不要用「內部累加」這種容易被 unmount 打斷的算法，改成在不會被 unmount 的 `WorkspacePage` 那一層額外記一個 `busyStartedAt`（這次請求真正開始的時間戳，`Date.now()`），往下一路傳到 `ChatPanel`；`ChatPanel` 顯示的秒數改成每次都用「現在時間 − `busyStartedAt`」重新計算，而不是累加一個自己保管的計數器。這樣不管這個元件中途被 unmount、重新掛載幾次，算出來的永遠是這次請求「真正」經過的秒數，跟元件自己的生命週期完全無關。

**背後的通用觀念**：**「用一個從 0 開始、每隔固定時間 +1 的計數器」跟「拿現在時間去減一個起始時間戳」，這兩種算「經過了多久」的寫法，只有在計數器保證不會中途被重置或遺失時才等價；一旦計數器可能因為元件 unmount、頁面切換、或任何原因被打斷重來，只有記錄「起始時間點」再即時反算才是正確、不怕中斷的做法**。這是跟同一天稍早的草稿消失 bug 同一個根因家族的第二個症狀（同一個元件的兩種不同 state 都因為「浮動視窗收合會 unmount ChatPanel」這件事而各自出包），值得記住的通用教訓是：一旦發現一個元件會被有條件地 unmount／remount（收合展開、路由切換、列表項目重新排序造成 key 改變等都算），這個元件裡任何「需要跨越那個 unmount 邊界持續存在」的狀態，都不能用元件自己的 `useState` 存，同時也要留意「累加型」的邏輯（像秒數計時器）在這種情境下特別容易出錯，因為它們的正確性依賴「從頭到尾沒有中斷過」這個經常不成立的假設。

**檔案**：`frontend/src/pages/WorkspacePage.tsx`（新增 `busyStartedAt` state，`handleGenerate`／`handleSendMessage` 開始與結束時一併設定，往下傳給 `FloatingChat`）、`frontend/src/components/FloatingChat.tsx`（新增 `busyStartedAt` prop 並往下傳給 `ChatPanel`）、`frontend/src/components/ChatPanel.tsx`（`elapsedSeconds` 改成用 `busyStartedAt` 即時反算，不再是元件內部累加的計數器）。

**驗證方式**：`npx tsc --noEmit`、`npx vitest run`（32 個測試全過）。瀏覽器實測（dev 環境 18002/5175）：暫時攔截 `window.fetch` 讓 `/chat` 請求永遠不 resolve（模擬長時間處理中），送出訊息後等待幾秒，確認「已等待 N 秒」正常累加；按叉叉收合視窗、實際等待數秒（收合期間計時器本來就不會顯示，但背後的時間仍在流逝）後重新展開，確認顯示的秒數是收合前後加總的真實經過時間，不是從 0 重新算。測試過程中不慎在 dev 環境留下幾筆測試用的聊天紀錄，已用直接呼叫 API 的方式移除，不影響使用者原本的對話紀錄，也確認 `last_result.test_cases` 沒有被測試用的假回應污染。

## 2026-08-31 收合浮動聊天視窗、或切換對話時，打到一半的草稿會消失

**問題**：使用者按浮動聊天視窗右上角的叉叉關閉視窗、或切換到另一個對話再切回來，打到一半、還沒送出的訊息內容就不見了。使用者的期待很明確：草稿只要活在同一個瀏覽器分頁的這次操作期間（session）就好，重新整理頁面或關閉分頁讓它消失是可以接受的；但單純收合視窗、或在對話之間切換來切去，不應該把還沒送出的內容弄丟；而且每個對話的草稿要各自獨立，不能互相蓋掉。

**修法**：根因是草稿（`draft`）原本用 `useState` 存在 `ChatPanel.tsx` 元件自己裡面，而 `ChatPanel` 只有在浮動視窗展開（`open === true`）時才會被 `FloatingChat.tsx` 渲染出來——按叉叉收合時 `FloatingChat` 直接不渲染 `ChatPanel`，這個元件連同它裡面的 `draft` state 就整個被 React unmount 銷毀掉了，草稿當然就沒了；切換對話則是換了一個 `conversationId`，`WorkspacePage` 重新掛載，同樣的 unmount 也會發生一次。修法是把草稿的 state 往上移到 `ProjectLayout.tsx`（這一層在使用者停留於同一個專案期間、不管展開/收合浮動視窗或切換哪個對話都不會被 unmount），存成 `Record<對話 id, 草稿內容>`，透過既有的 `ShellContext` 往下傳給 `WorkspacePage`，`WorkspacePage` 依目前的 `conversationId` 取出對應的草稿，再往下傳給 `FloatingChat`、`ChatPanel`。`ChatPanel` 原本自己管理的 `useState('')` 整個拿掉，改成完全由外層控制的 controlled component（`draft` 讀外面傳進來的值、`onDraftChange` 把使用者輸入的內容回傳給外層存），元件本身不再擁有這份狀態，也就不會因為自己被 unmount 而弄丟它。因為 `ProjectLayout` 的這份 state 純粹活在瀏覽器記憶體裡，沒有寫進 `localStorage`／後端，重新整理頁面或關閉分頁就會自然消失，剛好符合使用者說的「只要在 session 內保留就好」。

**背後的通用觀念**：**React 元件的 `useState` 只活在這個元件被掛載（mounted）的期間，元件一旦因為條件渲染（像這裡的 `if (!open) return ...`）或路由切換（像這裡換了 `conversationId`）被 unmount，它自己的 state 就會被徹底銷毀，重新掛載時是全新的、初始值的 state，不是「暫停後恢復」**——這是很多「資料神秘消失」bug 的共同根因：直覺上會覺得「我只是把視窗關起來」，但對 React 來說，如果那個「視窗」在程式碼裡是條件渲染出來的，關起來這個動作在底層其實是「把整棵子樹連同它所有的 state 一起摧毀」。判斷一份狀態該放在哪一層元件，關鍵問題永遠是：「這份資料需要活多久、跨越哪些操作還要保留？」把它放在資料需要存活的整段期間內、始終保持掛載的最上層祖先元件裡（這裡是 `ProjectLayout`，跨越了浮動視窗開關、對話切換這兩種操作），而不是放在使用者實際看到、操作的那個元件本身——這兩者的生命週期經常對不上。

**檔案**：`frontend/src/pages/ProjectLayout.tsx`（`ShellContext` 新增 `draftsByConversation`／`setDraftForConversation`，新增對應的 state 與 setter）、`frontend/src/pages/WorkspacePage.tsx`（從 context 取出草稿並往下傳給 `FloatingChat`）、`frontend/src/components/FloatingChat.tsx`（新增 `draft`／`onDraftChange` props 並往下傳給 `ChatPanel`）、`frontend/src/components/ChatPanel.tsx`（移除自己的 `useState('')`，改成完全受控的 `draft`／`onDraftChange` props）。

**驗證方式**：`npx tsc --noEmit`、`npx vitest run`（32 個測試全過，這次改動是把 state 換位置、沒有新增可獨立測試的純邏輯）。瀏覽器實測（dev 環境 18002/5175）：在浮動聊天視窗打一段文字，按叉叉關閉再重新點開，確認文字還在；切換到另一個對話，確認那邊的草稿欄位是空的（不會被前一個對話的內容污染），輸入另一段文字後再切回原本的對話，確認兩邊的草稿內容都各自保留、沒有互相覆蓋。

## 2026-08-31 聊天送出失敗（例如模型逾時）時，浮動視窗裡完全看不到任何通知

**問題**：使用者反映：模型逾時沒有回應時，常常只看到「思考中…（已等待 N 秒）」的計時器直接消失，感覺不到有任何錯誤通知。追查程式碼後發現：`/chat` 逾時確實會被正確攔下轉成「模型服務暫時無回應，請稍後再試一次」的錯誤訊息，畫面上也確實有呼叫 `setError(...)` 顯示——但顯示的位置是 `ProjectLayout.tsx` 裡跨頁共用的 `.error-banner`，畫在整個頁面最上方；而使用者平常操作聊天，看的是右下角的浮動聊天視窗（`FloatingChat.tsx`，`position: fixed`，跟頁面本身的捲動、版面完全脫鉤）。錯誤訊息雖然「有顯示」，但顯示在使用者視線根本不會看到的地方，等於沒通知到。

**修法**：`ChatMessage` 新增 `isError?: boolean` 欄位。`WorkspacePage.tsx` 的 `handleSendMessage` 送出失敗時，除了原本的 `setError(...)`，額外在對話紀錄裡插入一則 `isError: true` 的助手訊息（內容就是同一句錯誤訊息），並照原本的方式存檔（`persistChatLog`），確保重新整理後這則錯誤紀錄還在。`ChatPanel.tsx` 渲染時，`isError` 的訊息改用警示樣式（`.chat-bubble-error`，跟頁面上方 `.error-banner` 同一組紅色系配色）並在文字前加上 ⚠️，直接顯示在原本「思考中」泡泡消失後的同一個位置——使用者不用移開視線去找页面上方的訊息，失敗的當下眼前就看得到。

同時修正了 2026-08-31 稍早那次「工作區標題列改成滿版背景」的一個連帶副作用：`.workspace-title-row` 用 `margin-top: -24px` 把自己往上拉貼齊 `.app-main` 頂端，這個負 margin 沒辦法區分「上面什麼都沒有」跟「上面剛好有 `.error-banner`」兩種情況——如果 `.error-banner` 的 `margin-bottom`（原本 16px）小於這個負 margin 的量（24px），標題列的背景就會往上蓋到錯誤訊息最下面的幾個像素。把 `.error-banner` 的 `margin-bottom` 從 16px 調整成剛好 24px，兩者互相抵銷，不管有沒有錯誤訊息，標題列都不會蓋到任何內容。

**背後的通用觀念**：（1）**「有沒有觸發錯誤處理」跟「使用者有沒有真的看到這個錯誤」是兩件事，只驗證前者很容易漏掉後者**——這個專案裡已經有正確的逾時偵測、正確的錯誤訊息轉換、正確的 `setError` 呼叫，整條錯誤處理鏈路在程式邏輯上完全正確，唯一的問題是「顯示位置」跟「使用者當下的視覺焦點」對不上：畫面上同時存在多個獨立的 UI 區塊（頁面本身的捲動內容、浮動視窗）時，共用一個全域錯誤狀態、固定顯示在某個位置，沒辦法保證使用者在每個情境下都會看到——尤其當其中一個區塊是 `position: fixed` 的浮動視窗，跟頁面的捲動位置完全無關時。設計錯誤通知時，除了「錯誤有沒有被攔下、訊息夠不夠清楚」，還要多問一句：使用者在觸發這個錯誤的當下，注意力實際在畫面的哪個區塊？通知要出現在那個區塊，而不是預設的、跟操作情境無關的全域位置。（2）**負 margin／滿版背景這類「假設自己是容器裡第一個元素」的排版手法，一旦容器裡在特定情境下會多出其他內容（像這裡偶爾出現的錯誤訊息），就要連帶檢查那個新內容的間距夠不夠抵銷負 margin 的量**——這種 bug 只在「剛好同時發生」的情境下才會出現（這裡是「工作區頁面」加上「剛好有錯誤要顯示」），平常測試很容易漏掉，跟前面幾輪 `position: sticky` 修法一樣，都是「條件疊加時才現形」的一類問題。

**檔案**：`frontend/src/types.ts`（`ChatMessage` 新增 `isError` 欄位）、`frontend/src/components/ChatPanel.tsx`（`isError` 訊息套用 `.chat-bubble-error` 樣式並加上 ⚠️ 前綴）、`frontend/src/pages/WorkspacePage.tsx`（`handleSendMessage` 失敗時把錯誤訊息也寫進對話紀錄並存檔）、`frontend/src/index.css`（新增 `.chat-bubble-error`；`.error-banner` 的 `margin-bottom` 16px→24px 修正跟工作區標題列的重疊）。

**驗證方式**：`npx tsc --noEmit`、`npx vitest run`（32 個測試全過，這次改動主要是 UI 互動，沒有新增純邏輯需要覆蓋）。瀏覽器實測（dev 環境 18002/5175）：暫時攔截 `window.fetch`，讓 `/chat` 這個請求回傳 502 錯誤（模擬真正逾時失敗但不用真的等模型逾時），在浮動聊天視窗裡送出訊息，確認「思考中」的泡泡消失後，原地換成「⚠️ 模型服務暫時無回應，請稍後再試一次」的警示樣式泡泡；重新整理頁面後這則錯誤紀錄依然存在，確認有正確存檔。也驗證了 `.error-banner` 重疊的修正：手動在工作區頁面插入一個 `.error-banner` 元素，量測跟 `.workspace-title-row` 的邊界，確認兩者完全沒有重疊（原本會重疊 8px）。測試過程中不慎在 dev 環境留下兩筆測試用的聊天紀錄，已經用直接呼叫 API 的方式移除，不影響使用者原本的對話紀錄。

## 2026-08-31 統一所有確認彈窗的樣式，修正工作區標題列露出捲動內容

### 全部改用同一套自訂確認彈窗，不再用瀏覽器原生 window.confirm

**問題**：畫面上「需要使用者確認」的情境長得不一致——取消勾選有風險的素材，用的是自訂的 `ModalOverlay` 彈窗（深色遮罩、可以分組列出詳細清單）；但刪除測試用例、刪除素材、刪除專案、刪除對話，用的卻是瀏覽器原生 `window.confirm`（單純一個系統對話框，排版、配色完全由瀏覽器決定，不同瀏覽器/作業系統長得還不一樣）。同一個網站裡出現兩種完全不同視覺語言的「確認」互動，使用者體驗不一致。

**修法**：新增共用元件 `ConfirmDialog.tsx`，包在既有的 `ModalOverlay` 之上，統一提供標題、內文、取消／確定兩顆按鈕；有風險的動作（刪除）可以加上 `danger` 屬性，確定按鈕會換成跟用例列表「刪除這筆測試用例」按鈕同一種警示紅色（`#b3312c`），視覺上跟「這是危險動作」的語意對得起來。把原本 5 處 `window.confirm`（`TestCaseTable.tsx` 刪除用例、`MaterialGrid.tsx`／`MaterialRow.tsx` 刪除素材、`HomePage.tsx` 刪除專案、`ProjectLayout.tsx` 刪除對話）全部改用這個元件；連原本就是自訂彈窗的「取消勾選有風險素材」警示視窗也一併重構成用 `ConfirmDialog`（把原本手刻的 `modal-header`／按鈕區塊抽掉，改成把分組清單當作 `message` 傳進去），讓整個專案只剩一套「確認彈窗」的寫法，不是兩套並存。

`window.confirm` 是同步、會阻塞 JavaScript 執行的呼叫（跳出對話框時整個分頁會凍結，等使用者按下按鈕才繼續往下執行），原本的程式碼很自然地寫成「`if (!window.confirm(...)) return; 繼續做事`」這種同步流程；換成自訂彈窗後，跳出視窗跟使用者按下確定是「非同步」的兩個時間點，程式碼要跟著改寫成「先把『準備要刪除的對象』存進 state 觸發畫面顯示彈窗，使用者按下確定時才真正執行動作」的模式（例如 `TestCaseTable.tsx` 的 `removeCase` 從直接做完刪除，拆成 `removeCase`（存 state）+`confirmRemoveCase`（真正刪除）兩個函式）。

**背後的通用觀念**：**瀏覽器原生的 `confirm()`／`alert()`／`prompt()` 都是同步阻塞的 API，這是它們最大的限制，不只是「醜」而已**——同步阻塞代表沒辦法在等待使用者回應的同時做任何其他事，也沒辦法客製化外觀。一旦専案裡已經有自訂彈窗的基礎設施（`ModalOverlay`），把原生 `confirm()` 換成自訂彈窗，程式流程必然要從「同步 if 判斷」改寫成「用 state 記錄『正在詢問使用者』這件事，交給非同步的使用者互動決定下一步」——這是幾乎所有「原生阻塞式互動換成非阻塞 UI 元件」都會遇到的固定改寫模式（同樣的道理也適用於把原生的 `window.prompt()` 換成自訂輸入框、把原生 `<select>` 換成自訂下拉選單元件）。

**檔案**：新增 `frontend/src/components/ConfirmDialog.tsx`；修改 `frontend/src/components/TestCaseTable.tsx`（刪除用例）、`frontend/src/components/MaterialGrid.tsx`（刪除素材、重構取消勾選風險警示視窗）、`frontend/src/components/MaterialRow.tsx`（刪除素材）、`frontend/src/pages/HomePage.tsx`（刪除專案）、`frontend/src/pages/ProjectLayout.tsx`（刪除對話）；`frontend/src/index.css` 新增 `.confirm-dialog-panel`／`.confirm-dialog-message`／`.confirm-dialog-actions`／`.confirm-dialog-danger-button`，並把 `.material-unlock-risk-panel` 改成複合選擇器 `.confirm-dialog-panel.material-unlock-risk-panel`，確保寬度覆寫不受 CSS 檔案裡規則先後順序影響（跟既有 `.modal-overlay.image-viewer-overlay` 用同樣的理由）。

**驗證方式**：`npx tsc --noEmit`、`npx vitest run`（32 個測試全過，這次改動沒有新增/修改任何有測試覆蓋的純邏輯，主要是 UI 互動）。瀏覽器實測（dev 環境 18002/5175，用 `javascript_exec` 直接操作 DOM，這個環境的畫面截圖工具目前無法正常顯示視窗内容）：對一筆未鎖定的測試用例按「刪除這筆測試用例」，確認彈出的是自訂視窗（不是瀏覽器原生對話框）且標題／內文正確；按「取消」確認用例沒有被刪除；再次觸發並按「確定刪除」，確認用例真的從清單消失。同樣方式驗證素材庫的刪除素材彈窗。測試中不慎正式刪除了一筆既有的測試用例，已用直接呼叫 API 的方式把原始內容（從 `backend/logs/app.log` 找到最後一次完整內容）還原回去，不影響使用者原本的測試資料。

### 工作區標題列（對話名稱／匯出 Excel 那一行）捲動時會露出底下的用例，且看起來像懸浮的卡片

**問題**：使用者截圖回報四個層次的問題。第一次：捲動測試用例列表時，固定在畫面上方的標題列（對話名稱、共 N 筆用例、鎖定進度、使用中的素材、匯出 Excel）底下會看到用例卡片的內容「飄過去」，看起來不是「一整片有背景」，而是有東西從縫隙裡露出來。第二次：即使背景已經補滿，這個標題列左右兩側仍縮排在 `.app-main` 的內距裡，看起來仍然只是內容區裡「浮著」的一個區塊，捲動固定時更像一張懸浮卡片。第三次：左右貼齊之後，使用者指出「頂部也要填起來」，於是改用 `top: -24px` 讓固定基準線貼齊 `.app-main` 真正的頂端。第四次：使用者發現這個修法會讓標題列「捲動上去後才被推到頂端」，但他要的其實是「一開始（還沒捲動）就已經固定在頂端」，不需要先捲一小段才貼齊。

**修法**：分四步（前三步見上方逐次修改，這裡完整記錄最終正確的版本）：
1. 把留白從 `margin-bottom` 改成 `padding-bottom`（`padding: 4px 0 20px`），讓 `position: sticky` 固定住的 border-box 直接涵蓋這段留白，不留缺口；加上 `border-bottom` 分隔線，`z-index` 5→10。
2. 用等於 `.app-main` 內距的負 margin（`margin-left/right: -32px`）搭配同樣大小的 padding 補回內容縮排，讓背景左右貼齊 `.app-main` 邊界。
3.（第三次修法，之後被第 4 步取代）只改 `top: 0` 為 `top: -24px`：這只調整了「固定基準線」，元素在**還沒捲動、初次渲染時的位置仍然照一般排版流程走**（也就是照樣吃到自己的 margin，這裡 `margin-top` 當時是 `0`），排版流程算出來的初始位置停在 `.app-main` 的 `padding-top`（24px）那裡，比新的固定基準線（`-24px` 換算後等於 `.app-main` 邊框邊界）低了整整 24px。因為初始位置比基準線低（還沒「頂到」基準線），`position: sticky` 不會主動把元素拉去貼齊基準線——只有捲動到讓初始位置想要移動超過基準線時，才會被「卡住」在那條線上，看起來就像「捲上去才被推到頂端」，不是一開始就貼齊。
4.（正確修法）**同時**把 `margin-top` 也改成 `-24px`（原本是 `0`），並把 `padding-top` 對應加大到 `28px`（`4px` 原本的留白 + `24px` 補償）。這樣一來，初始（還沒捲動）的排版位置本身就已經被負 margin 拉到跟固定基準線同一個位置——兩者一開始就重合，畫面一載入就已經貼齊頂端，不需要捲動才會「跳」上去。

**背後的通用觀念**：（1）**`position: sticky`／`fixed` 元素只會用自己的 border-box（內容加 padding，不含 margin）去遮蓋捲動經過的內容**，留白只要是用 margin 做的就一定會露出縫隙，該用 padding 才會算進背景範圍。（2）**「負 margin 等於周圍內距」是讓子元素在一個有內距的容器裡做「滿版／出血（bleed）」效果的標準手法**——用等於容器內距大小的負 margin 把子元素往外推到跟容器邊界齊平，再用同樣大小的 padding 補回內容縮排，兩者抵銷後背景貼齊邊界、內容位置卻完全沒變。（3）**`position: sticky` 的行為要分成兩個獨立的東西理解，而且兩者都要對齊，效果才會正確**：一是「初始（未捲動）排版位置」，這個完全遵守一般排版規則，包含吃到元素自己的 `margin`；二是「捲動固定的基準線」，只由 `top`／`left` 等位移值決定，相對於捲動容器的 padding box，跟元素的 margin 無關。瀏覽器的實際行為是：**只有當初始排版位置想要往基準線的反方向移動超過那條線時，才會被卡住**——如果初始位置原本就比基準線更保守（例如比基準線低），畫面上就會維持初始位置不動，直到捲動幅度大到讓它「追上」基準線為止，才開始固定，這正是使用者說的「捲上去才被推到頂端」的成因。想要「一開始就已經貼齊」，必須讓這兩者（初始位置、固定基準線）從一開始就落在同一個座標，不能只改其中一個——這也解釋了為什麼上一輪只改 `margin-top` 卻沒改 `top` 時完全沒有效果：基準線沒有跟著動，瀏覽器一發現初始位置（被 margin 拉高）已經衝過基準線，就會立刻把它「拉回」基準線的位置，畫面上完全看不出 margin 改動的痕跡。

**檔案**：`frontend/src/index.css`（`.workspace-title-row` 的 `margin-bottom` 併入 `padding-bottom`，新增 `border-bottom`，`z-index` 5→10；`margin-left/right: -32px` 搭配 `padding-left/right: 32px` 做左右滿版背景；`margin-top: -24px` 與 `top: -24px` 一起設定，並把 `padding-top` 加大到 `28px`，讓垂直方向從一開始就貼齊 `.app-main` 真正的頂端，不需要更動 `.app-main` 本身）。

**驗證方式**：瀏覽器實測（dev 環境 18002/5175）用 `javascript_exec` 讀取 `.workspace-title-row` 與 `.app-main` 的 `getBoundingClientRect()`，確認兩者的 `top`／`left` 完全一致（`0`／`0`，一載入就貼齊，不用先捲動），且對話名稱輸入框跟下方 `.case-list` 仍維持同一條左右對齊線；再把 `.app-main` 的 `scrollTop` 設成 `10` 模擬捲動，確認標題列仍然固定在 `top: 0`，沒有跑掉。這次這個環境的畫面截圖工具／視窗尺寸問題似乎已經緩解，量到的是真實非零的版面尺寸（先前幾輪修法時視窗量出來是異常的 0 寬度，只能改用獨立測試沙盒間接驗證），這次是直接在真正的應用程式頁面上量測到的結果，可信度比前幾輪更高。

## 2026-08-31 鎖定的用例改名後會被 AI 悄悄改動，且內容偶爾會被過期資料覆蓋

**問題**：使用者在正式環境對話裡回報：「本次變動」摘要顯示一筆已鎖定的用例「步驟 2 的內容有調整」，但同一則回覆裡又同時出現「已鎖定審核，AI 嘗試調整但未套用」的提示——兩句話互相矛盾。追查對話紀錄（`backend/logs/app.log`）發現真正發生過的情況比表面看到的更嚴重：這筆用例曾經被 AI 悄悄改名成完全不同的名字、同時解除鎖定，往後連續好幾十輪對話裡都維持在「已解鎖、內容持續被改」的狀態，使用者完全沒被提示過，直到某次手動把名字跟鎖定狀態改回來才恢復正常。

**修法**：查出兩個各自獨立、疊加在一起才會被使用者看到的問題：

1. **鎖定比對只認名稱，改名就能繞過鎖定**——`test_case_lock.py` 的 `enforce_lock_on_llm_result` 原本用 `{tc.name: tc for tc in previous}` 建索引，靠「新結果裡有沒有同名用例」判斷這筆鎖定用例有沒有被動過。但 AI 回傳的 JSON 其實一直都有帶著看到的 `id`（這點在 log 裡的每一輪都能驗證），只是這個比對邏輯沒有用它。只要 AI 把用例名稱也一起改掉，比對用的 key 就對不上，這筆用例會被當成「找不到、可能是新用例」直接照單全收——包括它被改成的解鎖狀態——鎖定保護整個失效，而且不會產生任何提示，使用者毫無所覺。修法是把比對順序改成**先比 id、比不到才退回比名稱**：`old = previous_by_id.get(tc.id) or previous_by_name.get(tc.name)`，這樣即使 AI 順手把名稱也改了，只要它老實帶回原本的 id（目前觀察到它一直都會），鎖定保護就抓得到。
2. **同一次請求裡，兩個環節用了不同的「之前」基準**——`/chat` 這個 endpoint 同時做了兩件事：把「目前的測試用例清單」放進送給模型的提示詞裡（用的是這次請求 body 帶來的 `payload.current_test_cases`，也就是使用者瀏覽器當下畫面上的內容），但事後判斷「鎖定用例有沒有被改動」時，卻是拿伺服器自己存的 `conversation.last_result.test_cases` 來比對。這兩份資料平常大部分時間是一致的，但前端手動編輯表格是 debounce 1 秒後才真的呼叫 API 存檔（見 `WorkspacePage.tsx` 的自動存檔 `useEffect`），如果使用者編輯後不到 1 秒就送出聊天訊息，伺服器這時候存的 `last_result` 還是編輯前的舊內容。這種情況下，即使 AI 完全沒有更動這筆用例、只是老實把使用者剛編輯好的內容原封不動送回來，鎖定保護也會因為拿它跟一份過期的伺服器記錄比對出「不一樣」，反而把使用者自己剛做的合法編輯覆蓋掉、還跳出一則說「AI 嘗試調整但未套用」的提示——把鍋甩給 AI，但其實是伺服器自己內部兩份資料對不齊。修法是讓 `/chat` 的鎖定判斷跟送給模型的提示詞用**同一份** `payload.current_test_cases` 當基準，不要另外去拿 `conversation.last_result`。

**背後的通用觀念**：（1）**任何需要「認出同一筆資料」的比對邏輯，優先用穩定的識別碼（id），名稱／標題這類人類可讀的欄位只適合當最後的備援**——名稱本來就是使用者或 AI 隨時可能主動去改的內容，拿它當主要比對依據，等於把一個安全機制的可靠性，建立在「這個欄位剛好沒被改到」這個脆弱假設上；這個專案先前在 `diffTestCases.ts`（純畫面顯示用的比對）就已經有一樣的已知限制，但因為那裡只影響顯示效果、頂多是文字說法不夠精準，沒人意識到同一種比對方式其實也被用在「決定要不要保護資料」這種安全機制上，後果差很多。（2）**同一次操作裡，如果有兩個地方各自需要「操作前的狀態」，一定要來自同一份資料，不能一個抓請求參數、一個抓伺服器自己存的記錄**——這兩份資料乍看永遠一致，只有在有非同步延遲（這裡是 1 秒的 debounce）的情況下才會短暫不同步，這種只在特定時間窗口才會重現的不一致，正是最容易被忽略、也最難單純用肉眼看 log 找出來的一類 bug（這次是先在 log 裡比對出「AI 這次到底有沒有真的改內容」才抓到）。

**檔案**：`backend/app/services/test_case_lock.py`（`enforce_lock_on_llm_result` 改成先比 id 再退回比名稱，`seen_locked_names` 也一併改成 `seen_locked_ids`）、`backend/app/routers/conversations.py`（`/chat` 的鎖定判斷基準從 `conversation.last_result.test_cases` 改成 `payload.current_test_cases`）、`backend/tests/test_test_case_lock.py`（新增 `test_llm_result_locked_case_renamed_by_llm_is_still_protected`，直接重現正式環境發生過的改名繞過情境）。

順便處理使用者一併提出的另一個需求：聊天裡「本次變動」的異動摘要只顯示用例名稱，名稱長或有好幾筆變動時不容易對照到表格裡的哪一列。`diffTestCases.ts` 的 `diffTestCases` 對「新增」跟「修改」的項目，都加上該用例在聊天後清單裡的編號（`第 N 筆・`前綴，編號規則跟其他地方的「第 N 筆」標示一致）；「刪除」的用例已經不在清單裡了，加編號沒有意義，維持不加。

**驗證方式**：`.venv/Scripts/python.exe -m pytest`（後端 56 個測試全過，含新增的改名繞過回歸測試）、`npx tsc --noEmit`、`npx vitest run`（前端 32 個測試全過）。這次的根因是從正式環境的 `backend/logs/app.log` 直接比對「AI 這次回傳的內容」跟「送進去的舊內容」是否真的相同（寫了一次性的 Python 腳本逐欄位比對，排除 id/locked 後兩者完全一致，證實不是 AI 真的想改內容，而是伺服器內部比對基準不一致）才確認根因，不是憑印象猜測；沒有另外用真實模型跑一次完整對話流程重現這個 race condition（真實模型單次呼叫常要好幾分鐘甚至逾時，不適合拿來做重現用的整合測試），改動範圍也沒有牽動任何新的互動介面，因此以單元測試涵蓋根因（後端兩個比對邏輯）與畫面文字格式（前端 `diffTestCases`）作為驗證，之後如果使用者在正式環境又看到類似的「鎖定用例被改動」提示，第一步應該去比對 log 裡 AI 回傳的內容有沒有真的跟送進去的內容不同，而不是預設是這次修法沒生效。

### 已經回答過的澄清問題，換個說法又被重新問一次

**問題**：使用者截圖裡同一批問題（UUID 有效範圍、Group 溢位處理、起始位址範圍等）明明已經在先前對話裡給過明確答案，卻還是被 AI 重複詢問。查 `backend/logs/app.log` 發現一個具體重現案例：`current_test_cases=47 pending_questions=0`——也就是說，送給模型的請求裡完全沒有列出任何「尚未解決的問題」，使用者這次的訊息也只是要求根據規格幫某一筆用例補測試步驟，跟 UUID／起始位址完全無關——但 AI 的回覆仍然重新生出這 4 個問題，而且每個問題的 `context` 都清楚寫著「使用者回覆⋯，需確認此範圍是否為最終規格」，代表模型自己也知道使用者已經回答過，只是選擇用「這是否為最終定案」的說法把問題重新包裝一次，逃過既有的自相矛盾偵測（那道防線只認字面上寫「已解決」的情況，見 2026-08-28 的 `09c990e` commit）。

**修法**：這不是一個靠改資料結構或比對邏輯就能徹底解決的問題——模型每一輪都會重新讀一次完整的原始素材（`build_material_content`），規格文件本身確實沒有寫死 UUID 範圍之類的細節，模型單看素材文字，會很「合理」地覺得這裡還有疑問，只是它沒有把「先前對話紀錄裡使用者已經給過具體答案」這件事看得比「素材文字本身仍然含糊」更優先。因此分兩層處理：
1. **prompt 層**：在 `SYSTEM_PROMPT_CHAT` 規則 5 新增一條，明確告訴模型「先前的對話紀錄與用例備註」跟「目前尚未解決的澄清問題清單」一樣，都要拿來檢查有沒有現成答案；即使素材文字本身仍含糊，只要使用者已經給過具體答案就視為定案，不可以用「是否為最終規格」「是否為定案」「是否確定」這類說法把已回答的問題偽裝成新問題。這是治本的做法，但 prompt 是「引導」不是「強制」，模型還是有機率不遵守。
2. **程式層防線**：`prompt_builder.py` 的 `_drop_self_contradicting_questions` 原本只認字面上的「已解決」。新增 `_RECONFIRMATION_LOOP_PATTERN`，要求 context 裡同時出現「使用者回覆／依據使用者」等字樣**與**「是否為最終規格／是否為定案／是否確定」，兩個條件都成立才過濾——刻意要求雙重條件，是為了避免誤殺真正還沒有答案、只是剛好提到「最終規格」字樣的問題（這個專案的核心原則是「LLM 不可以亂猜」，寧可少過濾、讓使用者多看到一次已回答的問題，也不要多過濾、不小心讓一個真正該問的問題被默默拿掉）。

**背後的通用觀念**：**當防禦機制是「比對關鍵字」而防禦對象是「會自由改寫措辭的 LLM 輸出」時，這場仗永遠是防守方在追趕**——模型换一種說法（「是否為最終規格」代替「已解決」）就能繞過去，加關鍵字只能防到已經觀察過的措辭，防不了下一種换句話說的寫法。這種情況下，比起無止盡擴充關鍵字清單，更根本的做法是回到「為什麼模型會這樣輸出」：它是照著 prompt 的指示在做事，只是 prompt 沒有明確涵蓋這個情境（「素材文字含糊」跟「使用者已經給過答案」哪個該優先）。先把 prompt 的指示補完整，程式層的關鍵字過濾只當作最後一道、儘量保守（避免誤殺）的防線，而不是把全部期待都放在關鍵字比對上。

**檔案**：`backend/app/services/prompt_builder.py`（`SYSTEM_PROMPT_CHAT` 規則 5 新增「先前對話紀錄與備註視為已有答案」的說明；新增 `_RECONFIRMATION_LOOP_PATTERN` 與對應的過濾邏輯）、`backend/tests/test_prompt_builder.py`（新增 `test_parse_generation_result_drops_reconfirmation_loop_questions`，直接用真實事故裡的 context 原文驗證會被過濾，並驗證一個「使用者尚未回答」的問題不會被誤殺）。

**驗證方式**：`.venv/Scripts/python.exe -m pytest`（後端 57 個測試全過）。這一層修法的極限：prompt 調整無法用自動化測試驗證模型一定會遵守（下次真的呼叫模型時才能真正驗證），只有程式層的關鍵字防線有單元測試覆蓋；之後如果使用者又看到「已回答的問題被重新詢問」，且 context 沒有落在目前這兩種措辭裡，代表模型又换了新的說法，需要回頭看實際 log 裡的 context 原文，再決定是擴充防線關鍵字還是進一步調整 prompt。

## 2026-08-30 取消勾選有風險的素材時，改用自訂視窗列出詳細名單

**問題**：前一版取消勾選「有未鎖定用例在用」的素材時，用瀏覽器原生的 `window.confirm` 跳出警示，但只有一段純文字訊息、排版完全交給瀏覽器決定，沒辦法清楚分開「已鎖定、不受影響」跟「尚未鎖定、需要確認」這兩組用例，使用者體驗上跟畫面其他地方的視覺語言也不一致。

**修法**：`materialUsage.ts` 的 `MaterialUsage` 除了原本的 `unlockedCaseNames`，新增對稱的 `lockedCaseNames`，兩組都用名稱列出來，不是只給數字。`MaterialGrid.tsx` 把 `window.confirm` 換成用既有的 `ModalOverlay` 元件自建的彈出視窗：綠色區塊列出「✓ 已鎖定，取消勾選不受影響」的用例，黃色區塊列出「⚠️ 尚未鎖定，需要您先確認」的用例，底下才是「取消」／「確定取消勾選」兩個按鈕。跟畫面其他警示（工作區的過量提示、鎖定進度）用同一套顏色語言（綠＝安全、黃＝需要留意），使用者不用重新學一套新的視覺規則。

後續使用者又提出：光看名稱還不夠方便，最好能直接告訴使用者這是「第幾筆」用例，才能快速對照表格找到它。因此把 `lockedCaseNames`／`unlockedCaseNames`（純字串陣列）改成 `lockedCases`／`unlockedCases`（`{ index, name }` 物件陣列），`index` 直接取用例在 `test_cases` 陣列裡的位置（`caseIndex + 1`），跟表格上每筆用例卡片左側顯示的編號完全對應。清單改成顯示「第 N 筆：用例名稱」，不再只有名稱。

**背後的通用觀念**：**瀏覽器原生的 `window.confirm`／`alert` 只適合「一句話講得完」的簡單情境**——它沒有任何排版能力（不能分段落、不能用顏色區分輕重、不能加清單），一旦要傳達的資訊有結構（這裡是「兩組不同嚴重程度的清單」），硬塞進一段純文字只會讓使用者要自己在腦中重新解析。這個專案已經有現成的 `ModalOverlay` 元件跟一套跨畫面一致的警示配色（綠色＝安全、黃色＝需要留意），遇到這類「原生對話框資訊量不夠」的情況，直接重用既有的彈出視窗基礎設施，比硬湊 `window.confirm` 的文字排版更符合使用者體驗，付出的開發成本也不高。

**檔案**：`frontend/src/materialUsage.ts`（`MaterialUsage` 新增 `lockedCases`／`unlockedCases`，元素含 `index`／`name`）、`frontend/src/materialUsage.test.ts`（更新測試涵蓋兩組名單與編號）、`frontend/src/components/MaterialGrid.tsx`（新增 `unlockRiskMaterialId` 狀態與自訂警示視窗，取代 `window.confirm`；清單顯示「第 N 筆：名稱」）、`frontend/src/index.css`（新增 `.material-unlock-risk-*` 系列樣式）

**驗證方式**：`npx tsc --noEmit`、`npx vitest run`（31 個測試全過）。瀏覽器實測（dev 環境 18002/5175）：讓對話裡「第 1 筆」（已鎖定）跟「第 5 筆」（尚未鎖定）兩筆用例同時引用同一個素材，取消勾選時確認自訂視窗正確分兩組列出「第 1 筆：⋯」「第 5 筆：⋯」（編號跟畫面上表格顯示的一致，不是只顯示名稱）；點「取消」確認視窗關閉且勾選狀態不變；點「確定取消勾選」確認視窗關閉且勾選狀態真的變成未勾選；測試完成後已還原素材勾選與 `based_on_images`。

## 2026-08-29 取消勾選素材前，提醒可能影響尚未鎖定的用例

**問題**：使用者提出一個先前沒考慮到的風險——取消勾選一個素材之後，如果之後又跟模型繼續對話，模型會失去這個素材的依據；對於「尚未鎖定」的用例（本來就還開放給模型修改），這代表模型可能因此重新提出疑問、或修改內容時沒辦法再對照原始素材確認。已鎖定的用例不受影響（鎖定機制會擋下任何改動），但當時的「已用 N／未使用」標籤沒有區分這個差異，使用者無從判斷取消勾選某個素材到底安不安全。

**修法**：`materialUsage.ts` 的 `countMaterialUsage` 從回傳單純的引用次數，改成回傳 `{ total, unlockedCaseNames }`——除了總引用次數，額外列出「引用這個素材、但目前沒有鎖定」的用例名稱清單。`MaterialGrid.tsx` 取消勾選素材時，如果這個素材有 `unlockedCaseNames`，跳出 `window.confirm` 明確列出是哪幾筆用例會受影響，使用者可以就地判斷要先去鎖定那些用例、還是確定要繼續取消勾選；已鎖定用例引用的素材可以安心取消勾選，不會跳出這個確認。

同時使用者也問到「圖片是不是比文字耗費更多資源」——答案是肯定的，這點其實已經反映在先前逾時修法的係數裡（`materialRisk.ts` 估計每張圖片約當 10 秒、每千字約當 1 秒，是從真實逾時事件回推的比例），這次把這個原則也寫進送出前的過量警示文字裡，明確建議使用者遇到需要減量時優先拿掉圖片而不是文字。

**背後的通用觀念**：**「這個資料現在有沒有被用到」（used）跟「這個資料被用到的地方，之後還有沒有辦法被改動」（mutable）是兩個獨立的維度，只看前者容易漏掉真正的風險**——這次一開始的「已用／未使用」標籤只回答了第一個問題，但使用者真正關心的其實是第二個：已經定案（鎖定）的東西改不改都無所謂，還沒定案的東西才是需要留意的地方。日後設計任何「這個資源正在被使用中，要不要繼續？」的提示時，值得多問一句：使用它的那些東西，本身處在一個還會不會被改動的狀態？如果會，用了它的東西之後失去它，會不會連帶讓那些「還沒定案」的東西跟著出問題。

**檔案**：`frontend/src/materialUsage.ts`（`countMaterialUsage` 回傳結構改成 `MaterialUsage`）、`frontend/src/materialUsage.test.ts`（更新既有測試、新增列出未鎖定用例名稱的測試）、`frontend/src/components/MaterialGrid.tsx`（新增 `handleToggleSelect` 確認邏輯、標籤 tooltip 文字區分已鎖定／未鎖定）、`frontend/src/components/MaterialSelector.tsx`（更新 `usageCounts` prop 型別）、`frontend/src/components/ChatPanel.tsx`（過量警示文字加上「優先拿掉圖片」的建議）

**驗證方式**：`npx tsc --noEmit`、`npx vitest run`（30 個測試全過，含新增測試）。瀏覽器實測（dev 環境 18002/5175）：讓某個未鎖定用例的 `based_on_images` 指向某張已勾選圖片，攔截 `window.confirm` 驗證兩種情境——選擇「取消」時確認勾選框維持勾選（沒有真的取消）；選擇「確定」時確認勾選框真的變成未勾選，且 `confirm` 訊息正確列出受影響的用例名稱；hover 使用狀態標籤確認 tooltip 文字正確區分「都已鎖定，可安心取消勾選」跟「有未鎖定的用例，取消勾選會失去依據」兩種情境；測試完成後已還原素材勾選與 `based_on_images`。

## 2026-08-29 素材選取加上「使用狀態」標籤與「可能相關」建議

**問題**：使用者提出兩個相關的需求：(1) 在素材選取畫面標示每個素材有沒有真的被目前的用例引用，幫助判斷哪些素材可以放心取消勾選；(2) 當模型提出待確認問題時，如果素材庫裡有還沒勾選、但看起來跟這個問題有關的素材，主動提示使用者可能需要加進來，而不是讓使用者自己回頭翻素材庫。

**修法**：
1. 新增 `frontend/src/materialUsage.ts` 的 `countMaterialUsage`，用既有用例的 `based_on_images` 反查每個素材被幾筆用例引用——這是精確計算，不是猜測。素材選取畫面（`MaterialGrid.tsx`）圖片類素材卡片上新增一個標籤，顯示「已用 N」（引用過，綠色）或「未使用」（灰色），只在有實際生成過用例的情境下顯示（沒有 `usageCounts` 就完全不顯示，避免顯示一個永遠是 0、沒有意義的標籤）。
2. 新增 `materialRisk.ts` 的 `findLikelyRelevantUnselectedMaterials`：針對每個待確認問題，比對問題文字（question + context）跟「目前未勾選」素材的檔名／說明文字有沒有重疊，有重疊就在 `ChatPanel.tsx` 對應問題下方顯示提示與「加入這 N 項」按鈕。跟先前「找出可能無關的已勾選素材」是同一種思路的反向版本，一樣**只做字串比對，不理解語意**，判斷不出關聯就完全不建議，避免誤導使用者。
3. 把「使用中的素材」按鈕點開後彈窗裡的「取消勾選」跟這次新加的「加入」動作，共用同一個回呼，原本命名為 `onDeselectMaterials`（只反映得出「移除」這個方向）改名成 `onChangeSelectedMaterials`，更準確反映它其實是「設定完整的新選取清單」，不分加入或移除。

**背後的通用觀念**：**當一個回呼函式的用途從單一方向擴展成雙向時，回頭把名字改得準確，比讓一個名不符實的名字繼續留著更值得做**——`onDeselectMaterials` 這個名字在只用來取消勾選時完全合理，但直接拿來做「加入」語意上就會誤導之後維護的人（包括未來的自己）以為這個函式只能拿掉東西。函式簽章沒變、行為也沒變，純粹是命名跟用途對不上了，這種情況下花幾分鐘重新命名，換來的是往後每次看到這個 prop 都不用停下來想「這裡加素材為什麼呼叫一個叫 deselect 的東西」。

驗證這兩個功能時，意外踩到一個跟這次改動無關、但值得記錄的地雷：**測試資料如果剛好選中「已鎖定審核」的用例，PUT 測試用例清單去修改它的任何欄位都會被後端默默忽略**——`enforce_lock_on_manual_edit` 只要新舊資料都是鎖定狀態，就整筆用伺服器原本的版本蓋掉，不管送出去的內容差在哪裡。這是鎖定功能本來就該有的行為（鎖定的用例本來就該是唯讀的），不是 bug，但拿鎖定中的用例當測試資料、想手動改欄位驗證別的功能時，會因為改了卻沒生效而白白繞一大圈——**驗證跟目標功能無關的欄位時，記得先確認選的測試資料本身沒有被其他保護機制（鎖定、權限、狀態機限制等）擋住**，不然很容易誤以為是新功能壞掉，其實是舊功能正常發揮作用。

**檔案**：新增 `frontend/src/materialUsage.ts`、`frontend/src/materialUsage.test.ts`；`frontend/src/materialRisk.ts`（新增 `findLikelyRelevantUnselectedMaterials`）、`frontend/src/materialRisk.test.ts`（新增對應測試）；`frontend/src/components/MaterialGrid.tsx`（新增 `usageCounts` prop 與標籤）、`MaterialSelector.tsx`（轉傳 `usageCounts`）；`frontend/src/components/ChatPanel.tsx`（問題下方的建議提示、prop 改名）、`FloatingChat.tsx`（轉傳、prop 改名）；`frontend/src/pages/WorkspacePage.tsx`（計算 `materialUsageCounts`、prop 改名）

**驗證方式**：`npx tsc --noEmit`、`npx vitest run`（28 個測試全過，含新增的 8 個）。瀏覽器實測（dev 環境 18002/5175）：手動在對話裡加一則帶待確認問題（context 提到某個未勾選素材檔名）的聊天紀錄，確認問題下方正確顯示「💡 素材庫裡的「XXX」看起來可能跟這個問題有關」提示，點擊「加入這 1 項」後確認該素材真的被加入勾選、提示跟著消失；把某個未鎖定用例的 `based_on_images` 指向某張已勾選圖片，確認「使用中的素材」彈窗裡那張圖片正確顯示「已用 1」（綠色）標籤、其餘顯示「未使用」（灰色），hover 該標籤確認提示文字正確；測試完成後已還原對話原本的素材勾選、清空 `based_on_images`、移除測試用的聊天紀錄項目。

## 2026-08-28 素材量偏大時，送出前先擋下並提示可能無關的素材

**問題**：延續前一筆逾時修法的追查——同一個對話勾選了 28 張截圖＋一份 5.2 萬字的規格文件＋既有的 48 筆用例，導致呼叫模型連續逾時。就算加了逾時上限跟錯誤訊息，使用者還是得先等快 5 分鐘才知道失敗，而且失敗後也不知道該怎麼調整才會成功。同時把材料量減到只剩文字規格後，同一個請求 88 秒內就成功了，證實真正的問題出在「送出前完全沒有機制提醒使用者這次的內容量已經偏大」。

**修法**：新增 `frontend/src/materialRisk.ts`，依「圖片張數」「素材文字量」「既有用例 JSON 大小」「聊天紀錄長度」做一個粗略的處理時間估算（係數直接取自這次真實逾時事件的資料點：0 張圖片＋7.8 萬字內容成功需要 88 秒、28 張圖片＋同樣內容連續兩次逾時超過 300 秒，回推出「約略每張圖片 10 秒、每千字 1 秒」的粗估公式），估計結果超過門檻（120 秒）就直接讓 `ChatPanel` 的送出按鈕失效、顯示原因，而不是讓使用者送出後才發現卡住。同時新增 `findLikelyUnrelatedImageMaterials`：如果訊息裡看得出是在講哪一筆既有用例（例如「第 48 條」或直接提到用例名稱），就用該用例的 `based_on_images` 反查哪些已勾選的圖片素材真的有關聯，列出「看起來無關」的其餘圖片素材，讓使用者一鍵取消勾選來縮小這次的內容量；訊息裡看不出在講哪一筆用例時，完全不給建議，不要用猜的排除掉可能真的需要的素材（呼應這個專案「LLM 不可以亂猜」的核心原則，這裡雖然不是 LLM 產生的建議，一樣的精神也適用在這個判斷式邏輯上）。

送出按鈕被擋下時，刻意**不用**原生的 `disabled` 屬性，改用 `aria-disabled="true"` 搭配不掛 `onClick`：瀏覽器不會對真正 disabled 的表單元件觸發 `mouseover`／`mouseenter`，如果直接用 `disabled` 屬性，外面包的 Tooltip 就永遠沒有機會顯示說明文字——這是實作到一半、用瀏覽器手動觸發 hover 事件測試時才發現的，一開始寫成 `disabled` 版本時 Tooltip 完全不會出現。

**背後的通用觀念**：這次的估算係數是**用一個真實事件回推出來的粗略經驗公式，不是精確測量值**——刻意在程式碼註解裡把這個限制寫清楚，之後如果發現門檻抓得不準（擋太多或擋太少），可以直接調整這幾個常數，不用照抄去精算模型內部真正的處理時間，那是做不到的事。另外，「不確定的時候完全不建議」比「猜一個可能不準的建議」更安全，這在使用者體驗設計上是一個值得記住的原則：一個明確承認自己判斷不出來、什麼都不顯示的空白，比一個看起來很篤定但其實是亂猜的建議更值得信任——尤其在使用者可能會照單全收的情境下，錯誤的建議造成的傷害往往比沒有建議更大。最後，`disabled` 屬性跟滑鼠事件的關係也是值得記住的 DOM 冷知識：**HTML 表單元件（`button`／`input`／`select`／`textarea`）一旦加上 `disabled`，瀏覽器會直接不對它觸發大多數滑鼠事件**（不只是 `click`，`mouseover`／`mouseenter` 也包含在內），這跟「視覺上看起來不能點」完全是兩回事——需要在「不能點」的同時還想要 hover 提示、或任何依賴滑鼠事件的互動時，`aria-disabled` 加上手動擋下動作邏輯，是比原生 `disabled` 更彈性的做法。

**檔案**：新增 `frontend/src/materialRisk.ts`（估算與建議邏輯）、`frontend/src/materialRisk.test.ts`（8 個測試，含直接重現這次真實逾時事件的素材組合）；`frontend/src/components/ChatPanel.tsx`（新增 props、送出前的過量檢查、警示橫幅、無關素材建議、`aria-disabled` 送出鈕）；`frontend/src/components/FloatingChat.tsx`（轉傳新 props）；`frontend/src/pages/WorkspacePage.tsx`（把 `materials`／`selectedMaterialIds`／`result.test_cases`／`imageMap` 傳進 `FloatingChat`）；`frontend/src/index.css`（新增 `.chat-overload-warning`、`.chat-overload-suggestions`、`.button-disabled-hoverable`）

**驗證方式**：`npx tsc --noEmit`、`npx vitest run`（20 個測試全過，含新增的 8 個）。瀏覽器實測（dev 環境 18002/5175）：對既有對話額外掛上一份 12 萬字的測試素材，重新整理後確認警示橫幅正確出現、送出鈕的 `aria-disabled` 是 `true`、`disabled` 屬性是 `false`（確認沒有用原生 disabled）；手動 dispatch `mouseover` 確認 Tooltip 正確顯示說明文字；手動點擊送出鈕確認畫面上的訊息數量沒有增加（真的被擋下，不是只有視覺上看起來擋住）；測試完成後已還原對話原本的素材勾選並刪除測試用的素材。

### 擋下送出之後，卻找不到地方可以取消勾選素材

**問題**：把上一則過量情境實際 demo 給使用者看時，使用者反映「但我沒有看到有地方可以取消勾選素材」——這次的過量素材是一份大型文字素材，不是圖片，`findLikelyUnrelatedImageMaterials` 只會針對圖片類素材給建議（因為圖片才是最主要的權重來源），文字素材過量時完全不會顯示任何建議按鈕；而且對話一旦已經產生過用例，畫面上「使用中的素材（N）」那顆按鈕原本只會打開唯讀的 `MaterialsModal`（純顯示內容跟一顆關閉鈕），並沒有任何管道可以在產生用例之後回頭調整素材勾選——警示文字要求使用者「取消勾選」，但這個動作在畫面上根本沒有入口，等於是叫使用者做一件他做不到的事。

**修法**：把 `WorkspacePage.tsx` 裡「使用中的素材」按鈕原本打開的唯讀 `MaterialsModal`，換成用 `ModalOverlay` 包住既有的 `MaterialSelector`（跟產生用例前選擇素材用的是同一個元件，本來就有checkbox、上傳、貼文字、合併、拆圖等完整功能），直接重用 `handleSelectedMaterialsChange` 等既有的 handler。這樣不管過量的是圖片還是文字，使用者永遠有一個明確、通用的地方可以調整素材勾選，不用依賴「剛好有找到可疑的圖片建議」這個比較窄的路徑。同時把警示文字改成明確指向「請先到上方『使用中的素材』取消勾選」，讓使用者不用自己猜要去哪裡操作。

**背後的通用觀念**：**設計一個「請使用者做某件事」的提示時，一定要連同「使用者具體要去哪裡、怎麼做這件事」一起檢查，不能只顧著把限制擋下來**。這次的警示邏輯（估算過量就擋下）本身是對的，但配套的「使用者接下來該怎麼辦」只做了一半——只覆蓋了圖片建議這個特例，卻沒有先確認「一般情況下，使用者本來就有辦法改變素材勾選嗎」這個更基本的前提，而這個前提其實在這次改動之前就不成立（產生用例後素材選擇本來就是唯讀的）。任何攔截性的提示（禁止某個操作、要求使用者先做某件事才能繼續）上線前，都值得自己重新走一次「照著提示文字操作」的流程，而不是只驗證「擋下的邏輯本身有沒有正確觸發」——這次正是靠使用者實際照著文字操作才發現的落差，寫測試或用 JS 直接觸發判斷邏輯是驗證不出「使用者介面上有沒有對應的操作入口」這件事的。

**檔案**：`frontend/src/pages/WorkspacePage.tsx`（「使用中的素材」按鈕改成打開可編輯的 `MaterialSelector`，取代原本的 `MaterialsModal`）、`frontend/src/components/ChatPanel.tsx`（警示文字明確指向操作入口）

**驗證方式**：`npx tsc --noEmit` 通過。瀏覽器實測（dev 環境 18002/5175）：重現上一則過量情境後，點開「使用中的素材」確認彈窗變成可勾選的素材庫（顯示「已勾選 N 項」），取消勾選過量的測試素材、關閉彈窗後確認警示橫幅消失、送出鈕恢復正常（不再是 `aria-disabled`），證實使用者真的能透過畫面上的操作解除過量狀態，不只是理論上邏輯正確。

## 2026-08-28 呼叫模型卡住將近 30 分鐘才失敗，畫面上完全看不出來

**問題**：使用者反映送出訊息後等了很久都沒有回覆。查 `backend/logs/app.log` 發現兩次呼叫（分別從 14:18、14:41 開始）都是卡了差不多整整 27 分鐘才失敗，一次收到內部模型回的 400「模型不支援」，另一次收到 500「回應內容是空的」。這段期間前端只會顯示「思考中…」，20 秒後多一句「回應時間較長屬正常現象」的安慰文字，使用者完全無法分辨「還在正常等待」還是「其實已經卡死了」。

**修法**：根因是 `llm_client.py` 建立 `OpenAI` client 時完全沒有指定 `timeout`／`max_retries`，官方預設是**單次呼叫 600 秒逾時、失敗自動重試 2 次**（最多 3 次嘗試）——三次都卡滿逾時上限，總共就是 3×600 秒再加上重試間的等待，接近 30 分鐘，剛好對得上這次觀察到的兩次「卡 27 分鐘」。改成明確指定 `timeout=150.0, max_retries=1`，把最壞情況壓在 5 分鐘內失敗。同時後端 `/generate`、`/chat` 兩個端點呼叫模型的地方原本完全沒有包 try/except，模型呼叫失敗時的例外會直接往上拋，變成 FastAPI 預設的通用 500，前端拿不到有意義的訊息；新增共用的 `_call_llm` 包裝，攔下 `openai.OpenAIError`（涵蓋逾時、連線失敗、模型服務回傳錯誤等所有子類別），記錄詳細 log 之後回傳使用者看得懂的 502 錯誤「模型服務暫時無回應，請稍後再試一次」，讓既有的錯誤橫幅機制能顯示出來。

**背後的通用觀念**：**串接任何外部服務時，「使用官方 SDK 預設值」本身就是一個需要主動確認、而不是可以放心忽略的決定**——這次踩到的 600 秒逾時＋2 次重試，單獨看每個數字都不算誇張（很多場景確實需要給模型多一點時間），但兩個「合理」的預設值疊在一起、又遇到對方服務真的異常時，會放大成使用者完全無法接受的等待時間。任何呼叫第三方服務的地方都值得自問：「這個呼叫最壞情況下會卡多久？使用者能接受這個時間嗎？」而不是預設 SDK 幫你選好的值就是適合自己產品的值。另外，**「呼叫失敗」跟「呼叫失敗但沒人知道怎麼失敗的」是兩種嚴重程度完全不同的問題**——這次即使加了逾時讓失敗變快，如果沒有同時補上錯誤處理，使用者看到的仍然是一個無法理解的通用錯誤；兩者要一起修才算真正解決「使用者不知道發生什麼事」這個核心抱怨。

**檔案**：`backend/app/services/llm_client.py`（`OpenAI` client 新增 `timeout`／`max_retries`）、`backend/app/routers/conversations.py`（新增 `_call_llm` 包裝，`/generate`、`/chat` 都改用它呼叫模型）、`backend/tests/test_conversations_router.py`（新增：呼叫成功時正常回傳內容、`OpenAIError` 會被轉成帶有清楚訊息的 502）

**驗證方式**：`pytest -q`（backend 53 → 55 個測試全過）。這次的根因（內部模型服務短暫異常）沒辦法在本地穩定重現，無法用真實逾時去做端對端瀏覽器驗證；改用單元測試直接驗證 `_call_llm` 的轉換邏輯本身（monkeypatch `chat_completion` 讓它拋出 `OpenAIError`，斷言轉成的 `HTTPException` 狀態碼與訊息），並額外用一支獨立腳本直接呼叫 `chat_completion` 確認正式環境的模型服務目前已恢復正常（11.5 秒內收到正確回應）。

## 2026-08-28 工作區標題列補上鎖定進度、全站 hover 提示改用同一套元件

### 1. 標題列補上「已鎖定 X/Y 筆」進度

**問題**：工作區標題列原本只顯示「共 N 筆用例」，使用者要知道審核鎖定的進度（還有幾筆沒鎖），得自己數表格裡每一列的鎖定圖示，沒有一眼就能看到的總覽。

**修法**：在 `WorkspacePage.tsx` 算出 `lockedCount`（`test_cases.filter(tc => tc.locked).length`）與 `allLocked`（用例數大於零且全部鎖定），標題列的「共 N 筆用例」旁邊加一個「已鎖定 X/Y 筆」的徽章；全部鎖定完成時額外套用 `.workspace-lock-progress-complete`，改成跟表格裡「已鎖定」按鈕同一組綠色（`#1f7a45`／`#2f9e5c`／`#e3f5e9`），讓「全部審核完成」這件事在標題列就看得出來，不用捲到表格底部逐一確認。

**背後的通用觀念**：進度類的數字（幾筆完成／幾筆未完成）放在清單最頂端當摘要，讓使用者不用捲動、不用心算就能掌握整體狀態，是很常見的清單型介面模式（例如任務清單常見的「3/8 已完成」）。這裡額外用顏色（完成時變綠）做二次強化，是刻意跟表格裡「單一用例鎖定後也會變綠」的視覺語言保持一致——同一個「已完成／已鎖定」的意思，在畫面上不同地方最好用同一種顏色語言表達，使用者不用重新學一套新的視覺規則。

**檔案**：`frontend/src/pages/WorkspacePage.tsx`（新增 `lockedCount`／`allLocked`，標題列加上進度徽章）、`frontend/src/index.css`（新增 `.workspace-lock-progress`、`.workspace-lock-progress-complete`）

**驗證方式**：瀏覽器實測（dev 環境 5175）——開啟一個已有 5 筆用例、4 筆已鎖定的對話，確認標題列顯示「已鎖定 4/5 筆」；用 JS 觸發最後一筆的鎖定按鈕，確認數字變成「已鎖定 5/5 筆」且套用了完成樣式（`workspace-lock-progress-complete` class、綠色背景），再點一次解鎖確認正確退回「4/5 筆」且顏色恢復中性色，證實正反兩個方向都有連動。

### 2. 全站的 hover 提示統一改用自訂 Tooltip，不再用瀏覽器原生 title

**問題**：使用者反映畫面上各處 hover 顯示的說明文字，樣式跟「側欄收合時滑鼠移到對話圖示上跳出的提示」不一致——後者是深色底、白字、帶小箭頭的自訂樣式，其他地方（例如鎖定按鈕、展開/收合按鈕、素材卡片檔名、匯出按鈕旁的提示等）用的都是瀏覽器原生的 `title` 屬性，樣式完全由作業系統決定，跳出的時機、外觀、行動裝置支援度都不一致，長文字也沒辦法自動換行。

**修法**：把原本只寫在 `Sidebar.tsx` 內部、專門給側欄收合圖示用的 `IconTooltip`，抽成共用元件 `frontend/src/components/Tooltip.tsx`，全站約 18 處原生 `title` 屬性（`FloatingChat`、`MaterialGrid`、`MaterialRow`、`ProjectSwitcher`、`Sidebar` 展開狀態、`TestCaseTable`、`WorkspacePage` 的分頁警示與匯出提示）都換成用這個元件包起來，支援 `placement`（top/bottom/left/right，依元件在畫面上的位置選擇，避免提示框超出視窗）跟 `wrap`（給版本衝突警示這類長文字用，允許自動換行、限制最大寬度）兩個參數。

第一版實作沿用原本的做法，在使用的元素外面多包一層 `<span>` 當量測用的錨點，結果讓好幾個既有樣式壞掉：`ProjectSwitcher` 的觸發按鈕原本靠 `width: 100%` 撐滿父層寬度，多包一層沒有明確寬度的容器之後，百分比寬度找不到可以參照的基準，版面跟著跑掉；`MaterialGrid` 卡片右上角的刪除按鈕、圖片張數徽章都是 `position: absolute` 相對卡片本身定位，錨點 span 一開始還連帶設了 `position: relative`，直接改變了它們的定位基準。改成用 React 的 `cloneElement` 把量測用的 `ref` 跟滑鼠移入/移出、取得/失去焦點的事件處理器直接掛在原本的元素本身，不額外插入任何 DOM 節點——這樣任何原本依賴「跟父層的關聯」（百分比寬度、絕對定位的基準、flex 版面裡的角色）的樣式規則都不會被打斷，因為排版樹的結構完全沒變。

**背後的通用觀念**：**在既有元素外面「多包一層容器」看似無害，但只要那個元素的樣式有任何一條規則依賴「跟父層/祖先的關係」（百分比寬度、`position: absolute` 找最近的已定位祖先、flex/grid 版面裡作為第 N 個子項目的身分），多包一層就可能打斷那條規則，而且往往要等實際去量測渲染結果才會發現，光看程式碼讀不出來。**這也是為什麼許多 UI 函式庫的 Tooltip／Popover 元件都提供類似 `asChild`（Radix UI）的機制——讓提示邏輯掛在使用者傳入的元素本身，而不是包一層自己的容器；`cloneElement` 是 React 在沒有額外套件時，達到同樣效果的標準做法：把 `ref` 跟事件處理器合併進子元素既有的 props，子元素在 DOM 裡的位置完全不變。

**檔案**：新增 `frontend/src/components/Tooltip.tsx`；移除 `Sidebar.tsx` 內部的 `IconTooltip`，改用共用元件；`FloatingChat.tsx`、`MaterialGrid.tsx`、`MaterialRow.tsx`、`ProjectSwitcher.tsx`、`TestCaseTable.tsx`、`WorkspacePage.tsx` 的原生 `title` 屬性全數換成 `<Tooltip>`；`frontend/src/index.css`（原本只給側欄用的 `.sidebar-tooltip*` 規則改成泛用的 `.hover-tooltip*`，新增 `placement` 對應的四種方向樣式與 `.hover-tooltip-wrap` 換行變體）

**驗證方式**：`npx tsc --noEmit`、`npx vitest run`（12 個測試全過）。瀏覽器實測（dev 環境 5175）：對鎖定按鈕、收合側欄的對話圖示、聊天助手按鈕分別用 JS dispatch `mouseover` 事件，確認跳出的 `.hover-tooltip` 文字、樣式（深色底、白字）、`placement` 對應的 class 都正確；針對曾經壞掉的兩個案例額外驗證——素材卡片檔名 `.material-card-name` 量測其 `parentElement` 確認直接是 `.material-card`（沒有被多包一層），且過長檔名的 `scrollWidth` 大於容器 `width`、`text-overflow: ellipsis` 仍然生效；刪除按鈕 `.material-card-delete` 同樣確認父層還是 `.material-card`，`position: absolute` 的定位基準沒有跑掉。

## 2026-08-28 已經回答過的澄清問題，模型有時仍會重複顯示

**問題**：使用者附圖回報——對話裡明明已經回答了 Q1～Q4 四個問題，下一輪模型回傳的 `context`（畫面上顯示成「依據：」）卻寫著「此問題已解決，但…」，但這個問題本身仍然出現在「有 N 個問題需要您協助確認」清單裡，等於使用者已經回答過的問題又被重新問一次。

**修法**：`SYSTEM_PROMPT_CHAT`（`prompt_builder.py`）規則 5 原本已經要求模型「有回答到的問題就不要再放進 `clarification_questions`」，但沒有明確禁止「一邊在 `context` 裡說已解決、一邊又把問題留著」這種自相矛盾的輸出，模型不見得每次都會照字面理解言下之意。先把規則寫得更明確：只要 `context` 裡出現「已解決」之類的說法，這個問題就必須整個從清單移除，不能同時存在兩種矛盾的狀態。但 prompt 只能提高機率、不能保證模型一定遵守，所以在 `parse_generation_result` 裡加一道最後防線：用正則比對每個問題的 `context`，只要出現「已解決」「已經解決」「已排除」「已處理」「不需要再確認/澄清」這類字眼，就直接把該問題從回傳結果裡濾掉（並寫一筆 warning log 記錄被濾掉的問題，方便之後追查模型是不是常犯這個毛病）。

**背後的通用觀念**：**呼叫 LLM 產生結構化輸出時，prompt 裡寫的規則只是「提高模型照做的機率」，不是程式邏輯上的保證**——模型仍然可能寫出局部自相矛盾的內容（像這次一邊描述「已解決」一邊又留著沒刪）。對於這種「輸出內容本身就足以判斷是否違反規則」的情況（不需要額外語意理解，字面比對就能抓到明顯的矛盾），值得在後端加一道基於規則的防禦性過濾當最後防線，而不是完全依賴改善 prompt 文字去解決——調整 prompt 之後**沒有辦法測試「模型下次會不會照做」**，但一段過濾邏輯是可以寫測試鎖住的行為。這是很常見的分工方式：prompt 負責讓模型「大部分時候」做對，程式碼負責兜住「模型沒做對」的已知情況。

**檔案**：`backend/app/services/prompt_builder.py`（`SYSTEM_PROMPT_CHAT` 規則 5 補上禁止自相矛盾的說明；新增 `_RESOLVED_CONTEXT_PATTERN`、`_drop_self_contradicting_questions`，在 `parse_generation_result` 回傳前套用）、`backend/tests/test_prompt_builder.py`（新增兩個測試：context 含「已解決」字樣的問題會被濾掉、不含這類字樣的問題維持不變）

**驗證方式**：`pytest -q`（backend 51 → 53 個測試全過，含新增的兩個案例）。這個防線本身用單元測試直接驗證邏輯（模擬 LLM 回傳的原始 JSON，斷言過濾後的 `clarification_questions` 內容），沒有另外用真實 LLM 觸發這個情境去做端對端驗證——因為要讓真實模型剛好輸出「自相矛盾」的內容不是能穩定重現的條件，單元測試已經足以鎖住這道防線本身的正確性。

## 2026-08-27 圖片放大預覽加上縮放控制，避免小圖看不清楚

**問題**：使用者反映點開「依據圖片」放大預覽後，有些圖片還是太小看不清楚。追查共用的 `useImageLightbox()` 發現：放大彈窗裡的 `<img>` 只有 `max-width: 100%`——這個規則**只會限制圖片不要比彈窗還寬，完全不會把比彈窗小的圖片放大**。很多截圖、附件圖片本身解析度就不高（例如聊天室裡使用者隨手貼的小張截圖），原始尺寸可能只有一兩百像素，點開「放大」後畫面上看到的其實就是它原本的像素尺寸，完全沒有被放大，跟縮圖比起來只是「换了個地方顯示同樣小的圖」。

**修法**：`ImageLightbox.tsx` 新增 `zoom` 狀態（100%／150%／200%／250%／300%，用 +/− 按鈕調整），圖片的顯示邏輯從「不超過彈窗寬度」改成「zoom 100% 時固定填滿彈窗寬度」——不管原始圖片多小，預設一開就先撐到跟彈窗一樣寬，再讓使用者視需要用 + 按鈕往上加碼看細節。彈窗本身的最大寬度也從 720px 放寬到 `min(1100px, 92vw)`，讓「填滿彈窗」這個基準本身也更大。放大到超出彈窗可視範圍時，交給外層新增的 `.image-preview-scroll` 容器（`overflow: auto`）處理捲動，不用自己寫拖曳平移的邏輯。

過程中踩到兩個 CSS 的坑：(1) 一開始把捲動容器寫成 `display: flex; justify-content: center` 來置中圖片，結果放大後量出來的 `img` 寬度雖然正確吃到了設定的百分比，但**捲動範圍完全沒有跟著變大**——這是 flex 子項目的預設行為，`flex-shrink: 1` 會在空間不夠時把子項目縮回去塞進容器，等於放大設定被悄悄抵銷，改成 `flex-shrink: 0` 後圖片寬度數字才是對的；(2) 但即使寬度數字對了，畫面上還是有一部分捲不到——這是另一個更隱蔽的坑：**用 flex／grid 的 `justify-content: center` 置中「內容比容器還大」的東西時，瀏覽器對可捲動範圍的計算有名的怪癖，置中對齊會讓溢出容器「開始端」的內容變成完全捲不到**（不是這個專案的 bug，是瀏覽器排版規格本身在這個情境下的行為，各家瀏覽器都有同樣問題）。最後改成不用 flex，容器用一般的 `text-align: center`（圖片沒溢出時一樣置中，溢出時兩側都能正常捲到），才是真正可靠的寫法。

**背後的通用觀念**：**CSS 的 `max-width` 只設了尺寸的上限，從來不負責「小圖要不要放大」這件事**——這是兩個獨立的問題（防止圖太大 vs. 確保圖夠大），很容易誤以為同一個屬性兩者都顧到了。另外，「置中溢出的內容」這個常見需求，直覺會想到 flex/grid 的 `justify-content/align-items: center`，但只要內容可能會比容器大、又需要保留捲動能力，這個直覺做法就會踩到瀏覽器排版規格裡「置中會讓部分內容捲不到」的已知限制——這類問題單靠讀程式碼很難發現（CSS 數值看起來完全正確、`getBoundingClientRect()` 量出來的寬度也對），必須實際在瀏覽器裡把內容撐到溢出、動手捲捲看，才會發現「捲得到」跟「捲得到全部」是两回事。這次能抓到，是因為驗證時沒有只看放大後的螢幕截圖覺得「圖變大了、看起來沒問題」，而是進一步去比對 `scrollWidth` 是否等於圖片實際寬度，纔揭穿它其實悄悄被裁掉了一截。

**檔案**：`frontend/src/components/ImageLightbox.tsx`（新增 `zoom` 狀態與 +/− 按鈕）、`frontend/src/index.css`（`.material-thumbnail-preview` 放寬最大寬度、新增 `.image-preview-toolbar`／`.image-preview-zoom-level`／`.image-preview-scroll`）

**驗證方式**：`npx tsc --noEmit`、`npx vitest run`（12 個測試全過）、`cd e2e && npm test`（golden-path 全流程通過）。瀏覽器實測（dev 環境 5175）：用程式產生一張刻意做得很小（120×60 像素）的測試圖片、透過聊天室附加送出，點開放大預覽確認圖片一開始就撐滿彈窗寬度（不是維持原本 120×60 的小尺寸）；接著點 + 放大到 300%、用 `getBoundingClientRect()` 跟 `scrollWidth` 交叉比對，確認捲動範圍真的等於放大後的圖片寬度（抓出並修正了前述兩個 CSS 坑）；點 − 縮回 100%、確認下限鎖在 100%（不能縮到比「填滿彈窗」還小）且按鈕正確 disable；關閉再重新開一張圖，確認 zoom 有重置回 100%，不會殘留上一張圖的放大倍率。

### 改成全螢幕深色檢視器，支援左鍵拖曳平移

**問題**：使用者看過前一版「彈窗＋捲軸」的放大效果後，覺得不夠好——附上參考圖是常見圖片檢視器的樣子（全螢幕深色背景、右上角圖示按鈕、可以直接用滑鼠拖曳圖片本身來移動），要求照這個方向改，並且要能左鍵按住拖曳圖片。原本用「小彈窗 + 捲軸」的做法雖然解決了「圖太小」的問題，但捲軸平移終究不是圖片檢視器慣用的操作方式，而且彈窗本身還是被卡在 `min(1100px, 92vw)` 的上限內，不是真正沉浸式的大圖體驗。

**修法**：`ImageLightbox.tsx` 整個重寫——彈窗改成佔滿全螢幕的深色版面（背景 `rgba(10,12,16,0.96)`，取代原本的小白色卡片），右上角換成圓形圖示按鈕（縮小／放大／關閉，仿照參考圖的放大鏡圖示）。移除原本靠 `overflow:auto` 捲軸的機制，改成滑鼠拖曳：`onMouseDown` 記錄下滑鼠起點座標跟目前的平移量，`mousemove`／`mouseup` 掛在 `window` 上（比照 `FloatingChat.tsx` 拖拉調整視窗大小已經在用的同一套寫法，這個專案裡已經有前例、風格保持一致），拖曳中即時算出位移量、用 CSS `transform: translate(...) scale(...)` 同時套用平移跟縮放，寬度基準也從「填滿彈窗寬度的百分比」換成更單純的 `width: 80vw; height: 80vh; object-fit: contain`——不管圖片原始比例或尺寸是什麼，一律先撐到視窗的 8 成大再讓使用者用滾輪外的按鈕加碼放大。滑鼠游標也依拖曳狀態切換 `grab`／`grabbing`，符合這類檢視器的慣例提示。

`ModalOverlay.tsx` 額外開放一個 `overlayClassName` prop，讓圖片檢視器可以蓋掉共用的遮罩／面板樣式，同時不影響其他還在用同一個共用元件的彈窗（編輯素材、選擇素材）——那些場景仍然適合原本的小白卡片，不該因為圖片檢視器的需求被牽連著一起變成全螢幕深色。

**背後的通用觀念**：**同一個 class 名稱疊加到共用元件上時，「後蓋前」還是「前蓋後」不是看邏輯上誰該贏，是看兩條規則在 CSS 檔案裡誰寫得比較後面**——這次新樣式一開始完全沒生效，量出來的背景色、彈窗尺寸全部還是舊值，原因就是共用元件的基礎樣式（`.modal-overlay`／`.modal-panel`）剛好定義在檔案更後面的位置，選擇器優先權（specificity）又跟新樣式的單一 class 選擇器一樣高，兩個優先權相同的規則打架時，CSS 的規則是「原始碼順序在後面的贏」，跟語意上「這個更特殊的樣式應該蓋掉那個更通用的樣式」完全無關。改成複合選擇器（`.modal-overlay.image-viewer-overlay`）墊高優先權後就穩定生效，不用再擔心之後如果有人把樣式表其他規則搬動位置、悄悄讓覆蓋順序又反過來。另外，驗證這次改動時也再次證實一個先前才學到的教訓：**用同一段程式在同一次同步執行裡「觸發動作＋立刻讀結果」，讀到的常常是 React 還沒 flush 完的舊畫面**——例如模擬拖曳時如果在同一個 script 裡把 mousedown／mousemove／mouseup 一次派送完就馬上讀 `transform`，量到的會是全部動作都還沒生效的初始值，必須拆成分開的呼叫、讓 React 有機會重新渲染，才能量到真正的結果，不然容易誤判成「拖曳沒有作用」。

**檔案**：`frontend/src/components/ImageLightbox.tsx`（整個重寫：`pan` 狀態、拖曳事件、圖示按鈕）、`frontend/src/components/ModalOverlay.tsx`（新增 `overlayClassName` prop）、`frontend/src/index.css`（拿掉 `.image-preview-*` 系列規則，新增 `.image-viewer-*` 系列全螢幕深色樣式）

**驗證方式**：`npx tsc --noEmit`、`npx vitest run`（12 個測試全過）、`cd e2e && npm test`（golden-path 全流程通過）。瀏覽器實測（dev 環境 5175）：點開圖片後用 JS 量測確認遮罩背景色、面板尺寸都吃到新樣式（面板寬高等於瀏覽器視窗大小，不是被 720px／1100px 卡住），且圖片預設就是視窗的 80% 大；分開三次呼叫模擬 `mousedown` → `mousemove` → `mouseup` 完整拖曳流程（拆開呼叫是因為同一次同步執行裡量到的會是 React 還沒 flush 的舊值），確認拖曳中 `transform` 的位移量確實跟著滑鼠移動改變、游標樣式切換成 `grabbing`，放開滑鼠後位移停止更新、游標恢復成 `grab`；放大鈕點擊後確認縮放同時作用在 `transform` 上且平移歸零；額外開啟另一個原本就有的彈窗（選擇素材），確認遮罩／面板還是原本的半透明背景＋白色卡片樣式，證實新增的 `overlayClassName` 沒有連帶影響其他共用同一個 `ModalOverlay` 元件的地方。

## 2026-08-27 偵測「同一個對話不小心開了兩個以上的分頁」，提前提醒

**問題**：使用者反映實際遇過這個情境——不小心把同一個對話開在兩個分頁，其中一個分頁鎖定了某筆用例，另一個分頁的過期快照後來存檔時把鎖定狀態悄悄改回去，導致「明明點了解鎖，AI 卻還是不能改」，完全摸不著頭緒。PR #20 加的版本衝突機制（`result_version`）已經解決了「悄悄覆蓋」這個資料安全問題——過期的存檔現在會被伺服器擋下（409），不會再無聲蓋掉別人剛做的修改。但擋下之後使用者還是得發現提示、重新整理、重做一次剛才的編輯，體驗上仍然是「先讓你撞牆，再告訴你怎麼回事」。既然使用者說明這其實不是真的多人協作、而是自己不小心開了重複分頁，更直接的做法是**在使用者開始編輯之前就提醒他「這裡還有另一個分頁」**，從源頭避免撞到衝突，而不是每次都要事後補救。

**修法**：新增 `frontend/src/useDuplicateTabWarning.ts`，用瀏覽器原生的 `BroadcastChannel` API 讓開著同一個對話的分頁互相「打招呼」：每個分頁進入對話時廣播 `announce`，收到別人的 `announce` 就回一個 `ack`，讓雙方都能記錄到對方的存在（用一個 `Map<分頁id, 最後收到訊息的時間>` 追蹤）。只要記錄裡有其他分頁，`WorkspacePage.tsx` 的標題列就會顯示一個持續存在的警示徽章「⚠️ 有其他分頁開著」（`title` 屬性放完整說明），不會自動消失也不能手動關掉——因為它反映的是「現在」還存不存在的即時狀態，不是一次性事件。

第一版原本設計成分頁關閉時廣播一次 `bye` 就好，但實測發現**分頁被關掉時 `beforeunload` 不保證會確實觸發**（測試時用瀏覽器自動化工具關閉分頁就重現了——這個事件在分頁被強制關閉、瀏覽器當機、或行動裝置直接砍掉背景分頁時本來就不可靠），導致警示卡死在畫面上、另一個分頁明明已經關了卻永遠提醒你「還有分頁」。改成心跳機制：每個分頁固定每 4 秒重新廣播一次 `announce`，其他分頁定期（同樣 4 秒一次）檢查每個記錄「上次收到訊息是多久以前」，超過 12 秒（心跳間隔的 3 倍，容許漏掉一兩拍不算真的離線）就當那個分頁已經不在了，從記錄裡移除。`beforeunload` 廣播 `bye` 還是保留，正常關閉分頁時可以讓提示秒退，但不再是唯一的依靠——就算它沒觸發，最多 12 秒後心跳機制也會自動修正。

**背後的通用觀念**：**任何靠「主動通知我要走了」來偵測對方存不存在的機制，都必須假設這個通知有可能不會送達，並且要有一個不依賴它的備援**。這是分散式系統裡很經典的「心跳（heartbeat）／存活偵測（liveness detection）」模式：與其相信每個參與者都會乖乖說再見（優雅離線是一種樂觀假設，現實中程式會當機、網路會斷、使用者會直接拔電源），不如反過來要求每個還活著的參與者持續證明自己還活著（定期廣播），沒有動靜超過一段時間才判定對方離線。這裡踩到的坑（`beforeunload` 沒有觸發）不是瀏覽器的 bug，而是一種需要事先設計進去的正常情況；能在自己的瀏覽器自動化測試裡就複現這個「優雅關閉沒有發生」的情境，比等到使用者未來真的遇到裝置當機才發現卡死的提示，划算得多。另外值得注意的是這個功能刻意**不提供關閉按鈕**——跟同一個檔案裡另一種提示（`workspaceNotice`，通知「剛剛發生了一件事」）不同，這裡通知的是「現在是什麼狀態」，只要狀態還成立、提示就該一直在，讓使用者手動關掉會製造一個「明明還有問題、畫面卻裝作沒事」的假象。

**檔案**：新增 `frontend/src/useDuplicateTabWarning.ts`；`frontend/src/pages/WorkspacePage.tsx`（呼叫這個 hook，兩處標題列都加上警示徽章）；`frontend/src/index.css`（新增 `.workspace-duplicate-tab-warning`）

**驗證方式**：`npx tsc --noEmit`、`npx vitest run`（12 個測試全過）、`cd e2e && npm test`（golden-path 全流程通過，確認新功能沒有影響單一分頁的正常使用）。瀏覽器實測（dev 環境 5175）：開兩個分頁指向同一個對話，確認**雙方**都出現警示徽章（不是只有後開的那個看得到）；用瀏覽器自動化工具直接關閉其中一個分頁（不是正常導航離開，刻意模擬 `beforeunload` 可能沒觸發的情況），確認另一個分頁的警示在等待期間持續存在、大約 12 秒後自動消失，證實心跳備援機制真的有在運作、不是只靠 `beforeunload`。

### 版本衝突擋下之後的提示，改成輕量、自動消失的 toast

**問題**：加了分頁警示徽章之後，使用者反而覺得原本版本衝突擋下時跳出的訊息（「這個對話的測試用例已經在別的分頁被修改過，已為您載入最新內容，剛才的編輯請重新確認並套用一次」）太重了——這則訊息借用的是整個頁面共用的通用錯誤橫幅，會一直卡在畫面上直到使用者手動關掉。既然分頁警示已經先把「風險」講清楚了，事後真的撞到衝突時，不需要再用一個要手動處理的橫幅去解釋一次完整的來龍去脈。

**修法**：討論過程中一開始考慮乾脆整個拿掉這則提示、衝突發生時安靜地重新載入就好，但這樣有個真實風險：畫面會無聲無息地被別的分頁的內容取代（使用者剛打的字可能因此消失），使用者可能根本沒注意到。所以保留提示，但改成新增的 `conflictToast` 狀態——用固定定位（`position: fixed`）浮在畫面下方置中的一句話，4 秒後用 `setTimeout` 自動清掉，不需要使用者手動關閉，文字也精簡成「已載入其他分頁的最新內容，剛才的編輯請重新套用一次」，不再重複解釋分頁警示已經講過的背景。同時把分頁警示徽章的說明文字（`title` 屬性）加強，明講可能造成的後果：「較晚存檔的一邊可能會因為版本衝突而無法套用（畫面會自動被另一邊的最新內容取代）」，把「為什麼要在意這個提示」講清楚，而不是只說「小心，這裡有另一個分頁」卻不解釋會怎樣。

**背後的通用觀念**：**同一件事要不要提醒使用者、提醒的「份量」該多重，取決於使用者在那個當下已經知道多少背景**。分頁警示徽章在編輯前就已經持續提醒過風險了，所以真的撞到衝突時，適合的提示是「確認一下發生了什麼、然後讓它自然消失」，而不是重新完整解釋一次原理再等使用者手動處理——後者在沒有事前提醒的情境下是必要的（使用者需要足夠資訊才能判斷該怎麼辦），但在已經被充分預告的情境下就變成不必要的干擾。另外，「完全不提示」跟「提示得剛剛好」是兩個容易混淆但後果差很多的選項：完全不提示看似最不打擾，但代價是使用者可能不會發現自己的編輯不見了；一個會自動消失、不需要處理的輕量提示，才是在「不打擾」跟「不要讓使用者被蒙在鼓裡」之間真正的平衡點。

**檔案**：`frontend/src/pages/WorkspacePage.tsx`（新增 `conflictToast` 狀態與自動清除的 `useEffect`，兩處版本衝突的 catch handler 改用 `setConflictToast` 取代 `setError`，分頁警示徽章的 `title` 文字加強）、`frontend/src/index.css`（新增 `.conflict-toast` 與一次性的 `conflict-toast-in` 進場動畫）

**驗證方式**：`npx tsc --noEmit`、`npx vitest run`（12 個測試全過）、`cd e2e && npm test`（golden-path 全流程通過）。瀏覽器實測（dev 環境 5175）：開兩個分頁，一邊鎖定用例存檔成功，另一邊（過期快照）鎖定另一筆用例時確認畫面下方浮出 toast「已載入其他分頁的最新內容，剛才的編輯請重新套用一次」，且該筆用例的鎖定操作被正確丟棄（沒有套用，畫面顯示的是伺服器最新狀態）；等待 5 秒後確認 toast 自動消失，不需要手動關閉。

## 2026-08-27 開放區網 IP 連線後，同事那邊跳出「crypto.randomUUID is not a function」

**問題**：把後端 CORS 白名單加開一個區網 IP、讓同事能用 `http://10.197.177.13:5173` 連進來用之後，同事那邊操作到一半跳出 `crypto.randomUUID is not a function`。追查發現前端有三處直接呼叫瀏覽器原生的 `crypto.randomUUID()` 產生本地識別碼（`WorkspacePage.tsx` 的聊天訊息 id、`TestCaseTable.tsx` 新增用例時的 `emptyCase()`）。這個方法屬於瀏覽器的 Web Crypto API，規格明定**只在「安全情境」（secure context）才會出現在 `crypto` 物件上**——安全情境的定義是 HTTPS，或是 `localhost`／`127.0.0.1` 這種本機位址。同事是用純 HTTP 透過區網 IP（不是 localhost、也不是 HTTPS）連進來，瀏覽器判定這不是安全情境，直接讓 `crypto.randomUUID` 從物件上消失，呼叫就變成呼叫一個不存在的方法。我自己用 localhost 連測試環境時完全不會遇到，因為 localhost 永遠算安全情境，這也是這個 bug 直到真的開放給別人用才被發現的原因。

**修法**：新增 `frontend/src/id.ts`，把「產生本地識別碼」包成一個 `newId()` 函式——先試 `crypto.randomUUID`，方法不存在時退回一個用 `Math.random()` 拼出來的簡易 UUID v4 格式字串當備援。這些 id 只是用來當 React 的 `key`、或標記聊天訊息／草稿用例這種純前端本地資料，從來不會送進任何需要密碼學等級隨機性的地方（例如權杖、密碼），所以備援方案不需要跟原生方法一樣安全，只要「夠不會撞號」就符合這裡的實際需求。`WorkspacePage.tsx`、`TestCaseTable.tsx` 兩處原本直接呼叫 `crypto.randomUUID()` 的地方都改成呼叫這個共用函式。

**背後的通用觀念**：**瀏覽器有一整類 API 只在「安全情境」下才存在**（Web Crypto 的部分方法、Clipboard API、Geolocation、Service Worker 等等），這個限制是瀏覽器規格本身刻意設計的，不是某個瀏覽器的 bug 或設定問題。安全情境的判斷不是看「連線有沒有加密」這麼單純，而是看**協定＋主機**的組合：`https://` 任何網域都算、`http://localhost`／`http://127.0.0.1` 也算（因為封包從沒真的離開這台機器，沒有中間人竊聽的風險），但 `http://` 加任何區網 IP 或網域一律不算——即使是同一個內網、即使實際上很安全。這造成一個很容易踩到的陷阱：**開發時幾乎必然用 `localhost` 測試，天生就是安全情境，這類 API 永遠正常；一旦部署到別的網址（哪怕只是同事用區網 IP 連你電腦上跑的 dev server），同一段程式碼就可能突然壞掉**——而且不是編譯期或型別檢查抓得到的錯誤，因為 TypeScript 看不出「這個方法在執行當下的網址情境下存不存在」，只有真的用非 localhost 的位址在瀏覽器裡跑過一次才會發現。這也是為什麼「這個功能已經測過沒問題」這句話要小心——測試環境（localhost）跟正式使用情境（別人從別的網址連進來）的安全情境可能不一樣，光是自己用 localhost 反覆測試無法保證涵蓋到這種差異。日後任何用到只在安全情境才有的瀏覽器 API 時，值得先確認呼叫的地方是不是核心功能路徑，是的話就該準備一個不依賴該情境的備援，而不是預設使用者一定是用 localhost 或 HTTPS 連進來。

**檔案**：新增 `frontend/src/id.ts`；改用它的 `frontend/src/pages/WorkspacePage.tsx`、`frontend/src/components/TestCaseTable.tsx`

**驗證方式**：`npx tsc --noEmit`、`npx vitest run`（12 個測試全過）、`cd e2e && npm test`（golden-path 全流程通過）。瀏覽器實測（dev 環境 5175）：用 JS 把 `window.crypto.randomUUID` 覆寫成 `undefined`，模擬「非安全情境」瀏覽器的實際狀況，接著在畫面上點「新增測試用例」（觸發 `emptyCase()`）與送出一則聊天訊息（觸發 `newId()`），確認兩個操作都正常完成、畫面正確顯示新增的用例與訊息，且 console 沒有任何錯誤——修好之前，同一組操作在覆寫 `randomUUID` 之後會直接跳出 `crypto.randomUUID is not a function` 並讓整個對話頁面白屏。

## 2026-08-26 兩個分頁同時開同一個對話，會互相覆蓋掉對方的修改

**問題**：使用者提問「如果兩個瀏覽器都開啟同一個對話，一個顯示鎖定一個顯示未鎖定，會發生什麼事？」。實際追查程式碼後確認：`PUT /test-cases`（表格任何欄位編輯後 1 秒 debounce 自動存檔的那個 API）是整包陣列覆寫——`conversation.last_result = payload`，直接用瀏覽器送來的整份測試用例清單蓋掉伺服器上的版本，中間完全沒有版本號或時間戳記比對。假設分頁 A 剛把某筆用例鎖定存檔成功，分頁 B 因為還沒重新整理，記憶體裡的快照仍是鎖定之前的樣子；只要 B 之後做了任何觸發自動存檔的編輯（不需要是同一筆用例），B 就會把整包過期快照存回去——`enforce_lock_on_manual_edit` 只保護「舊版有鎖、新版也有鎖」這種情況，B 送來的是「新版沒鎖」，會被判定成「使用者主動解鎖」而直接接受，等於悄悄蓋掉 A 剛做的鎖定，而且兩邊都不會跳出任何錯誤或警告。範圍其實比鎖定欄位更大：因為是整包覆寫，A 對其他未鎖定用例做的修改，只要不巧被 B 的過期快照覆蓋，也會一起消失。

**修法**：加上一個最小可行的**樂觀併發控制（optimistic concurrency control）**——`GenerationResult` 新增 `result_version` 欄位，每次 `generate`／`chat`／`update_test_cases` 成功寫入 `conversation.last_result` 都會把它 `+1`。`update_test_cases` 存檔前，比對「這次編輯是基於哪個版本」（前端送來的 `result_version`）跟伺服器目前的版本：一致才允許覆寫，不一致就回 409、完全不寫入。前端收到 409 後（`api.ts` 新增 `isConflictError` 判斷式），不會把使用者剛才的編輯硬套上去，而是重新抓一次伺服器目前的最新內容蓋掉本地這份過期狀態，並跳出提示「這個對話的測試用例已經在別的分頁被修改過，已為您載入最新內容，請重新確認並套用一次」——比起用假裝很快、讓其中一邊的修改悄悄消失，誠實地告訴使用者發生了什麼、並保證看到的一定是伺服器上最新的真實狀態，是更安全的選擇。這個版本號機制只保護「整包覆寫」這條最危險的路徑（手動編輯自動存檔）；`generate`／`chat` 兩個由使用者主動觸發的動作，因為本來就是讀取伺服器當下最新狀態去合併（不是拿用戶端的舊快照去覆寫），不受這個問題影響，這次也維持原樣沒有另外加上 client 端版本檢查（同一個分頁不可能同時按兩次產生／送出聊天；跨分頁同時各自觸發一次 AI 呼叫是更罕見的情境，先不在這次範圍內處理，作為已知限制記錄下來）。

**背後的通用觀念**：**「後寫的贏（last write wins）」在只有單一使用者、單一分頁的情境下看不出問題，一旦有多個地方可能同時寫入同一份資料，就會變成沉默的資料遺失。** 這裡最陰險的地方在於：沒有任何一方會收到錯誤——A 的鎖定存檔成功、B 的覆寫也存檔成功，兩次 API 呼叫都回 200，但兩次呼叫合起來的結果是「A 的修改憑空消失」，比起明顯的當機或報錯更難察覺、更難除錯（使用者只會覺得「我明明鎖定了，怎麼變回沒鎖」，完全想不到根因是另一個分頁）。**樂觀併發控制**是解決這類問題的標準做法：不是每次寫入前都上鎖擋住其他人（悲觀鎖，會犧牲很多可用性），而是讓每次寫入都帶著「我是基於哪個版本做的修改」，伺服器只在版本吻合時才接受，版本對不上就直接拒絕、讓呼叫端自己決定怎麼處理衝突（這裡選擇最簡單的策略：重新載入、請使用者重做）。這也是 HTTP `ETag` / `If-Match` header、資料庫樂觀鎖（一張表存一個 `version` 欄位）背後的同一個原理，只是這次是自己土法煉鋼加一個整數欄位實作出最小可用的版本。另外值得記住：**判斷「一個寫入端點安不安全」不能只看它自己的邏輯對不對，要問「如果同一份資料在我讀取之後、寫入之前，被別人動過，我的寫入還會是對的嗎？」**——這次的 `enforce_lock_on_manual_edit` 邏輯本身完全正確（鎖定保護的判斷沒有 bug），問題出在它的前提假設「傳進來的 `previous`／`incoming` 都是同一份、沒有人插隊」在多分頁情境下不成立。

**檔案**：`backend/app/models/test_case.py`（`GenerationResult` 新增 `result_version`）、`backend/app/services/test_case_lock.py`（新增 `VersionConflictError`、`check_result_version`）、`backend/app/routers/conversations.py`（`generate`／`chat`／`update_test_cases` 遞增版本，`update_test_cases` 版本不符時回 409）、`backend/tests/test_test_case_lock.py`（`check_result_version` 的通過／衝突測試）、`frontend/src/types.ts`（`GenerationResult` 加 `result_version`）、`frontend/src/api.ts`（`handleResponse` 把 HTTP 狀態碼掛在拋出的 `Error` 上、新增 `isConflictError`）、`frontend/src/pages/WorkspacePage.tsx`（自動存檔與匯出前存檔遇到衝突時改呼叫 `reloadResultAfterConflict` 重新載入最新內容並提示）

**驗證方式**：`cd backend && pytest tests/`（51 個測試全過）；`cd frontend && npx tsc --noEmit && npx vitest run`（12 個測試全過）；`cd e2e && npm test`（golden-path 全流程仍然通過，確認新加的版本檢查沒有擋到正常的單分頁使用情境）。瀏覽器實測（dev 環境 18002/5175）：開兩個分頁載入同一個對話，分頁 B 先鎖定用例 2 並確認存檔成功；分頁 A（快照仍是鎖定前的狀態）接著鎖定用例 1，觀察到該次 `PUT /test-cases` 確實收到 409、畫面跳出提示、並自動重新載入成最新內容（正確顯示用例 2 已鎖定、用例 1 這次的鎖定操作被安全丟棄未套用）；接著在分頁 A 重新點一次鎖定用例 1，這次因為已經是最新版本，存檔成功，兩個分頁的鎖定狀態最終正確一致。

## 2026-08-26 聊天等待回應時補上經過秒數，讓使用者知道沒有卡住

**問題**：使用者反映跟用例助手聊天時，送出訊息後畫面只顯示一句靜態的「思考中…」，等待期間完全看不出任何變化——內部模型回應本來就可能要幾秒到幾十秒不等（尤其附件是圖片或大份文件時），但使用者無從分辨「這是正常等待」還是「其實已經卡住／壞掉了」，只能乾等或猜測。

**修法**：`ChatPanel.tsx` 新增一個 `elapsedSeconds` 狀態，配合 `useEffect` 在 `busy` 變成 `true` 時啟動一個每秒 `+1` 的 `setInterval`（`busy` 變回 `false` 時清掉並歸零），把秒數即時顯示在「思考中…」後面（例如「思考中…（已等待 12 秒）」）。這個數字本身就是最直接的「活著」證明——它持續在跳動，代表前端跟這次請求都還在正常運作，不是畫面凍結；使用者也能自己判斷「這次等比較久，但還在動，先等等看」還是「感覺太久了」。額外加了一條規則：秒數達到 20 秒後，在泡泡下方多顯示一行小字提醒「模型回應時間較長屬正常現象，尤其是附件含較多圖片或文件時，請耐心等候」，把「這是預期內的正常現象」講清楚，減少使用者誤以為壞掉的疑慮。刻意沒有加轉圈動畫或閃爍效果——秒數本身持續遞增就足以傳達「還在跑」，不需要額外的動態視覺（也呼應這個專案稍早才把持續閃爍的提示改回靜態的教訓，見上一則）。

**背後的通用觀念**：**一個操作需要等待時，光靠「看起來像是在等」的靜態文字（如「Loading…」「思考中…」）沒辦法回答使用者心裡真正的問題——「這個東西是不是壞了」。** 真正能回答這個問題的是**一個會隨時間持續變化、使用者能親眼確認它還在動的訊號**，最單純可靠的形式就是一個真的在跳動的計時數字：它不需要额外的視覺設計技巧，卻直接證明了「渲染這個畫面的程式碼此刻仍在正常運作」。這跟單純的載入動畫（CSS `@keyframes` 迴圈）不同——CSS 動畫本身是瀏覽器引擎在跑，即使頁面的 JS 邏輯（例如負責這次請求的程式）真的卡住了，動畫依然會繼續轉，反而可能是一種「假裝活著」的誤導訊號；而由 JS 的 `setInterval` 驅動、真的在遞增的秒數，才跟「這個功能背後的邏輯是否還在正常運作」直接掛鉤。另外也提醒一件事：等待時間長不一定是 bug，很多時候就是任務本質（例如呼叫一個處理圖片的大型語言模型）本來就需要這麼久——與其假裝很快、讓使用者對「正常」的期待落空，不如誠實地告訴他「這是正常的，請再等等」，把使用者的疑慮轉換成明確的資訊。

**檔案**：`frontend/src/components/ChatPanel.tsx`（新增 `elapsedSeconds` 狀態與計時 `useEffect`，「思考中…」泡泡加上秒數與 20 秒後的提示文字）、`frontend/src/index.css`（新增 `.chat-bubble-hint`）

**驗證方式**：`npx tsc --noEmit` 與 `npx vitest run`（12 個測試全過，沒有動邏輯層）；瀏覽器實測（dev 環境 18002/5175）——送出聊天訊息後用 JS 每 300ms 對「思考中…」泡泡的文字內容取樣，實際觀察到秒數從「已等待 2 秒」跳到「已等待 3 秒」後泡泡被真正的回覆取代，確認計時器真的隨時間遞增、不是寫死的文字，回應完成後泡泡也正確消失、秒數歸零。

## 2026-08-26 未回答問題的閃爍提示改成靜態，不再持續閃

**問題**：未回答的澄清問題（聊天記錄裡的問題泡泡、收合狀態的對話按鈕）原本會用無限循環的動畫持續閃爍來提醒使用者，但使用者實際使用後回報：這個動畫一直在跑，會讓人分心、很難專注在正在做的事情上——不管是正在讀那則訊息，還是正在編輯旁邊的測試用例表格，眼角餘光一直有東西在閃，注意力一直被拉走。

**修法**：把兩處的 `animation: ... infinite`（在原色跟琥珀色之間不斷來回漸變）拿掉，改成套用同一組琥珀色系的**靜態**樣式——顏色還是跟平常不一樣、還是看得出來「這裡有需要注意的東西」，但不再有動態效果一直反覆播放。「有沒有醒目的視覺提示」跟「這個提示會不會動」是兩件事，可以只留前者、拿掉後者。

**背後的通用觀念**：**用「持續的動態效果」做提醒，是有代價的**——動畫天生會不斷重新吸引人的視覺注意力（這正是它能「提醒」的原因），但代價是使用者沒辦法把它當背景訊息忽略掉，只要它在視野範圍內，就會持續打斷專注力，跟「已讀」「知道了但現在還不想處理」這種常見的使用情境是衝突的。一次性的動畫（提醒使用者「有新狀況」）跟靜態的視覺標記（讓使用者「隨時瞄一眼就能確認狀態」）是兩種不同的設計意圖，適合的情境也不一樣：前者適合「剛發生、使用者應該馬上看到」的事件，後者適合「這個狀態會持續存在一段時間、使用者會反覆看到同一個提示」的情境。未回答的問題屬於後者——它可能會在畫面上停留很久，這種情況就不該用會一直重播的動畫。

**檔案**：`frontend/src/index.css`（拿掉 `@keyframes question-blink`／`.question-list-blink` 跟 `@keyframes chat-fab-blink`／`.chat-fab-blink` 裡的 `animation`，改成直接套用靜態顏色）

**驗證方式**：瀏覽器實測，用 JS 檢查兩個元素的 `getComputedStyle(...).animationName` 都是 `none`（不是套了動畫只是碰巧沒觸發），且背景色仍然是原本閃爍時的琥珀色（沒有失去視覺提示效果，只是不再閃）。

## 2026-08-26 測試用例可拖曳排序，鎖定只保護內容不保護順序

**問題**：使用者想手動調整測試用例的執行順序（把相關用例排在一起、排出想要的執行順序），但測試用例清單只能照 AI 產生的順序排列，沒有拖放功能——用例內部的「步驟」已經可以拖曳排序，整個用例清單本身卻沒有對應功能。

**修法**：比照既有的步驟拖曳排序（`reorderSteps`）同一套 HTML5 原生拖放機制，在 `TestCaseTable.tsx` 加上用例層級的 `reorderCases`：每張用例卡片標題列加一個拖曳把手（沿用步驟拖曳的六點圖示），`caseDragIndex`/`caseDragOverIndex` 狀態跟步驟拖曳用的是各自獨立的一組，不會互相干擾。

設計上有一處原本沒想清楚：一開始預設「鎖定的用例也不能拖動」，但使用者馬上問了一個關鍵問題——如果兩筆鎖定的用例中間夾著一筆未鎖定的，那筆未鎖定的用例會不會因為兩邊鎖住了而動彈不得？重新想過後確認：**鎖定的本意是保護內容不被誤改，跟排在第幾個是兩件不相干的事**，所以改成鎖定完全不影響拖曳——不管鎖定與否，任何一筆用例都可以自由拖動調整順序；鎖定依然只影響能不能改名稱／步驟／優先級這些內容欄位，跟這次的拖曳排序邏輯完全獨立。

**背後的通用觀念**：**設計一個「保護機制」的時候，要先想清楚保護的對象到底是哪個維度——不要因為兩個維度都跟同一筆資料有關，就預設保護規則要一併套用**。這次一開始直覺是「鎖定＝什麼都不能動」，聽起來合理，但「內容」跟「順序」其實是兩個獨立的維度：內容錯了會影響測試結果本身的正確性，順序錯了頂多是執行體驗不順，兩者的風險並不對等，值得分開決定要不要保護。「順序」這個維度還有一個內容維度沒有的特性——它是**相對的**（一筆東西排第幾，取決於它旁邊還有什麼），保護規則如果沒設計好，很容易出現「這筆東西本身沒被保護，卻因為鄰居被保護了而動彈不得」這種連帶效應，這正是這次被使用者一問就問出破綻的原因。下次遇到類似「這個操作要不要被某個保護狀態鎖住」的設計，值得先問一句：這個維度是獨立的嗎？還是會被相鄰的東西連帶影響？

**檔案**：`frontend/src/components/TestCaseTable.tsx`（新增 `caseDragIndex`/`caseDragOverIndex` 狀態、`reorderCases`、`.case-drag-grip` 拖曳把手）、`frontend/src/index.css`（`.case-drag-grip`、`.case-dragging`、`.case-drag-over`）

**驗證方式**：瀏覽器實測——原生 HTML5 拖放需要真正的滑鼠拖曳手勢，一般滑鼠模擬工具無法觸發，改用 JS 直接派送真正的 `DragEvent`（`dragstart`／`dragover`／`drop`）模擬完整序列：拖一筆未鎖定的用例到清單最前面，確認順序正確更新；拖一筆鎖定的用例到清單最後面，確認同樣能移動、且移動後仍然維持鎖定狀態（欄位還是唯讀）；兩次操作後都確認自動存檔（`PUT /test-cases` 回 200）且重新整理頁面後順序有正確持久化，不是只有前端暫時狀態。

## 2026-08-25 用例鎖定審核功能上線後的 CI／UX 回饋修正

### e2e 測試沒更新，被新加的匯出鎖定檢查擋下

**問題**：加了「未鎖定用例不能匯出」之後，PR 的 e2e CI 直接紅燈——`golden-path.spec.ts` 產生一筆用例後直接點「匯出 Excel」等下載事件，但那筆用例預設是未鎖定的，匯出被新規則擋下、永遠等不到下載事件，30 秒後逾時失敗。

**修法**：這是預期中的行為改變，不是 bug，e2e 測試要跟著新流程更新——點匯出前先點「鎖定」按鈕、確認狀態變成「已鎖定」，再繼續原本等下載事件的流程。過程中也順便修正測試本身的一個小錯誤：一開始寫成用 `getByRole('button', { name: '鎖定，標記為已審核' })` 找按鈕，但那段文字其實是 `title` 屬性（滑鼠移過去的 tooltip），按鈕的可及名稱（accessible name）是由可見文字內容決定的（這裡是「鎖定」），`title` 只有在完全沒有文字內容時才會被拿來當可及名稱的備援，兩者是不同的東西——改成用可見文字「鎖定」/「已鎖定」（加 `exact: true` 避免兩者互相符合）才抓得到按鈕。

**背後的通用觀念**：**e2e／可及性測試裡的「這個元素叫什麼名字」，瀏覽器有一套固定的優先順序去決定（accessible name computation：`aria-label` > `aria-labelledby` > 表單元素的 `<label>` > 元素本身的文字內容 > `title` 屬性，順位高的存在就不會往下看），不是「畫面上看得到的任何文字」都算數。**寫測試選擇器、或幫元件加 `title`／`aria-label` 想讓螢幕報讀器或測試框架抓到正確名稱時，要知道自己加的屬性在這個優先順序裡排第幾、會不會被更前面的東西蓋掉。這次的情況是按鈕同時有文字內容（「鎖定」）又有 `title`（更長的說明），文字內容優先權比 `title` 高，所以可及名稱是短的那個，跟 tooltip 顯示的不是同一份文字，測試選錯了就永遠找不到元素。另外也提醒：新增一個會**改變既有操作流程**的規則（這裡是「匯出前必須全部鎖定」）時，要記得回頭檢查有沒有既有的自動化測試假設了舊流程，不只是加新測試涵蓋新規則本身。

**檔案**：`e2e/tests/golden-path.spec.ts`（匯出前補上鎖定步驟，選擇器改用可見文字）

**驗證方式**：本機跑 `cd e2e && npm test`，確認整條「建立專案→新增素材→產生測試用例→鎖定→匯出 Excel」流程通過（本機因為機器同時開著好幾個開發用的伺服器、資源競爭導致 webServer 啟動偶爾逾時，屬於本機環境雜訊，跟這次的程式改動無關，重跑幾次穩定會過；CI 的 runner 是乾淨獨立環境不會有這個問題）。

### 鎖定狀態不夠明顯、匯出攔截提示不會消失且重複點擊只生效一次

**問題**：三個使用者實測回報的問題：(1) 鎖定狀態的視覺提示只在卡片標題列最右側一顆小按鈕上，容易被優先級／步驟數等其他文字淹沒，掃過去很難注意到「這筆已經鎖定了」；(2) 匯出被「還有用例未鎖定」擋下時顯示的紅字提示，鎖定該筆用例後提示仍然留在畫面上不會消失；(3) 如果连續點兩次匯出、兩次都被同一筆用例擋下，第二次點擊畫面沒有反應，看起來像「這個跳轉功能只在第一次有效」。

**修法**：
1. **鎖定狀態更醒目**：鎖定按鈕從標題列最右側移到緊接在用例名稱後面（跟標題同一視覺焦點），文字也從「鎖定」變成「已鎖定」並改用綠色（審核通過的慣用色，跟原本的琥珀色警示色區分開）；卡片本身加上綠色左邊框，用例編號圓形徽章也一併變成綠色——從「要找到右下角一顆小按鈕」變成「一眼掃過去卡片左側的顏色跟編號就知道」。
2. **提示訊息會自動消失**：根因是原本直接借用整個頁面共用的 `error` 狀態顯示這則提示，這個狀態沒有任何自動清除機制，只會在下一次別的操作呼叫 `setError(null)` 時才被蓋掉，使用者鎖定完用例後這個訊息完全沒有連動。改成獨立的 `lockBlockNotice` 狀態，配一個 `useEffect` 盯著測試用例清單：只要當初擋下匯出的那一筆變成鎖定狀態（或全部都鎖定了），就自動把提示收掉，不用使用者自己意識到「喔看來已經處理好了」還要手動想辦法讓提示消失；另外也加了一顆「✕」讓使用者可以隨時手動關掉。
3. **重複點擊每次都要生效**：根因是「跳轉到第一筆未鎖定用例」的邏輯放在一個 `useEffect`，依賴陣列只有「第幾筆」這個數字；如果連續兩次擋下剛好指向同一筆（數字沒變），React 認為這個值沒有變化，不會重新執行 effect，捲動／展開的動作就不會再跑一次，實際上卻是「使用者又點了一次匯出」這個新事件，不應該被當作沒發生過。修法是額外帶一個每次擋下就遞增的 `token`，把它也放進 `useEffect` 的依賴陣列——這樣即使「第幾筆」的值沒變，`token` 一定會變，effect 保證每次都重新執行。

**背後的通用觀念**：**React 的 `useEffect` 依賴陣列比較的是「值有沒有變」，不是「這件事有沒有再發生一次」**——如果你需要的是後者（每次某個使用者動作發生都要重新跑一段邏輯，不管前後兩次的參數是否剛好相同），只放實際參數當依賴是不夠的，因為值相同時 React 會合理地跳過重跑。常見解法就是額外帶一個單調遞增的「事件序號」（token / nonce）進依賴陣列，用它來表達「這是一個新事件」而不是「這是一個新的值」，两者是不同的語意，混在一起會出現這種「同樣的操作，偶爾看起來沒反應」的詭異 bug。另外，提示訊息要不要顯示、什麼時候該消失，跟它想通知的那件事的「目前是否仍然成立」綁在一起做成一個 reactive 的判斷（而不是「顯示了就一直顯示，直到某個不相干的地方剛好把它蓋掉」），是更貼近使用者心智模型的做法——使用者會預期「我把它處理好了，提示就該消失」，UI 也該照這個預期走。

**檔案**：`frontend/src/components/TestCaseTable.tsx`（鎖定按鈕移到標題旁、新增 `focusToken` prop 強制重新觸發捲動）、`frontend/src/index.css`（`.case-card-locked` 綠色左邊框／編號徽章、`.case-lock-toggle-locked` 改綠色、新增 `.lock-block-notice` 樣式）、`frontend/src/pages/WorkspacePage.tsx`（`lockBlockNotice` 狀態取代借用 `error`，`useEffect` 自動清除，`handleExport` 遞增 token）

**驗證方式**：瀏覽器實測，對一筆已鎖定的用例先解鎖、連續點兩次匯出（不做任何其他操作），用 JS 確認第二次點擊後提示文字依然正確顯示且該筆用例依然是展開狀態（不是「維持第一次點擊後的殘留畫面」，是真的重新觸發了一次）；接著鎖定該筆用例，確認提示在沒有任何額外操作下自動消失；全部鎖定後點匯出，確認 API 回 200、正常匯出成功。

### 未回答問題的閃爍提示只有打開對話框才看得到

**問題**：先前實作的「AI 提出的問題還沒回答就閃爍」，閃爍的元素是聊天記錄裡的那則問題泡泡——但 `FloatingChat` 收合時整個 `ChatPanel`（含聊天記錄）根本沒有掛載在畫面上，等於這個閃爍提示只有使用者「已經點開對話框」之後才看得到，而使用者恰好最需要被提醒的情境就是「還沒點開、不知道有問題在等他」，提示形同虛設。

**修法**：閃爍的目標從對話框內部的問題泡泡，改成（同時保留）收合狀態下那顆圓形對話按鈕本身——`FloatingChat` 原本就掌握著完整的 `log` 陣列，直接在這裡判斷「最後一則是不是 AI 提出、使用者還沒回應的問題」，是的話幫收合按鈕加閃爍動畫（改變底色＋放大淡出的光暈效果，比對話框內部原本那個柔和的背景色漸變更搶眼，因為要能在螢幕邊角用餘光注意到）。原本對話框內部的閃爍邏輯不用拿掉——使用者點開之後，在一串聊天記錄裡能快速定位到「就是這一則」仍然有用，兩層提示各自解決不同階段的問題（先讓人注意到「有東西要看」，再讓人在點開之後快速找到是哪一則）。

**背後的通用觀念**：**幫某個狀態加視覺提示時，要想清楚「使用者在看到這個提示的當下，畫面上實際掛載、看得到的是什麼」，不能只想著「資料層面這個狀態存不存在」。** 這裡的資料判斷邏輯（是否有未回答問題）從頭到尾都是對的，問題出在放閃爍效果的那個 DOM 節點，剛好是一個「只有在使用者已經做了某個動作（點開對話框）之後才會被渲染」的節點——等於這個提示的可見性被綁死在使用者已經完成的動作之後，變成「提醒使用者去做他已經做過的事」，邏輯上自洽但完全沒達到提醒的目的。判斷一個提示有沒有效，不能只驗證「條件成立時樣式有沒有套上去」，還要驗證「條件成立的那個當下，使用者的螢幕上真的看得到套了樣式的那個元素嗎」。

**檔案**：`frontend/src/components/FloatingChat.tsx`（收合按鈕依 `log` 最後一則判斷是否閃爍）、`frontend/src/index.css`（`@keyframes chat-fab-blink`／`.chat-fab-blink`）

**驗證方式**：瀏覽器實測，對一個聊天記錄最後一則是未回答問題的對話，收合對話框後用 JS 檢查 `.chat-fab` 元素的 `animation-name` 確實是 `chat-fab-blink`（不用先點開才看得到）；按鈕 `title` 也同步顯示「測試用例助手（有未回答的問題）」。

### 匯出提示還是會被捲動滑走、跟另一則提示各用一套機制且太佔版面

**問題**：上一輪把「未鎖定用例」的提示改成會自動消失，但位置還是放在標題列下方、用例表格上方——這個位置本身就在會捲動的區塊裡，使用者往下滑看後面的用例，提示照樣跟著滑出畫面外，等於「會不會自動消失」修好了，但「滑一下就看不到」這個更根本的問題還在。另外「還有尚未回答的澄清問題」那則提示是另外借用整頁共用的 `error` 狀態顯示，跟「未鎖定」提示是兩套完全不同的機制、長在不同地方，畫面上一次只可能出現其中一種，卻要維護兩套邏輯；「未鎖定」提示本身也因為 CSS 用了 `flex: 1` 把標題列剩餘空間全部吃滿，看起來異常寬大，排擠到旁邊的按鈕。

**修法**：
1. **搬進標題列，借用既有的 sticky 定位**：標題列（含對話名稱、匯出按鈕）本來就是 `position: sticky; top: 0`，會一直釘在畫面頂端不隨內容捲動。把提示直接搬進這一列，不用另外實作「固定在頂端」的邏輯，直接沿用已經在運作的機制。
2. **兩則提示合併成同一個狀態、同一個插槽**：`lockBlockNotice`（只處理未鎖定）跟借用的 `error`（處理未回答問題）合併成一個通用的 `workspaceNotice: { message, focusIndex?, focusToken? }`——`focusIndex`/`focusToken` 是「未鎖定」那種需要捲動跳轉的提示才會帶，「未回答問題」那種純資訊性的提示就不用帶。畫面只需要一份渲染邏輯、一份自動清除的 `useEffect`（依 `focusIndex` 是否存在分兩支：未鎖定提示看該筆用例是否已鎖定，未回答提示看聊天記錄最後一則是否還是未回應的問題），不用兩套平行維護的程式碼。
3. **瘦身**：拿掉 `flex: 1`，改成 `max-width: 200px` 搭配文字超出省略號（`text-overflow: ellipsis`），提示文字本身也精簡成短版（例如「⚠️ 有用例未鎖定」），完整說明改放進 `title` 屬性，滑鼠移過去才看到，畫面上不再佔用一大塊空間。

**背後的通用觀念**：**「這個提示會不會消失」跟「這個提示會不會被使用者滑走看不到」是兩個獨立的問題，各自需要不同的機制解決（前者是狀態要不要清除，後者是定位方式）**，只修好其中一個不代表整個「提示提醒到使用者」的目標就達成了——上一輪只處理了第一個問題，這一輪才處理第二個，回頭看很容易誤以為「提示相關的 bug」是同一件事分兩次修，其實是兩個不同性質的需求。另外，**當畫面上出現兩個「性質相同、但觸發條件不同」的東西（這裡是兩種匯出時的提示），與其各自用一套獨立的狀態／渲染邏輯，不如提早收斂成一個共用的資料結構跟渲染插槽**，用欄位是否存在（這裡是 `focusIndex` 有沒有值）分辨「這次是哪一種」，而不是複製一份幾乎一樣的 JSX 跟 CSS class——不只減少重複程式碼，之後如果又冒出第三種類似的提示，也知道該長在同一個地方、用同一套規則，不用每次都重新設計一次「提示要放哪裡、怎麼消失」。

**檔案**：`frontend/src/pages/WorkspacePage.tsx`（`workspaceNotice` 取代 `lockBlockNotice` 跟借用的 `error`，`useEffect` 分兩支自動清除，notice 搬進 `workspace-title-row`）、`frontend/src/index.css`（`.workspace-notice` 取代 `.workspace-lock-notice`，拿掉 `flex: 1` 改成 `max-width` + ellipsis）

**驗證方式**：瀏覽器實測，觸發「未鎖定」提示後用 JS 把 `.app-main`（實際的捲動容器）`scrollTop` 設成 300px，確認提示元素的 `getBoundingClientRect().top` 前後完全沒變（真的釘住了，不是碰巧沒捲到那麼遠）；鎖定該筆用例後提示自動消失；接著讓「未回答問題」那則提示觸發，確認它長在同一個 `.workspace-notice` 插槽、同樣的寬度（不是另外一套更寬的樣式），點「✕」可以手動關閉；全部鎖定後匯出，確認 API 回 200。

## 2026-08-25 測試用例人工審核／鎖定，含未回答問題提示

### 鎖定後人工與 AI 都不能修改，未鎖定不能匯出

**問題**：測試用例產生出來之後，人工編輯（表格手動改欄位）跟 AI 對話式編輯（聊天下指令）都可以隨時整批覆蓋掉任何一筆用例的內容，沒有「這筆已經審核過、之後不能再動」的機制；也沒有任何機制擋下「還沒審核完就直接匯出」。

**修法**：`TestCase` 新增 `id`（穩定識別，之前完全沒有——`diffTestCases.ts` 一直是靠名稱比對，見 CLAUDE.md 已知限制）跟 `locked` 兩個欄位。人工編輯跟 AI 編輯是兩條不同的路徑，用的比對方式也不一樣，所以拆成兩個獨立的把關函式（`backend/app/services/test_case_lock.py`）：
- 人工整批 PUT（`update_test_cases`）：前端本來就會把 `id` 原封不動送回來，所以後端可以直接用 `id` 比對——舊資料是鎖定狀態、且這次送來的還是鎖定狀態，就整筆用舊版蓋掉使用者這次送來的內容（等於強制唯讀）；如果這次送來的是解鎖狀態，才當作「解鎖並編輯」放行。
- AI 對話編輯（`/chat`）：LLM 回傳的 JSON 完全不知道 `id` 是什麼，只能用**名稱**比對。鎖定用例如果在 LLM 回傳結果裡同名還在，就整筆換回鎖定前的版本，內容如果真的跟 LLM 想給的不一樣，額外塞一則「已鎖定審核，AI 嘗試調整但未套用」的澄清問題進回傳結果，讓使用者知道 AI 想改但被擋下、而不是靜默失敗；如果 LLM 乾脆把整筆用例從回傳結果裡拿掉了（改名或誤刪），一樣把舊版插回去並補問題。這一層後端強制是最後防線，另外也在 `SYSTEM_PROMPT_CHAT` 加規則、把鎖定用例名單放進 prompt，讓 AI 事先知道不要去動，體驗上會比每次都被後端事後攔截更順。匯出（`/export/excel`）前後端都會擋：前端在按下匯出當下先檢查，擋下的話直接跳到表格裡第一筆未鎖定的用例、不送出任何 API 請求；後端 `export_excel` 也重複檢查一次當防禦性保險（避免前端狀態不同步時繞過限制）。

**背後的通用觀念**：**同一份資料如果有兩條不同的寫入路徑（這裡是「使用者手動編輯」跟「AI 生成覆蓋」），不能只在其中一條路徑上做防呆**——兩條路徑各自的資料型態、能拿到的識別資訊都可能不一樣（人工編輯的前端狀態有穩定的 `id`，AI 只看得懂名稱），如果只用同一套邏輯硬套在兩條路徑上，要嘛在其中一條路徑上比對失敗、要嘛乾脆漏掉沒做防護。正確做法是先搞清楚每條路徑實際能依賴什麼資訊來做識別與比對，再各自寫對應的把關邏輯，而不是假設「反正都是同一個資料結構，用同一種比對方式就好」。另外，「AI 想做某件事，但因為某個約束不能直接做」的情境，比起靜默丟棄或直接報錯失敗，把它轉換成一則明確的澄清問題（也就是既有的溝通管道）交還給使用者決定，是更自然的處理方式——不用另外設計一套新的錯誤通知機制，重用已經存在的、使用者已經熟悉的互動介面就夠了。

**檔案**：`backend/app/models/test_case.py`（`TestCase` 新增 `id`／`locked`／`based_on_images`）、`backend/app/services/test_case_lock.py`（新檔，`enforce_lock_on_manual_edit`／`enforce_lock_on_llm_result`）、`backend/app/routers/conversations.py`（`generate`／`chat`／`update_test_cases` 都套用對應的把關函式）、`backend/app/routers/export.py`（未鎖定擋匯出）、`backend/app/services/prompt_builder.py`（`SYSTEM_PROMPT_CHAT` 加鎖定規則，`build_chat_messages` 把鎖定名單放進 prompt）、`frontend/src/components/TestCaseTable.tsx`（鎖定/解鎖按鈕、鎖定時所有欄位與操作唯讀、`focusCaseIndex` 跳轉）、`frontend/src/pages/WorkspacePage.tsx`（匯出前檢查未鎖定用例並跳轉）

**驗證方式**：後端新增 `test_test_case_lock.py`（人工編輯鎖定/解鎖各種情境、AI 編輯內容被攔下並補問題、AI 刪掉鎖定用例被補回來），`pytest tests/` 47 個測試全過。瀏覽器實測：對一個真實有 6 筆用例的對話，逐一鎖定並確認每鎖一筆匯出就往下一筆未鎖定的跳，欄位變唯讀（用 JS 檢查 `disabled` 屬性逐一確認，不只是看畫面）；全部鎖定後匯出成功（後端回 200）；接著用聊天要求「把所有用例優先級都改成 P0」，確認已經是 P0 的兩筆沒有觸發任何提示（代表沒有誤判「沒真的改變內容」也算違規），另外四筆（優先級不是 P0）各自被攔下、內容維持不變，且聊天回應裡各自出現一則對應的澄清問題。

### 未回答的澄清問題氣泡閃爍提示

**問題**：AI 提出的澄清問題如果使用者一直沒回應，畫面上完全沒有任何提示——問題氣泡跟已經回答過的訊息長得一樣，容易被使用者忽略，匯出的時候也不會有任何警示。

**修法**：判斷「未回答」完全不需要新的資料欄位或狀態機——聊天紀錄本來就是一個陣列，AI 提出問題之後，只要使用者還沒有送出下一則訊息，這則問題就還是陣列的最後一筆；一旦使用者送出任何新訊息（不管有沒有真的答對問題），它自然就不再是最後一筆。所以只要在畫面渲染時判斷「這個問題氣泡是不是聊天紀錄的最後一項、而且是 AI 發的」，是的話就加一個會閃爍的 CSS class。匯出時不擋——只是在真正送出匯出請求前多檢查一次同樣的條件，是的話跳一則不會中斷流程的提示文字，使用者可以直接無視繼續匯出。

**背後的通用觀念**：**不是所有「狀態」都需要一個對應的資料欄位去記錄**——如果某個狀態可以直接從既有資料（這裡是聊天紀錄陣列本身的順序）推導出來，額外加一個 `answered: boolean` 欄位反而是多餘的：多一個地方要記得同步更新、多一種「資料跟畫面對不起來」的方式可能發生（例如某個路徑忘記把 `answered` 設回 `true`）。能用「衍生計算」取代「額外儲存的狀態」時，優先選前者，資料來源只有一份、不會有兩份資料互相打架的風險。

**檔案**：`frontend/src/components/ChatPanel.tsx`（判斷是否為最後一筆問題氣泡並加上 class）、`frontend/src/index.css`（`@keyframes question-blink`／`.question-list-blink`）、`frontend/src/pages/WorkspacePage.tsx`（匯出前檢查並顯示非阻斷提示）

**驗證方式**：瀏覽器實測，用 JS 檢查 `.question-list-blink` 元素確實存在且套用了 `animation-name: question-blink`；讓 AI 再提出新一輪問題後，確認閃爍會轉移到最新一則、舊的那則不再閃爍；直接匯出確認會出現「⚠️ 目前有尚未回答的澄清問題」提示，且匯出仍正常完成（Excel 匯出 API 回 200，沒有被擋下）。

### 測試用例顯示依據圖片縮圖，可點擊放大

**問題**：測試用例產生出來之後，完全看不出這筆用例是根據哪一張截圖寫的——之前雖然已經讓模型在文字裡用「圖N」引用截圖（見下面 2026-08-22 那筆記錄），但畫面上的用例本身沒有把對應的縮圖秀出來，使用者想確認某筆用例的依據，得自己回素材庫一張張找。另外聊天室（用例助手）裡使用者貼上的截圖雖然有顯示縮圖，但不能點擊放大看細節。

**修法**：
1. **讓模型直接標出用到哪幾張圖**：`TestCase` 新增 `based_on_images: list[int]` 欄位，值是「圖N」的編號；prompt 規則要求模型每筆用例都要重新判斷、標出實際引用到的圖片編號，純粹根據文件文字寫的用例留空陣列即可。
2. **編號改成跨素材連續遞增**：原本「圖N」是每個素材各自從 1 開始編號，同一個 prompt 裡如果選了兩個素材，會出現兩個不同的「圖1」，模型分不清、也沒辦法簡單反查回真正的圖片網址。改成用一個貫穿所有素材的計數器，讓編號在整個對話裡是唯一的；連原本沒有分組、只有單張主圖的圖片素材也一併給編號（之前是完全沒有標記），這樣任何一張送給模型的圖都能被引用、也都能反查。`build_material_content`（組 prompt 文字）與新增的 `resolve_image_numbers`（反查「圖N」對應的真實素材與網址）共用同一個走訪函式 `_iter_numbered_images`，避免兩份邏輯各自維護、编號對不起來。
3. **新增查詢端點**：`GET /conversations/{id}/image-map` 把目前選取素材的「圖N」反查表整包回傳；前端在載入對話、產生用例後、聊天中新增附件後都會重新抓一次，轉成 `Map<number, ImageRef>` 存在 `WorkspacePage` 裡往下傳。
4. **畫面呈現**：`TestCaseTable` 每筆用例展開時，如果 `based_on_images` 非空，標題下方會有一排縮圖（沿用素材庫既有的圓形編號徽章樣式），點擊可以放大看大圖。同時把「縮圖點擊放大」這段邏輯（`MaterialRow.tsx` 原本就有一份）抽成共用的 `useImageLightbox()` hook，`TestCaseTable` 跟 `ChatPanel`（用例助手訊息裡使用者貼的截圖）都改用同一份，不必各自重寫一次彈窗邏輯。

**背後的通用觀念**：**同一份「編號」邏輯如果只實作在一個地方（這裡原本只有組 prompt 文字那邊），日後只要有第二個地方需要用到同一套編號（這次是反查真實圖片網址），最安全的做法不是照抄一份新邏輯，而是把「怎麼編號」抽成一個共用函式，兩邊都呼叫它**——如果各寫一份，兩份邏輯只要有一點點不同步（例如其中一份忘記處理某個邊界情況），編號就會對不起來，而且這種錯誤往往要到實際比對畫面跟模型輸出時才會發現，很難靠型別檢查抓到。另外，「加了一個新欄位讓模型填」（`based_on_images`）光是加欄位本身沒有用，還要有辦法把模型填的「編號」轉換成使用者看得懂的東西（這裡是縮圖）——跟前一則「分組圖片編號」的教訓是同一件事的延伸：模型輸出的資料只有轉譯回使用者熟悉的呈現方式，才算真正把這個功能做完整。

**檔案**：`backend/app/models/material.py`（新增 `ImageRef`）、`backend/app/models/test_case.py`（`TestCase` 新增 `based_on_images`）、`backend/app/services/prompt_builder.py`（`_iter_numbered_images`／`resolve_image_numbers`，`build_material_content` 改成跨素材連續編號，兩份 prompt 加規則要求模型填 `based_on_images`）、`backend/app/routers/conversations.py`（新增 `GET /image-map`）、`frontend/src/components/ImageLightbox.tsx`（新檔，`useImageLightbox()` 共用 hook）、`frontend/src/components/MaterialRow.tsx`（改用共用 hook）、`frontend/src/components/TestCaseTable.tsx`（依據圖片縮圖）、`frontend/src/components/ChatPanel.tsx`（附加圖片可點擊放大）、`frontend/src/pages/WorkspacePage.tsx`（載入/產生/聊天後刷新 image-map）

**驗證方式**：後端 `test_prompt_builder.py` 新增測試釘住「編號跨素材連續遞增」與「`resolve_image_numbers` 跟 prompt 裡的編號完全對得起來」，`pytest tests/` 49 個測試全過。瀏覽器實測：選兩張真實截圖、呼叫真實模型產生用例，確認回傳的 JSON 裡每筆用例的 `based_on_images` 正確指向對的圖（例如根據第一張截圖寫的幾筆用例都是 `[1]`，根據第二張寫的是 `[2]`），畫面上對應用例展開後縮圖跟編號徽章正確顯示、點擊會放大且標題顯示「圖N：檔名」；素材庫裡原有的縮圖點擊放大（`MaterialRow`）重構後行為不變；聊天室貼一張截圖送出後，訊息裡的縮圖點擊也能正確放大。

## 2026-08-22 反覆「拆出→合併→拆出」，檔名尾綴無限疊加

**問題**：把圖片從群組拆出來會自動取名「原檔名（拆出的圖片）」。但如果使用者反覆操作——拆出一張圖片、把它跟別的素材重新合併、之後又想再拆出來——檔名尾綴會一直疊加，變成「a.png（拆出的圖片）（拆出的圖片）（拆出的圖片）……」沒完沒了。

**修法**：根因是 `ungroup_image` 產生新檔名時，直接在「素材當下的檔名」後面接一次「（拆出的圖片）」，完全沒有檢查這個檔名是不是本來就已經帶著這個尾綴——如果這筆素材是「上一輪拆出的結果，後來又被當成合併的主體」，它的檔名早就帶著一次尾綴了，這次再拆出就會疊成兩次，如此循環就會無限疊加下去。修法是在接上尾綴之前，先用正規表示式把檔名結尾**已經存在的**尾綴（不管有幾層）全部剝掉，剝乾淨了才接上唯一一份，這樣不管反覆操作幾輪，新拆出的素材檔名永遠只帶一份尾綴（如果剛好跟專案裡其他素材撞名，還是會照 `make_unique_filename` 既有規則加上 `(2)` 之類的編號，那是正常的撞名處理，跟這次修的「尾綴疊加」是兩件不相干的事）。

**背後的通用觀念**：**任何「在既有字串後面接一段固定文字」的邏輯，只要那個「既有字串」有可能就是前一次同一個操作的產物，就要先檢查一次會不會疊加，不能無條件接上去**——這其實是遞迴／循環操作裡很常見的一類 bug：操作本身沒問題（拆出一次是對的），但沒考慮到「這個操作的輸出，有可能變成同一個操作下一次的輸入」，兩次操作之間沒有做「等冪」處理（同一個轉換重複套用多次，結果應該跟套用一次一樣，或至少不會每次都疊加出新東西）。字串尾綴、檔名編號、路徑組合這類地方特別容易犯這個錯，因為寫程式的當下往往只想著「這次要加什麼」，沒有反過來想「如果这個值本身已經被加過一次了呢」。日後只要看到「取現有值＋固定尾綴／前綴」這種命名邏輯，就該養成習慣先問一句：這個現有值有沒有可能已經帶著同一個尾綴／前綴了？

**檔案**：`backend/app/services/project_store.py`（新增 `_UNGROUP_SUFFIX`／`_strip_ungroup_suffix`，`ungroup_image` 接尾綴前先剝乾淨）、`backend/tests/test_project_store.py`（新增測試釘住連續兩輪拆出都只帶一份尾綴，且不是巧合通過——刻意在專案裡多放一筆不相干的素材、也讓兩次拆出各自產生獨立的新素材）

**驗證方式**：`pytest` 新增的測試模擬「素材本來就已經帶著一次尾綴、又要再拆出一次」，確認結果不會疊成兩份；另外用真實 API 呼叫重現使用者回報的完整循環——上傳兩張圖片、合併、拆出（確認檔名是「scratch_r1.png（拆出的圖片）」）、再把拆出來的那張跟主體重新合併、再拆出一次（確認檔名還是同一個「scratch_r1.png（拆出的圖片）」，不是疊成兩份），兩輪結果完全一致，證明修法對得上使用者實際重現的操作路徑，不是只在單元測試裡自圓其說。

## 2026-08-22 分組圖片加上編號，模型引用時才能對得回畫面上的縮圖

**問題**：素材可以分組帶多張圖片之後，模型如果要在測試用例、備註或澄清問題裡指出「是哪一張圖」（例如「根據開關前的畫面…」），完全沒有任何依據可以精確指出是哪一張——原本送給模型的內容只有一段「這幾張圖同屬一組，請對照理解」的提示文字，後面接著幾張沒有標示的圖片，模型就算想講清楚也講不出「第幾張」，使用者讀到模型的輸出時，也沒辦法對回畫面上素材庫的哪一張縮圖。

**修法**：在 `prompt_builder.py` 裡幫每一張送給模型的圖片加上文字編號——圖片素材當主體時主圖是「圖1」、附加圖片依序是「圖2」「圖3」……；文字／PDF 素材當主體時（沒有自己的主圖）附加圖片直接從「圖1」開始編號。每張圖片前面都插入一個單獨的 `圖N：` 文字區塊，而不是只在最前面講一次「有 N 張圖」就把圖片全部丟出去；分組提示文字裡也明講「如果需要指出是哪一張，請直接用『圖1』『圖2』這種編號稱呼，不要用『上圖』『下圖』這種畫面上根本沒有編號可以對應的說法」，主動引導模型使用這套編號，而不是被動等模型自己想到要這樣做。前端同步在畫面上顯示一模一樣的編號——編輯素材視窗跟「檢視素材內容」彈窗，主圖縮圖跟每張附加圖片的縮圖左上角都疊了一個小圓形編號徽章，數字算法（`isText ? 1 : 2` 當起始偏移量）跟後端完全一致，這樣使用者讀到模型輸出裡的「圖2」，回頭到畫面上數第幾張縮圖就能對上，不用自己猜編號怎麼算的。

**背後的通用觀念**：**讓模型看到一組資料，不等於模型就有能力精確引用其中一項**——如果沒有給每一項一個明確、雙方都認得的識別方式（這裡是編號，其他情境可能是 id、名稱、座標），模型要嘛完全不敢在輸出裡指名道姓（怕講錯），要嘛用「上面那張」「其中一張」這種聽起來像在講、但使用者完全對不回去的模糊說法，兩種結果都沒有真正解決「使用者要知道模型在講哪一項」這個需求。**這類「跨模態關聯」的功能（把好幾張圖、好幾段文字綁在一起讓模型理解關聯）ship 出去之前，值得多問一句：如果模型真的想在輸出裡提到某一項，它有沒有辦法講清楚是哪一項？講清楚了，使用者看得懂嗎？** 光是把資料送過去、加一句「請對照理解」的提示語，只解決了「模型看得到全部內容」，沒解決「模型講得出、使用者聽得懂是哪一項」——這是兩件不同的事，容易漏掉後者。

**檔案**：`backend/app/services/prompt_builder.py`（每張圖片前插入 `圖N：` 文字標記，提示文字裡教模型用編號引用）、`frontend/src/components/MaterialRow.tsx`（主圖與附加圖片縮圖疊加編號徽章）、`frontend/src/components/MaterialsModal.tsx`（同步顯示編號，並在區塊標題顯示編號範圍「圖2～圖3」）、`frontend/src/index.css`（新增 `.material-thumbnail-wrap`／`.material-image-number` 樣式）

**驗證方式**：後端 `pytest`（`test_prompt_builder.py`）改寫成逐一比對每個 `content` 區塊，確認圖片素材主體時主圖標記為「圖1」附加圖片為「圖2」，文字素材主體時附加圖片從「圖1」開始，且分組提示文字裡確實包含「圖1、圖2」這樣的編號說明。瀏覽器實測：合併兩張圖片後，編輯素材視窗裡量測 `.material-image-number` 的文字內容，確認依序是「圖1」「圖2」；「檢視素材內容」彈窗同一筆素材也顯示相同編號，且區塊標題正確寫出「同一組的其他圖片（共 1 張，圖2）」。

## 2026-08-22 合併功能延伸：文字／PDF 也能當主體、圖片可以拆出群組

**問題**：前一版的「合併成一組」只允許選取的每一筆都是圖片素材。但實際情境常常是一份需求文件（PDF／貼上的文字）搭配幾張相關截圖，需要讓模型把文件內容跟截圖對照理解，而不是只有圖片跟圖片之間可以對照。另外，合併是單向操作，一旦把圖片合併進去，沒有辦法反悔、把某一張圖片單獨拆出來重新變回獨立素材，選錯了只能整組刪掉重來。

**修法**：兩個獨立的延伸，都是在既有的 `embedded_images` 機制上加規則，沒有新增資料結構。

1. **文字／PDF 當主體**：`project_store.merge_materials` 的驗證邏輯從「全部都要是圖片」放寬成「只有第一筆（主體）以外的其餘幾筆要是圖片」——第一筆選的可以是文字／PDF 素材，這樣它自己的 `text` 內容保持不變，圖片依序放進它的 `embedded_images`。前端 `MaterialGrid` 的合併模式也跟著調整：`mergeSelected.length === 0`（還沒選過任何一筆）時任何種類都可以點選當第一筆，選過一筆之後，後續只能再點圖片素材（用同一條規則 `mergeIndex >= 0 || mergeSelected.length === 0 || material.kind === 'image'` 同時處理「已選的可以取消」跟「規則限制」兩件事）。`prompt_builder.py` 原本只有圖片素材當主體時才會加「這幾張圖同屬一組」的提示文字，這次也補上文字素材版本，讓模型知道附加的圖片是跟這份文件相關（不管是 PDF 本身內嵌的，還是使用者事後合併進去的）。

2. **把圖片從群組裡拆出來**：新增 `POST /materials/{id}/ungroup`（body 帶 `index`）與對應的 `project_store.ungroup_image`——把某筆素材 `embedded_images` 陣列裡第 `index` 張圖片移除，另外開一筆全新的獨立圖片素材承接那張圖片內容，檔名自動取成「原檔名（拆出的圖片）」並確保不撞名。前端在編輯素材視窗裡每張附加圖片的縮圖上，滑鼠移過去會浮現一顆小小的「拆出」按鈕（跟垃圾桶圖示同一套 hover 顯示的模式）。

**背後的通用觀念**：這兩個延伸都在示範同一件事——**先設計好「資料結構本身能表達什麼」，之後功能的增減常常只是調整驗證規則跟操作介面，資料結構完全不用動**。`embedded_images` 從一開始的本質就是「一筆素材可以附掛好幾張圖片」，跟「誰可以當主體」「附掛的圖片能不能反悔移除」都是額外疊加的業務規則，不是資料本身的限制；能不能做這件事，取決於當初設計欄位時有沒有把它做得夠通用，而不是每次有新需求就重新想一套資料格式。另外，「拆出」是「合併」的逆操作，這種**成對出現的正反操作**（合併／拆散、新增／移除、鎖定／解鎖）在設計功能時值得養成習慣主動想一下：使用者做了一個不容易復原的動作（這裡是合併，一旦做了原本兩筆素材的邊界就消失了），是不是應該同時提供對應的反悔／復原路徑，而不是只做了「做」卻沒做「回頭」。

**檔案**：`backend/app/services/project_store.py`（`merge_materials` 放寬驗證只檢查 `rest`；新增 `ungroup_image`）、`backend/app/routers/projects.py`（新增 `ungroup` 端點與 `UngroupImagePayload`）、`backend/app/services/prompt_builder.py`（文字素材有 `embedded_images` 時也加分組提示）、`frontend/src/components/MaterialGrid.tsx`（合併資格判斷改成看選取順序）、`frontend/src/components/MaterialRow.tsx`（每張附加圖片加「拆出」按鈕）、`frontend/src/api.ts`（新增 `ungroupImage`）、`frontend/src/pages/MaterialLibraryView.tsx`／`WorkspacePage.tsx`（串接 `onUngroupImage`）

**驗證方式**：後端 `pytest` 新增／調整多個測試——文字素材當主體合併成功並保留 `text`、文字素材出現在非第一筆時合併失敗、拆出圖片後原素材與新素材各自內容正確、拆出索引超出範圍或素材不存在時不做任何變動；`prompt_builder.py` 也補了文字素材有分組提示的測試，並把舊測試（斷言文字素材不該有提示）改成反映新行為。瀏覽器實測：建一個文字素材＋兩張圖片，合併模式裡先點文字素材（確認徽章 1）、再依序點兩張圖（確認徽章 2、3 且都能選取），合併後卡片正確顯示「🖼️ 2」徽章且素材種類仍是文字（圖示仍是 📝）；接著在編輯視窗裡點其中一張附加圖片的「拆出」按鈕，確認素材庫從 1 筆變 2 筆、原素材徽章從 🖼️2 變 🖼️1、新拆出的素材正確帶著那張圖片的內容獨立存在。

## 2026-08-22 素材庫新增「合併成一組」與對話選取畫面補上刪除／合併

### 1. 讓模型知道好幾張圖片是同一組（例如開關前／開關後）

**問題**：有些介面只看單一畫面看不出行為，要對照兩張（或更多）截圖才看得出差異，例如同一個開關「切換前」跟「切換後」的畫面。原本每個素材只能裝一張圖片，模型看到的是好幾張互不相關的圖片，沒辦法知道「這幾張其實是同一組、要對照著看」，也沒辦法知道彼此的先後順序。

**修法**：後端 `ParsedMaterial` 本來就有 `embedded_images`（多圖片陣列）欄位，只是原本只有解析 PDF、把 PDF 內夾帶的截圖抽出來時才會用到。這次沒有另外新增欄位，而是重用同一個機制：新增 `POST /api/projects/{id}/materials/merge` 端點跟對應的 `project_store.merge_materials`，讓使用者可以在素材庫（或對話的素材選取畫面）裡**事後**選取多筆既有的圖片素材，按下「合併成一組」，第一筆點的當主圖（`image_data_url`），其餘依點選順序放進主圖的 `embedded_images`，其餘幾筆素材本身接著被刪除；如果選到的素材裡本來就已經是別的合併組（自己就帶著 `embedded_images`），也會一併攤平保留，不會弄丟。前端 `MaterialGrid` 新增一個「合併模式」：點「合併素材成一組」進入後，點卡片不會再打開編輯視窗、改成勾選要合併的圖片（用數字徽章標示點選順序），選滿 2 張以上才能按下確認。`prompt_builder.py` 也一併修正一個既有死角：原本 `embedded_images` 只有文字素材（PDF）會送給模型，圖片素材的 `embedded_images` 直接被忽略；改成兩種 kind 都會送，且圖片素材有 `embedded_images` 時額外插入提示文字：「以下 N 張圖片跟上面這張同屬一組……通常代表同一畫面在不同狀態或操作前後的對照，請對照理解」，明確告訴模型這些圖片彼此相關。

**檔案**：`backend/app/routers/projects.py`（新增 `merge_materials` 端點）、`backend/app/services/project_store.py`（新增 `merge_materials` 函式）、`backend/app/models/material.py`（更新 `embedded_images` 欄位註解）、`backend/app/services/prompt_builder.py`（`embedded_images` 迴圈移出 kind 判斷式、圖片素材加分組提示文字）、`frontend/src/components/MaterialGrid.tsx`（合併模式：切換按鈕、卡片點選邏輯、數字徽章、確認列）、`frontend/src/api.ts`（新增 `mergeMaterials`）、`frontend/src/pages/MaterialLibraryView.tsx`／`frontend/src/pages/WorkspacePage.tsx`（串接 `onMergeMaterials`）

**驗證方式**：後端 `pytest`（`test_project_store.py` 新增 4 個、`test_prompt_builder.py` 3 個）涵蓋合併成功、合併保留雙方原有的 `embedded_images`、選到非圖片素材時整個合併不執行、選到不存在的素材時整個合併不執行。瀏覽器實測：分兩次個別上傳兩張圖片（模擬先貼上截圖、事後才想合併的真實用法）→ 在素材庫裡進入合併模式、依序點選兩張卡片（確認數字徽章顯示 1、2）→ 按下合併，確認素材數從 2 變 1、卡片顯示「🖼️ 1」徽章；再用 API 逐位元組比對，確認主圖／附加圖片內容分別跟原始兩個檔案完全一致，不是弄混或弄丟。

### 2. 走了一次錯路：一開始做成「上傳時勾選分組」，使用者根本用不到

這次的設計不是一次到位，值得記下來的是**為什麼一開始的做法是錯的**，而不只是最後做對的版本。

**最初的版本**：在上傳檔案的 `<input type="file" multiple>` 旁邊加一個「這次選的圖片要合併成一組」勾選框，邏輯是「先勾選、再選檔案、選完立刻上傳成一筆」。程式碼本身沒有 bug，型別檢查、單元測試、瀏覽器操作全部驗證過都正常。

**問題**：使用者實際的操作習慣是**先截圖、直接貼上（Ctrl+V）**，根本不會用「一次選好幾個檔案」這種上傳方式——所以這個功能對她來說完全不存在，因為她的操作路徑從來不會經過那個勾選框。就算真的用了檔案選取視窗，「先勾選、再選檔案」這種順序本身也不直覺：使用者要在看到自己選了哪些檔案之前，就先決定好要不要分組，等於要求她事先規劃好，而不是選完之後再決定。

**修法**：整個推翻重做——拿掉上傳時的勾選框，改成「先讓素材各自存在（不管是貼上還是上傳），之後在素材庫／對話選取畫面裡回頭選取既有的卡片、按下合併」，也就是本節第 1 點最終採用的設計。

**背後的通用觀念**：**這是「有沒有先問清楚使用者實際的操作方式」的教訓，不是技術問題**。功能本身邏輯正確、測試也綠燈，並不代表使用者用得到——如果沒先確認清楚使用者的真實使用情境（她用什麼方式建立素材、習慣的操作順序是什麼），很容易做出一個自己覺得合理、但完全接不上使用者實際動線的功能。看到使用者回報「看不出來怎麼操作」時，第一直覺不該是「把說明文字改大、改明顯」，而是先確認**功能出現的時機、順序，是不是跟使用者真正的操作流程對得上**——文字再清楚，擺在使用者根本不會經過的地方也沒用。

### 3. 對話的素材選取畫面原本完全不能刪除素材

**問題**：專案素材庫頁面（`MaterialLibraryPanel`）的卡片可以直接刪除素材，但對話裡「選擇要使用的素材」畫面（`MaterialSelector`，只在對話還沒產生過結果時出現）完全沒有刪除按鈕——這是刻意設計的（程式碼註解寫「對話素材選取畫面不給，避免誤刪整個專案共用的素材」），但實際用起來很不方便：使用者常常就是在這個畫面裡邊挑素材邊發現某張截圖貼錯了、想刪掉重貼，卻只能先跳出去、找到專案素材庫頁面才能刪，多繞一大圈。

**修法**：拿掉這個限制，讓 `MaterialSelector` 跟 `MaterialLibraryPanel` 一樣可以刪除素材——`MaterialGrid` 本來就用同一個 `onRemoveMaterial` prop 控制要不要顯示刪除鈕（有給才顯示），只是 `MaterialSelector` 原本沒有把這個 prop 接上去、`WorkspacePage` 也還沒有實作對應的刪除處理函式，這次一併補上。刪除素材本身的風險（誤刪整個專案共用的素材）並沒有消失，只是重新判斷後認為「防呆」帶來的操作阻力比「保護使用者不誤刪」的價值更高——刪除鈕本身還是有 `window.confirm` 二次確認，且後端刪除素材時本來就會自動把所有對話（含目前這個）persist 過的勾選狀態一併清乾淨（`project_store.delete_material` 呼叫 `conversation_store.remove_material_from_all_conversations`），不會留下懸空的殘留狀態。

**背後的通用觀念**：**「防呆設計」本身也是有代價的，不是加了就一定更安全**。當初拿掉刪除鈕是為了防止使用者在對話畫面誤刪整個專案共用的素材，出發點合理；但這個限制實際造成的摩擦（想刪個東西卻要先跳頁面去找）比它防住的風險更常發生、更讓人困擾。判斷一個限制該不該存在，不能只看「這樣做比較安全」，要回頭看「使用者實際上會不會因此而困擾」，兩者要一起衡量；已經有二次確認對話框擋著的操作，通常代表「防呆的責任」已經轉嫁給那個確認框了，不需要再疊加一層「乾脆不給這個功能」的更重防呆。

**檔案**：`frontend/src/components/MaterialSelector.tsx`（新增 `onRemoveMaterial` prop 並接到 `MaterialGrid`）、`frontend/src/pages/WorkspacePage.tsx`（新增 `handleRemoveMaterial`，刪除後同步過濾本地的 `selectedMaterialIds`，避免下次勾選變動時把已刪除的 id 又存回去）

**驗證方式**：瀏覽器實測，切到一個還沒產生過測試用例的對話，展開素材選取畫面裡的素材庫清單，確認每張卡片都出現刪除鈕（`document.querySelectorAll('.material-card-delete').length` 對上素材數）。

**檔案**：`backend/app/models/material.py`（更新 `embedded_images` 欄位註解，反映新的用途）、`backend/app/routers/projects.py`（`upload_materials` 新增 `group` 參數與驗證邏輯）、`backend/app/services/prompt_builder.py`（`embedded_images` 迴圈移出 `text`/`image` 的判斷式、改成兩種 kind 都會送；`image` kind 且有 `embedded_images` 時插入分組提示文字）、`frontend/src/components/MaterialLibraryPanel.tsx`（新增分組勾選框）、`frontend/src/api.ts`（`uploadMaterials` 新增 `group` 參數）、`frontend/src/pages/MaterialLibraryView.tsx`（把 `group` 往下傳）、`frontend/src/components/MaterialRow.tsx`（多圖片區塊的標籤文字依素材種類調整，圖片組不再顯示「文件內夾帶的圖片」這種只適用 PDF 情境的措辭）

**驗證方式**：後端補了 `backend/tests/test_prompt_builder.py` 三個單元測試，釘住「單張圖片素材沒有分組提示」「分組圖片素材會送出所有圖片＋分組提示文字」「PDF 文字素材的既有行為不受影響（不會混進新的分組提示）」三種情境；`pytest` 29 個測試全數通過。瀏覽器／curl 實測：直接呼叫 API 上傳 2 張圖片並帶 `group=true`，確認回應只產生 1 筆素材、`embedded_images` 正確包含第 2 張圖；驗證「只選 1 張圖 + `group=true`」與「混了非圖片檔 + `group=true`」都會被擋下並回傳明確錯誤訊息；瀏覽器裡打開這筆分組素材的編輯畫面，確認卡片徽章顯示「🖼️ 1」、詳細畫面正確顯示「同一組的其他圖片（共 1 張）」區塊與縮圖。

## 2026-08-22 換網路環境後前端連不到後端、專案清單變空白

**問題**：主環境（`http://localhost:5173`）打開後顯示「還沒有任何專案」，但後端的 `backend/data/projects/` 資料夾裡其實有真實資料（`DIATwin` 專案，12 項素材、3 個對話）。瀏覽器 console 有 `net::ERR_CONNECTION_TIMED_OUT`，代表前端根本連不上後端，不是後端真的把資料弄丟了——這點很容易被誤會成資料遺失，其實只是連不到而已。

**修法**：根因是 `frontend/.env` 裡 `VITE_API_BASE_URL` 寫死了 `http://10.197.176.170:8000`，但這台機器目前的區網 IP 已經是 `192.168.1.x` 系列（用 `Get-NetIPAddress` 查出來的），`10.197.176.170` 這個位址現在打不通了，八成是先前接的網路環境（例如公司網路）跟現在的網路不一樣。把 `frontend/src/api.ts` 裡 `API_BASE_URL` 沒設環境變數時的預設值，從寫死的 `'http://localhost:8000'`，改成用 `` `http://${window.location.hostname}:8000` `` 動態算出來——`window.location.hostname` 就是瀏覽器當下實際打進來用的那個網域／IP，用 `localhost:5173` 開就會算出 `localhost:8000`，用區網 IP（例如手機、別台電腦連進來）開就會算出對應那個區網 IP 的 `:8000`，永遠不用手動維護、也不怕之後又換網路環境。`VITE_API_BASE_URL` 仍然保留為可選的覆蓋機制（例如測試環境後端不是 8000 埠，就要靠這個環境變數明確指定）。同時清掉 `frontend/.env` 裡那行過期的寫死 IP，讓它退回新的自動偵測預設值，立即解除使用者當下被卡住的狀態。

**背後的通用觀念**：「前端要呼叫的後端位址」這種設定，只要牽涉到 IP，就要想清楚它的值**什麼時候會失效**——寫死一個當下量到的 IP，本質上是把「這台機器現在恰好是這個網路身分」的暫時事實，錯當成永久不變的設定存起來，一旦機器換了網路（DHCP 重新配址、換公司/換家裡網路、接了不同的 VPN），這個值就直接過期，而且失效時完全沒有提示，只會安靜地連線逾時，很容易被誤診斷成別的問題（這次就差點被誤會成「資料被弄丟了」）。**只要前後端本來就佈署在同一台主機上，`window.location.hostname`（或更廣義地說，「請求當下實際使用的位址」）幾乎永遠是比寫死 IP 更正確的預設值**，因為它是瀏覽器連進來的當下才決定的，天生就跟著使用者實際的存取路徑走，不會過期。這也是同一類問題的其中一種：任何組態設定只要記錄的是「環境當下的某個快照」而不是「怎麼推導出這個值」，都有一天會變成過期資訊，效果不明顯、直到某天環境變了才會爆出來。

**檔案**：`frontend/src/api.ts`（`API_BASE_URL` 的預設值改成動態算出）、`frontend/.env.example`（更新註解說明新的預設行為）、`CLAUDE.md`（更新啟動步驟裡對這行的說明）、`frontend/.env`（本機設定檔，不進版控，清掉過期的寫死 IP）

**驗證方式**：先直接量到這台機器現在的真實區網 IP（`Get-NetIPAddress`），確認跟 `.env` 裡寫的完全不一樣，證實過期推論成立；清掉 `frontend/.env` 裡的覆蓋值後，瀏覽器重新整理 `http://localhost:5173`，原本顯示「還沒有任何專案」的畫面正確變回顯示 `DIATwin` 專案內容，證明「不設定環境變數、退回自動推斷」這條路徑本身是通的。`window.location.hostname` 這個運算式在 LAN IP 存取情境下的正確性直接源自瀏覽器 API 本身的定義（存取當下用的是哪個網域/IP，這個屬性就回傳哪個），屬性用法本身沒有模稜兩可的空間，不需要另外架設雙後端情境反覆驗證。

## 2026-08-21 手動編輯測試用例（新增步驟／用例、改欄位）不會自動存檔

**問題**：在測試用例表格裡手動新增步驟、新增用例、或直接編輯欄位文字，這些改動只會反映在畫面上；只要不送出聊天訊息、也不按「匯出 Excel」，改動就完全沒有存進後端。使用者這時如果重新整理頁面、或切到別的對話再切回來，前端會重新從後端抓資料回來蓋掉畫面，剛剛手動編輯的內容就無聲無息消失，沒有任何提示或警告。

**修法**：根因是 `WorkspacePage.tsx` 裡 `TestCaseTable` 的 `onChange` 只呼叫 `setResult(...)` 更新 React 本地狀態（`frontend/src/pages/WorkspacePage.tsx:367`），從來沒有呼叫任何 API。後端真正把 `test_cases` 寫進 `conversation.last_result`（`PUT .../test-cases`，對應前端的 `updateTestCases`）的時機，原本只有兩個：送出聊天訊息（`/chat` 端點內部處理）、按下匯出前手動呼叫一次。純手動編輯完全沒有觸發到這兩條路徑中的任何一個。修法是新增一個 `useEffect`，監看 `result` 這個狀態，只要它變動就 debounce 1 秒後呼叫 `updateTestCases` 存回後端；用 `skipNextAutosaveRef` 這個旗標讓「剛從後端載入資料、或切換對話造成的那一次 `result` 變動」不要被誤判成使用者編輯而觸發一次不必要的存檔。

**背後的通用觀念**：這是很典型的「畫面狀態」跟「持久化狀態」沒有同步的 bug——UI 上看起來運作正常（打字、新增都會馬上反映在畫面上），使用者完全感覺不出來「這個改動其實還沒真的存進去」，只有在剛好觸發別的路徑存檔、或剛好沒有觸發時才會露餡，非常容易被忽略、也很難單靠肉眼盯著畫面測出來（因為當下畫面看起來完全正常）。**只要一個功能有「使用者編輯了東西、畫面立刻反映出來」的體驗，就要主動問一句：這個改動實際上是什麼時候真正存進後端的？是每次編輯都存、還是要等某個特定動作（送出、離開頁面、按存檔鈕）才存？**如果答案是「要等某個特定動作」，那所有「不會觸發那個動作」的操作路徑（像這次的純手動編輯）都要逐一檢查是不是真的有涵蓋到，不能只驗證最常見的那條路徑（這次最常見的路徑是「聊天」，剛好把「手動編輯」這條路徑漏掉了）。debounce（延遲一小段時間、期間有新變動就重新計時，只在真正停下來後才觸發一次）則是「頻繁變動的狀態要存檔，但又不想每個按鍵都打一次 API」的標準解法。

**檔案**：`frontend/src/pages/WorkspacePage.tsx`（新增 `skipNextAutosaveRef`；新增 debounce 自動存檔的 `useEffect`；載入資料的 `useEffect` 開頭補上重置旗標）

**驗證方式**：瀏覽器實測——在對話裡新增一筆全新測試用例（不聊天、不匯出），用 `navigate` 觸發整頁重新載入（等同使用者重新整理），確認畫面上仍顯示這筆新用例（用例數從 6 筆變 7 筆且重新整理後維持 7 筆）；接著刪除這筆測試用的用例、同樣重新整理，確認畫面正確變回原本的 6 筆，證明新增與刪除兩種編輯都有被自動存檔涵蓋到，而不是只驗證了其中一種操作就下結論。

## 2026-08-21 測試步驟拖曳握把與刪除按鈕排版修正

### 1. 拖曳握把跟步驟編號的排版，一連三輪才修乾淨

**問題**：測試步驟表格第一欄（多步驟時才會顯示的拖曳握把＋步驟編號）先後出現三個問題，一輪一輪浮現：(1) 握把在上、編號在下疊成兩行，不是同一行水平排列；(2) 修完排版後，握把圖示本身用肉眼看是歪斜、不對稱的；(3) 修完圖示後，使用者截圖回報**整張表格**的格線跟著跑掉、看起來是歪的。

**修法**：三輪根因完全不同，依序記錄：

1. 排版疊成兩行：`.step-drag-grip` 這個 CSS class 寫死了 `display: block`，會強制自己獨佔一行、把後面緊接著的步驟編號文字往下推。改成 `display: inline-flex`。
2. 圖示本身歪斜：原本的握把是直接寫死的 Unicode 字元 `⠿`（Braille 六點圖案），外觀完全交給瀏覽器當下用的字型去畫，不同字型畫出來的六個點不保證排成工整方格。換掉整個實作方式，改成手畫一顆行內 SVG，用 6 個 `<circle>` 精確指定座標排成 2 欄 3 列——SVG 是向量圖形，座標寫死多少就是多少，不受字型影響。
3. 整張表格跟著歪掉：為了讓握把＋編號水平排列，第一輪順手把 `display: flex` **直接設在 `<td>` 本身**上。這一步是這次問題的真正禍首——`<td>` 瀏覽器預設的 `display` 是 `table-cell`，這個值讓它乖乖參與表格的行／列格線計算；一旦用 CSS 把它的 `display` 整個覆寫成 `flex`，瀏覽器就不再把它當一般的表格儲存格看待，它跟同一列其他仍是 `table-cell` 的儲存格之間，格線對齊的計算方式就不一致了，導致整張表格的框線跟著跑掉。修法是把 `display: flex` 從 `<td>` 身上整個拿掉、還給它預設的 `table-cell`，改成在 `<td>` 裡面多包一層 `<span className="step-number-inner">`，只在這個內層的 `span` 上做 `display: inline-flex`，讓「水平排列握把跟編號」這件事只發生在儲存格內部，不去動到 `<td>` 對外參與表格佈局的身分。

**背後的通用觀念**：三輪疊在一起，剛好對應三種不同層次的排版錯誤，值得一起記住：
- `display: block` 的元素預設一定像段落一樣自己另起一行——看到「兩個本來想並排的東西卻上下疊在一起」，先檢查是不是有元素被設成（或預設就是）`block`。
- 任何「拿 Unicode 符號／emoji 字元當圖示用」的做法，外觀都不是自己決定的，是使用者當下的作業系統／瀏覽器／字型決定的——只要圖示長相對體驗有影響，就該用 SVG 精確畫出來，不要賭字元在對方機器上長得夠好看。
- **`<table>`／`<tr>`／`<td>` 這類表格相關元素，`display` 屬性不是「純粹的排版工具」，而是它們「是不是仍被瀏覽器當成表格的一部分」的身分標記**。像 `td { display: flex }` 這種直接覆寫表格元素本身 `display` 的寫法，看起來只是想要「這個儲存格內容水平排列」，但實際上動到的是這個儲存格「還算不算表格網格的一份子」，副作用往往要等到多列一起顯示、格線互相比較時才會現形，光看單一儲存格的截圖很容易發現不了。**日後只要是要調整表格儲存格「裡面內容」的排列方式，一律在儲存格內部再包一層元素做 flex/grid，永遠不要直接改 `<table>`／`<tr>`／`<td>` 本身的 `display`**。

**檔案**：`frontend/src/index.css`（`.step-drag-grip` 的 `display` 由 `block` 改成 `inline-flex`；新增 `.step-number-inner` 承接原本掛在 `<td>` 上的 `display: flex` 排版；`td.step-number-cell` 的 `display` 恢復瀏覽器預設，不再覆寫）、`frontend/src/components/TestCaseTable.tsx`（握把內容從 `⠿` 字元改成 6 個 `<circle>` 組成的行內 SVG；握把＋編號多包一層 `<span className="step-number-inner">`）

**驗證方式**：瀏覽器裡用 JS 量測實際渲染出來的 DOM 座標。排版部分確認握把的 `right` 邊界小於等於編號文字的 `left` 邊界、儲存格高度回到單行高度；SVG 部分量測 6 個 `<circle>` 的實際渲染座標，確認只有 2 個不重複的 x 值、3 個不重複的 y 值，排成工整對稱的 2×3 方格；**表格格線部分**額外量測 `getComputedStyle(td).display` 確認真的是 `table-cell`（不是被覆寫成 `flex`），並比較多列同一欄位的 `getBoundingClientRect().left/right`，確認所有列的邊界完全相等——這一項是這次「表格整體歪掉」問題學到的教訓：**光看單一儲存格量得到的數字漂亮沒有用，一定要跨列比較才驗證得出「表格格線有沒有跑掉」這種問題**，之前兩輪只驗證了單一儲存格內部的相對位置，沒抓到這個。

### 2. 刪除按鈕文字「刪除」被壓成直排

**問題**：測試步驟每一列最右邊的「刪除」按鈕，在畫面空間較窄時，「刪」「除」兩個字會各自換行變成直的兩行，而不是正常橫向的一個詞。

**修法**：全站共用的 `button` 基礎樣式沒有設定 `white-space`，瀏覽器預設值是 `normal`——代表文字在容器不夠寬時可以在任何字元邊界換行。測試用例的步驟表格用的是預設的 `table-layout: auto`，欄寬會依整個表格可用空間動態分配；當表格所在的容器（例如聊天面板跟表格左右並排時，表格拿到的寬度變窄）擠壓到最後一欄，瀏覽器就會讓「刪除」這種沒有空格可斷詞的兩個中文字，直接從字元中間斷開換行。修法是幫全域 `button` 補上 `white-space: nowrap`，明確告訴瀏覽器「這段文字永遠不准自動換行」。

**背後的通用觀念**：CSS 版面配置時，瀏覽器會盡量把內容塞進可用空間，`white-space: normal`（預設值）允許文字在空白字元或（對中日韓文字而言）任何字元邊界斷行；按鈕、標籤、徽章這類「短文字、寧可讓外框稍微溢出也不該斷行」的 UI 元件，通常都要主動加上 `white-space: nowrap` 表態，不能依賴瀏覽器的預設換行行為，尤其中文沒有空格分詞，比英文更容易在意想不到的地方斷開。這次順手把這個規則加在全站共用的 `button` 基礎樣式上，而不是只修這一個按鈕，是因為同一個問題理論上任何按鈕在夠窄的容器裡都會發生，屬於「基礎元件該有的預設行為」而不是這個特定畫面的特例。

**檔案**：`frontend/src/index.css`（全域 `button` 規則新增 `white-space: nowrap`）

**驗證方式**：瀏覽器裡量測刪除按鈕的實際渲染尺寸，確認 `white-space` 電腦運算後的值是 `nowrap`，且按鈕高度是正常單行高度（40px），不是雙倍高度。



### 1. 素材卡片可直接刪除，不用先點開

**問題**：素材庫的每張卡片點下去只會打開編輯彈窗，要刪除素材必須先點開、在彈窗裡才找得到刪除鈕。

**修法**：`MaterialGrid.tsx` 的卡片裡加一顆垃圾桶圖示按鈕（沿用 `Sidebar.tsx` 既有的 SVG 路徑跟「hover 才顯示」的樣式模式），按鈕 `onClick` 加 `e.stopPropagation()`，避免冒泡觸發卡片本身「點下去開編輯彈窗」的 onClick；沿用既有的 `onRemoveMaterial` callback 跟 `window.confirm` 二次確認文案（跟原本編輯彈窗裡的刪除鈕維持同一套用詞）。只有 `onRemoveMaterial` prop 有給的地方才會顯示這顆按鈕——對話素材選取畫面（`MaterialSelector`）刻意不給這個 prop，避免在那個畫面誤刪整個專案共用的素材。

**背後的通用觀念**：**事件冒泡（event bubbling）**。整張卡片本身就掛了 `onClick`（點卡片開編輯視窗），垃圾桶按鈕是卡片內部的子元素。子元素被點擊時，瀏覽器預設會讓這個點擊事件沿著 DOM 樹一路往父層「冒泡」上去——不擋的話，點垃圾桶會同時觸發「刪除」跟「打開編輯視窗」兩件事。`e.stopPropagation()` 就是明確告訴瀏覽器「這個事件處理到我這裡就好，不要再往上傳」。這是很常見的模式：只要一個可點擊的容器裡塞了另一個可點擊的小按鈕（刪除鈕、勾選框、連結中的按鈕……），幾乎都要記得擋冒泡，程式碼裡的 checkbox（`onClick={(e) => e.stopPropagation()}`）本來就已經示範過同一招。

**檔案**：`frontend/src/components/MaterialGrid.tsx`（新增刪除按鈕與 `handleDeleteClick`）、`frontend/src/index.css`（新增 `.material-card-delete`，預設 `display: none`，`.material-card:hover` 時才 `display: flex`）

### 2. 彈出視窗在面板內選字、放開在外面會誤觸關閉

**問題**：`MaterialsModal`、`MaterialGrid` 的編輯素材彈窗、`MaterialRow` 的圖片放大預覽，這三個彈窗原本都是在 `.modal-overlay` 上掛 `onClick={onClose}` 判斷「點擊遮罩＝關閉」。但如果使用者在彈窗面板內選取文字、往面板外拖曳才放開滑鼠，瀏覽器合成 `click` 事件時，事件的 `target` 是 mousedown 與 mouseup 兩者的最近共同祖先——也就是遮罩本身——跟真的點擊遮罩沒有任何區別，因此會被誤判成「點擊外部」而意外關閉視窗。同一套邏輯下「縮放說明框」等超出面板範圍的區塊也會有一樣的誤觸風險。

**修法**：抽出共用元件 `ModalOverlay.tsx`，改用 `useRef` 記錄 `mousedown` 事件當下是否真的直接按在遮罩本身（`e.target === e.currentTarget`）——如果 mousedown 的 target 是面板內部的某個子元素（例如文字節點），就代表這是一次「從面板內部開始」的操作，不算「點擊外部」的意圖。`click` 事件觸發時再次確認「mousedown 當時就在遮罩上」且「click 本身的 target 也是遮罩」兩個條件同時成立，才真正呼叫 `onClose()`。三個用到彈窗的地方都換成使用這個共用元件，不再各自重複判斷邏輯。

**背後的通用觀念**：瀏覽器的 `click` 事件不是看「滑鼠放開（`mouseup`）的地方在哪」，而是看 **`mousedown` 按下的元素**跟 **`mouseup` 放開的元素**，兩者「最近的共同祖先」是誰，`click` 就發生在那個祖先身上——這是規格定義好的行為，不是 bug。所以任何「點擊外部關閉」（click-outside-to-close）的實作，只看 `click` 事件的 `target` 都不夠精確，正確做法是額外記錄 `mousedown` 的起點，確認「這次點擊真的從外部開始」才關閉。這是一次學會、到處都能用的模式——很多現成 UI 函式庫的 `useClickOutside` hook，內部也是同樣的邏輯。

**檔案**：新增 `frontend/src/components/ModalOverlay.tsx`；改用它的 `frontend/src/components/MaterialsModal.tsx`、`MaterialGrid.tsx`、`MaterialRow.tsx`

**驗證方式**：瀏覽器裡用 JS 手動 dispatch 事件序列驗證兩種情境——(a) mousedown 的 target 設在面板內的 `<pre>` 文字節點、mouseup／click 的 target 落在遮罩本身：確認彈窗**不會**關閉；(b) mousedown 與 click 都直接發生在遮罩本身：確認彈窗**仍然會**正常關閉，避免修過頭導致「點遮罩想關閉」失效。

### 3. 測試用例展開／收合只有箭頭能點

**問題**：`TestCaseTable.tsx` 裡每筆測試用例的展開/收合只綁在小箭頭 `<button className="case-expand-toggle">` 的 `onClick` 上，點名稱、優先級、步驟數所在的那一整排空白處完全沒反應，點擊熱區太小。

**修法**：把 `onClick={() => toggleExpanded(caseIndex)}` 移到整個 `.case-card-header` 容器上。連帶要處理兩個會被誤觸的子元素：(1) 箭頭按鈕自己的 `onClick` 補上 `e.stopPropagation()`——否則點箭頭時會變成「箭頭自己 toggle 一次 → 事件冒泡到 header 又 toggle 一次」，兩次互相抵銷、畫面看起來沒反應；(2) 展開狀態下顯示的用例名稱 `<input>` 也補上 `e.stopPropagation()`，避免使用者點進輸入框想編輯名稱時誤觸收合。CSS 幫 `.case-card-header` 加 `cursor: pointer` 提示整塊可點，`input.name-input` 覆寫回 `cursor: text` 維持輸入框該有的游標樣式。

**背後的通用觀念**：跟第 1 點是同一個「事件冒泡」原理，但這次是反過來——不是要擋冒泡，而是刻意**利用**冒泡讓「點擊熱區」擴大到整個容器，同時又要防止容器裡原本各自獨立的可點擊子元素（箭頭按鈕、輸入框）被連帶誤觸。判斷準則很單純：一個容器要嘛整個當成一個點擊目標，要嘛容器裡的子元素各自獨立處理自己的點擊——把這兩種模式混在一起用時（容器有 onClick，子元素也有自己的 onClick），子元素就一定要用 `stopPropagation()` 明確表態「我要獨立處理，不要讓外層也跟著反應」，不然行為會變得不可預期（像這裡的兩次 toggle 互相抵銷）。

**檔案**：`frontend/src/components/TestCaseTable.tsx`、`frontend/src/index.css`

---

**這次修改的共通驗證方式**：三項都先在 git worktree 開出的獨立測試環境（port 18002 後端／5175 前端，`feature/material-library-ux-fixes` 分支）用瀏覽器實測過，過程中未動到使用者正在用的主環境（8000／5173）；`npx tsc --noEmit` 型別檢查全數通過。
