# 內部部署 LLM，OpenAI 相容介面設定（注意結尾要含 /v1，SDK 會自動接上 /chat/completions）
LLM_BASE_URL=https://your-internal-llm-endpoint/v1
LLM_API_KEY=your-api-key
LLM_MODEL=your-model-name

# 前端開發伺服器來源，供 CORS 白名單使用（多個以逗號分隔）
CORS_ORIGINS=http://localhost:5173
