# 不設定這行的話，前端會自動用「瀏覽器當下打進來的網域／IP」組出後端位址（假設後端在
# 同一台主機的 8000 埠）：用 http://localhost:5173 開就打 localhost:8000，用區網 IP
# （手機、別台電腦連進來）開就打同一個區網 IP:8000，不用手動維護、也不怕換網路環境
# （DHCP 重新配到不同 IP）就打不通。
#
# 只有在後端不是 8000 埠（例如測試環境用 18002），或前後端根本不在同一台主機時，才需要
# 取消註解、明確指定：
# VITE_API_BASE_URL=http://localhost:8000
